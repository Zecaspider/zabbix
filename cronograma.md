# Cronograma de Actividades — BPC-Observe

> **Painel de controlo vivo.** Única fonte de verdade do progresso. Actualizar
> a cada ponto concluído (marca o estado + data + nota). O
> `documentacao/engenharia-do-sistema.md` define *como* se faz; este define
> *onde estamos*.
>
> Legenda: ☐ pendente · ◐ em curso · ☑ concluído · ⏸ bloqueado · ✖ descartado
> Cada ponto só passa a ☑ quando cumpre o DoD (engenharia §10.1).

Última actualização: 2026-07-06 (Fase 10.6: remap do N1 para modelo em camadas chão-de-fábrica→enduser — card Agências removido (era duplicado conceptual de Rede, já documentado como seu segmento), 8 cards reordenados fisico→virtualização→compute/storage→rede→dados→APIs→negócio, com Segurança marcada como transversal; "Serviços de Negócio"→"Serviços de Negócio (Aplicações)" para clarificar fronteira com "APIs & Serviços". Gatilho: utilizador questionou terminologia "domínio" e sobreposição de cards durante a validação pré-demo. Fase 15.F: resolvidos os 2 duplicados de grupo confirmados na 15.E — `655`/`656` (655 vazio, apagado) e `412`/`416` GitLab (fundido para `412`, renomeado para grafia oficial, `416` apagado); ambos os hosts GitLab confirmados desactivados (QA), housekeeping de baixo risco. Fase 1.24: validação de drill-down completa N1→N2→N3 do domínio VMware antes de demo ao cliente — 2 bugs reais encontrados e corrigidos (confirmados pelo utilizador antes do push): (1) `l2-vcenter.js` ligava N2→N3 vCenter usando `hostid` em vez do nome do host, partindo a âncora do N3 em todos os 4 vCenters; (2) `l2-vcenter.js`+`l3-vcenter-topo.js` usavam o grupo Zabbix errado (`608`, só VMs/appliances) para contar "Hosts ESXi", em vez do `603` (onde estão os 20 ESXi reais) — números antes do fix eram plausíveis mas totalmente errados (ex: PowerFlex "17 ESXi" eram na verdade 17 VMs). Armazenamento e Servidores Virtuais validados na mesma passagem, sem problemas. Fase 15.E: regressão de 7 dashboards Grafana ("N1 Visão Geral" e outros 6) por âncora Storage desactualizada — grupo Zabbix `602` foi renomeado em 07-04 (2→1 espaço, correcção de higiene já documentada), mas nenhum dashboard foi actualizado em conjunto; 29 alvos corrigidos via API, verificado ao vivo com `ds/query` (3591 pontos de dados). Auditoria alargada de grupos confirma 2 duplicados ainda por resolver (655/656 SEGURANÇA/SEGURANCA, Git lab/Gitlab) — só documentado, sem escrita. `push_panel.py::ANCHOR_TARGET` corrigido para não repetir o bug em painéis futuros. Sessão anterior (2026-07-05, Fase 14.28): registada pendência formal de auditoria completa das 292 VMs de Produção — só 33 (11%) têm tag `servico` confirmada com prova técnica, as restantes 259 nunca verificadas. Fase 14.27: mapeamento VM↔`app-*` — só 2/39 hosts sintéticos tinham tag própria; 21 escritas confirmadas e executadas (20 adições + 1 correcção `VS8000789` equation→SACC, achado pelo próprio nome do host contradizer a tag). Fase 14.26: investigados MATCH CASH/RECONCILIADOR (confirmado `Match Cash-2` instalado nos 4 hosts, decisão de não fundir agora apesar da evidência) e `MatchWeb`/VS8000914 (achado: duplicado já desactivado de VS8000867/SACC, mesmo UUID VMware — só documentado, sem acção). Fase 14.25: investigadas Fircosoft (12 hosts — confirmado que MQ/Utilities/App são camadas arquitecturais reais, não fragmentação, decisão de não fundir) e SWIFT (20 hosts — 2 hosts QA quebram o padrão dominante, mas decisão do utilizador de não mexer agora). Nenhuma escrita feita. Fase 14.24: confirmadas tecnicamente `TOBE`↔SOP e `ebankit`↔BPCNET via WinRM em hosts nomeados pelo utilizador (`VS8000454`/`VS8000724`) — sites IIS + assinatura HTTP 401 para SOP, binding de domínio de produção `ib.bpc.ao` para BPCNET; nenhuma escrita necessária, as tags já estavam correctas. Fase 14.23: investigado e corrigido o mismatch `VS8000813` — WinRM+teste HTTP directo confirmaram que a VM corre mesmo o SIC (título da página "Sistema Integral de Cobranças (SIC)", branding BPC), não Microcrédito; tag `servico` corrigida de `MICROCREDITO` para `SIC`. O web scenario `app-sic` estava correcto desde o início. Fase 14.22: consolidação escrita da família `vProxy` — 12 hosts investigados (mesmo padrão de nome/camada/departamento, mesma appliance Dell/EMC Networker vProxy), 8 escritas (`host.update` 8/8 OK) para canonical `vProxy`. Fase 14.21: consolidação escrita da tag `servico` — 36 hosts, 11 famílias de duplicados, `host.update` confirmado 36/36 sem falhas, após dry-run e confirmação explícita; `VS8000102` (colisão de sigla `ABC`) confirmado por WinRM como sistema diferente do Sistema de Abertura de Contas e excluído da consolidação. Tag `servico`: 168→156→154 valores distintos. Fase 14.20: cruzamento formal das 168 tags `servico` (650 hosts) contra os 50 sistemas do relatório diário de negócio, usando 4 fontes cruzadas (Excel + tags + 39 web scenarios `app-*` + `Mapa_VMs_Servicos_Zabbix_Grafana.xlsx` já confirmado). Achados novos: mismatch concreto `app-sic`→`VS8000813`/tag `MICROCREDITO`; `ebankit` (20 hosts) forte candidato a ser o BPCNET; `TOBE` (12 hosts) é a tag técnica do SOP; `MATCH CASH`/`RECONCILIADOR`/`MatchWeb` fragmentados (3 tags, 1 conceito). Confirma que os "gaps" de tag são maioritariamente a VS8000305 (9 sistemas atrás de 1 tag) outra vez. Relatório completo em `documentacao/auditoria-tag-servico-vs-relatorio-negocio.md`. Sessão anterior (2026-07-04, Fase 14 recuperação de agentes): 67 VMs recuperadas em 2 sessões, 46→122 a reportar `agent.ping`; `VS8000904` fechado como isolamento de rede completo, escalar à equipa de rede; `VS9000424`/`VS9000343` descartados por decisão do utilizador de não enfraquecer UAC remoto; mapeados os 3 vCenters do grupo 664 — OK em 2/3; Z.25 (duplicados `VM.UUID`, grupo 609): 8 pares, 4 novos tratados. Detalhe em `C:\Repositorios\zabbix\bpc-workspace\vm-agente-recuperacao-handoff-20260704.md` e `mapeamento-apps-vms-webscenarios-handoff-20260705.md`.)

---

## Fase 0 · Fundação

| # | Tarefa | Estado | Data | Nota |
|---|---|---|---|---|
| 0.1 | Decisões de arquitectura travadas | ☑ | 2026-06-16 | camadas A–E + 4 forks |
| 0.2 | Blueprint + mapa host groups escritos | ☑ | 2026-06-16 | em `documentacao/` |
| 0.3 | Limpeza Grafana (arquivo + pasta limpa) | ☑ | 2026-06-16 | pasta `efpbu5tvrhce8a` |
| 0.4 | Estrutura de directórios local limpa | ☑ | 2026-06-16 | `<dominio>/n2|n3`, commit `af33bed` |
| 0.5 | Documento de engenharia + contratos | ☑ | 2026-06-16 | §5.1/5.2/6/7/10 |
| 0.6 | Inventário host groups Infra (API) | ☑ | 2026-06-16 | 74 grupos |
| 0.7 | Inventário host groups Network (API) | ☑ | 2026-06-16 | grupos 24-35 |
| 0.8 | UID datasources Grafana confirmados | ☑ | 2026-06-16 | infra `3_KgG43nz`, network `ffo8sp8zllog0e` |
| 0.9 | **Auditar `l2-header-global.js` vs contrato §5.1** | ☑ | 2026-06-16 | expõe `BPC`/`waitForBPC`/`BPC.utils`; faltam `BPC.THEME`, `BPC_SHARED`, `BPC_CHARTS`, `BPC.state` → corrigir no 0.10/0.11 |
| 0.10 | **Implementar `utils.js` canónico** | ☑ | 2026-06-16 | `_comum/utils.js` v9; +`BPC.THEME`/`BPC_SHARED`/`BPC_CHARTS` (BLOCO 5); `node --check` OK |
| 0.11 | `BPC.state` (modelo de estado §6.1) no utils | ☑ | 2026-06-16 | `metric/worst/host/color` no BLOCO 5; cores §6 reconciliadas |
| 0.12 | Validar `utils.js` num dashboard de teste | ☑ | 2026-06-16 | header renderiza, consola limpa; tipo `marcusolsson-dynamictext-panel` confirmado |

## Fase 1 · Infraestrutura VMware (grupos 608+603, Infra)

> **Reestruturação 2026-06-17:** o antigo domínio "Servidores Físicos" foi elevado e
> renomeado para "Infraestrutura VMware". O conteúdo N2 ESXi passa a N3-ESXi;
> cria-se um novo N2 com visão de vCenters + clusters + ESXi.
> Directório: `infraestrutura-vmware/` (era `servidores-fisicos/`).

| # | Tarefa | Estado | Data | Nota |
|---|---|---|---|---|
| 1.1 | Sondar items do grupo 603/608 (CPU/RAM/disco/rede/HW) | ☑ | 2026-06-16 | 27 hosts em 603: 20 ESXi (`vmware.hv.*`) + 4 físicos ICMP-only + 2 Cisco UCS + 1 Dell sem items. Items fixados em mapa-host-groups.md §Sondagem 1.1. |
| 1.2 | Ratificar thresholds no catálogo §6.2 | ☑ | 2026-06-16 | ESXi: CPU warn/crit via `vmware.hv.status`. Sem agente OS nos físicos → triggers são a fonte de estado. |
| 1.3 | N3-ESXi — `utils.js` | ☑ | 2026-06-16 | copiado de `_comum/utils.js` v9; nocLabel "SERVIDORES FÍSICOS (ESXi) - NÍVEL 2"; push OK (UID `8f6a94be`) — título a actualizar quando N2 VMware estiver pronto |
| 1.4 | N3-ESXi — KPI strip (BT) | ☑ | 2026-06-17 | `l2-kpi.js` v3.0 (id=100) |
| 1.5 | N3-ESXi — tabela de hosts (BT) | ☑ | 2026-06-17 | `l2-tabela.js` v3.0 (id=101); 11 colunas NOC@3m |
| 1.6 | N3-ESXi — top triggers | ☑ | 2026-06-17 | `l2-triggers.js` (id=102) |
| 1.7 | N3-ESXi — layout final + snapshot | ☑ | 2026-06-17 | gridPos: h=3/8/36/14; snapshot `dashboard-completo.json` |
| 1.8 | N3-ESXi-Detalhe — header do host (BT) | ☑ | 2026-06-17 | `l3-header.js` (id=101); UID `b55d5481` |
| 1.9 | N3-ESXi-Detalhe — séries CPU/RAM/rede (nativo) | ☑ | 2026-06-17 | 3 timeseries; `push_native.py` criado |
| 1.10 | N3-ESXi-Detalhe — tabela eventos | ☑ | 2026-06-17 | `l3-eventos.js` id=102 |
| 1.11 | N3-ESXi-Detalhe — layout final + snapshot | ☑ | 2026-06-17 | transparent + sem título; snapshot ok |
| 1.12 | **N2 VMware** — criar dashboard + utils | ☑ | 2026-06-17 | dashboard `a967e936` criado; utils.js id=100 pushed OK |
| 1.13 | **N2 VMware** — painel vCenter overview (BT) | ☑ | 2026-06-17 | `l2-vcenter.js` v3.0 pushed id=101; cards com cluster table, drill-down links, fix race condition utils (`_initPending`) |
| 1.14 | **N2 VMware** — tabela ESXi (BT) | ☑ | 2026-06-17 | `l2-tabela.js` pushed id=103; grupo 608 âncora |
| 1.15 | **N2 VMware** — top triggers + VMs desligadas + layout final | ☑ | 2026-06-17 | `l2-triggers.js` id=104, `l2-vms.js` id=102; layout transparent aplicado |
| 1.16 | **N3 vCenter** — criar dashboard + utils | ☑ | 2026-06-17 | dashboard `59e7e4b2` criado; utils.js id=100 + `l3-vcenter-detalhe.js` id=101 pushed; lê `?var-vcenter_hostid` do URL |
| 1.17 | **N3 vCenter** — 4 painéis separados (topo/esxi/triggers) + layout | ☑ | 2026-06-17 | split via VCD_CACHE; transparent+layout pushed; painel antigo removido |
| 1.18 | **N3 vCenter** — snapshot JSON final | ☑ | 2026-06-18 | transparent+título vazio; snapshot v12→v14 em `infraestrutura-vmware/n3/dashboard-completo.json` |
| 1.19 | **N3 vCenter** — dropdown selector de vCenter (var-vcenter_hostid) | ☑ | 2026-06-18 | variável grupo 664; `vcdt_resolveId` resolve nome→hostid; anchor targets referenciam `$vcenter_hostid` (v18) — testado e funcional |
| 1.20 | Revisão de conformidade — todos os dashboards vs contratos da documentação | ☑ | 2026-06-18 | node--check ✓, initWithRetry ✓, content=elementId ✓, transparent+title ✓, UIDs navegação ✓ |
| 1.21 | Navegação N2-VMware → N3-vCenter → Abrir vCenter testada | ☑ | 2026-06-18 | auditoria código: l2-vcenter→59e7e4b2 ✓, l3-vcenter-topo→a967e936 ✓, vcWebUrl+/ui/ ✓ |
| 1.22 | Navegação N3-ESXi ↔ N2-VMware testada | ☑ | 2026-06-18 | back link `← N2 · INFRAESTRUTURA VMware` adicionado ao utils.js N3-ESXi; nocLabel corrigido para NÍVEL 3 |
| 1.23 | Navegação N1→N2-VMware pendente até Fase 10 | ⏸ | | N1 criado por último |
| 1.24 | Validação de drill-down completa (N1→N2→N3) antes de demo ao cliente — 2 bugs reais encontrados e corrigidos | ☑ | 2026-07-06 | **Bug 1** (`l2-vcenter.js:524`): link N2→N3 "Ver detalhes" usava o `hostid` Zabbix do vCenter na URL (`?var-vcenter_hostid=11877`) em vez do nome do host — o datasource Zabbix filtra `host.filter` por **nome**, nunca por ID (regra já documentada), pelo que a âncora do N3 (`$vcenter_hostid`) nunca batia certo e o dashboard mostrava "The query didn't return any results" em todos os 4 vCenters. Corrigido para `encodeURIComponent(vc.name)`, alinhado com o padrão já correcto de `l2-tabela.js` (ESXi) e `l2-tabela.js`/`l2-correlacionador...` (VMs). **Bug 2** (`l2-vcenter.js` + `l3-vcenter-topo.js`, `CFG.groupIdESXi`): desde a criação (1.14, 2026-06-17) usa o grupo `608` ("HYPERVISORES") para contar/listar "Hosts ESXi" por vCenter — mas o grupo `608` só contém VMs/appliances (vProxy EMC Networker, VMs de gestão PowerFlex, os próprios appliances vCenter), **zero ESXi reais**. Os 20 ESXi físicos genuínos (item `vmware.hv.*`) vivem no grupo `603` ("SERVIDORES FISICOS", já usado correctamente por `n3-esxi`/`n3-esxi-detalhe` desde 1.1). Resultado antes da correcção: card PowerFlex mostrava "17 Hosts ESXi" (na verdade 17 VMs), PRD mostrava "7" (na verdade 7 VMs) — números plausíveis mas completamente errados, nunca detectados porque ninguém cruzou com a contagem física real. Corrigido `groupIdESXi: 608→603` nos 2 ficheiros; após fix, PRD=11 + PowerFlex=9 = 20, bate certo com os 20 ESXi reais confirmados via `item.get` (`vmware.hv.version`). Ambos os fixes pedidos e confirmados explicitamente pelo utilizador antes do push; verificados ao vivo no browser (screenshots antes/depois). Detalhe de investigação completo nesta sessão (transcript), não replicado noutro documento. **Armazenamento** (N2 único, `993834a3`) e **Servidores Virtuais** (N2 `0758c24e` + N3 `0ae673a3`) validados na mesma passagem — sem problemas encontrados, drill-down N2→N3 funcional (testado com VM real `VS8000218`) |

> **Z.8 VMware (alargado):** dos 4 hosts do grupo 664, apenas `sv9000204 (Vcenter PRD)` tem
> todos os items VMware a recolher. `sv9000206 (PowerFlex)` — items `state=1` (unsupported,
> poller não consegue autenticar). `vCenter 02` — idem + sem ICMP. `vCenter Backup` — cluster
> status recolhe mas sem `vmware.fullname[` → não aparece nos cards N2. Causa provável em
> todos: `{$VMWARE.URL}` ausente ou errado ao nível do host (macro herdada de template
> incorrecto ou não definida). Audit e correcção → bloqueado em Z.8 (ver `z8-uuid-mismatch.md`).

## Fase 2 · Armazenamento (anchor 602 + tape 605, Infra)
| # | Tarefa | Estado | Data | Nota |
|---|---|---|---|---|
| 2.1 | Sondar items 602/605 | ☑ | 2026-06-17 | 10 hosts Storage + 1 Tape. Sem métricas capacidade/IOPS — só ICMP + `system.status` SNMP (IBM). SNMP e script Dell não recolhem (triggers activos). Registado em mapa-host-groups §Sondagem 2.1 |
| 2.2 | N2 (utils + KPI + tabela + triggers) | ☑ | 2026-06-17 | 4 painéis (utils/kpi/tabela/triggers); UID `993834a3`; transparent+layout aplicados. Card IBM SNMP mostra aviso Z.9 enquanto SNMP não recolhe — adapter pronto para activar automaticamente assim que Zabbix resolver |
| 2.3 | N3 (por array/tape) | ⏸ | | Bloqueado até Z.9 (SNMP IBM) e Z.10 (script Dell Unity) estarem resolvidos — sem dados reais no Zabbix, N3 seria só ping. Retomar após Fase 3+ |
| 2.4 | Navegação + teste + commit | ⏸ | | Depende de 2.3 |
| 2.5 | **N2 Datastores** — painel de saúde dos datastores SAN (grupos 603 ESXi) | ☐ | | Origem: volumes Dell EMC Unity (`DATA_UNT_400_8T-0..11`) exportados via FC SAN para cluster CS9000002. Items LLD `vmware.hv.datastore.size[url,uuid,pfree/total]` nos ESXi (grupo 603) — a colectar. Pode ser painel adicional no N2 Armazenamento (`993834a3`) ou dashboard separado em `armazenamento/n3/`. Não bloqueia Z.9/Z.10 — dados já disponíveis. Bloqueado por Z.8 nos ESXi PowerFlex (sv9000650-655 sem dados). |

## Fase 3 · Servidores Virtuais (anchor 609, Infra)

> N2 SV em construção activa. Arquitectura agente-first (VMware fallback).
> Filtro padrão: tag `ambiente` = "Produção" | "producao".

| # | Tarefa | Estado | Data | Nota |
|---|---|---|---|---|
| 3.1 | N2 — utils + KPI strip + tabela (agente-first) + triggers | ☑ | 2026-06-17 | Dashboard `0758c24e`; 4 painéis pushed (id=100-103) |
| 3.2 | N2 — corrigir CPU VMware (MHz→%) e RAM (keys/unidades) | ☑ | 2026-06-17 | `l2-tabela.js` v3.1: CPU→`usage.perf`; RAM→`vm.memory.size[used]` (não [available]); disco; sort por coluna; dropdown ambiente; "ver detalhes" |
| 3.3 | N2 — layout final + snapshot | ☑ | 2026-06-17 | gridPos h=3/10/36/12; transparent; `dashboard-completo.json` guardado |
| 3.4 | N3 — conformar rascunho (l3-*.js em `n3/`) | ☑ | 2026-06-18 | Dashboard `0ae673a3` (versão A BT). Painéis: header (100), KPI strip v18.1 (101), triggers (107), serviços (106), CPU detalhe (102), RAM detalhe (103), Rede (105), Disco I/O (104), Ficha (108). KPI: 6 cards — Saúde/CPU/RAM/Rede/Disco/Alertas; CPU Ready; trigger.get. Snapshot v14. |
| 3.4.1 | N3 — re-aplicar layout definitivo versão A (ordem triggers→KPI + alturas) | ☑ | 2026-06-18 | fix_versao_a_layout.py; ORDER=[100,107,101,106,102,103,105,104,108]; push v14 OK; manifest.json IDs corrigidos; dashboard-completo.json guardado |
| 3.5 | ~~N3 — painel Datastores~~ → **movido para 2.5** | ✖ | 2026-06-18 | Datastores são volumes SAN (Dell Unity) apresentados aos ESXi via FC — pertencem ao domínio Armazenamento, não às VMs. Items vivem nos hosts ESXi (grupo 603) como LLD `vmware.hv.datastore.size`. Adicionado como 2.5 na Fase 2. |
| 3.6 | Navegação N2-VMware ↔ N2-SV ↔ N3-VM testada | ☑ | 2026-06-18 | 5 fluxos corrigidos e auditados por código: `l2-tabela` UID→`0ae673a3`; correlacionador UID→`b55d5481`; `l3-vm-header` backNav 2 chips; `l2-vms` chip "Ver todas as VMs (N2) →" |

## Fase 4 · Rede (multi-grupo 26/27/28/29/35, **Network** `ffo8sp8zllog0e`)

> **Arquitectura aprovada 2026-06-18** (lente NOC + redes) em
> `documentacao/rede-arquitectura.md`. Modelo de 4 níveis: N2 (wallboard, 3 cards
> de segmento) → N3 ×3 (DC / Edifícios / WAN) → N4 (ficha por device,
> parametrizado por variável Grafana hostname). Decisões: N4 como nível novo ·
> WAN via interfaces do grupo 27 · N3 WAN separado · N4 usa hostname (não hostid)
> para compatibilidade com painéis nativos Time Series.

| # | Tarefa | Estado | Data | Nota |
|---|---|---|---|---|
| 4.0 | Plano de arquitectura N2+N3+N4 | ☑ | 2026-06-18 | `documentacao/rede-arquitectura.md`; 3 decisões aprovadas |
| 4.1 | Links WAN (grupo 35 vazio) — RESOLVIDO + topologia auditada | ☑ | 2026-06-18 | Auditoria directa → `documentacao/rede-topologia.md` + `topologia-dc.svg`. WAN = links nas interfaces do g27. IP SLA = verdade de serviço |
| 4.2 | Fundação — consolidar `utils.js` rede | ☑ | 2026-06-18 | 4 cópias (n3-wan, n3-wan-carriers, n4-wan-router, n4-dc-switch) com nocLabel próprio. Pushed e testado |
| 4.3 | N2 · Rede refactor — l2-kpi + l2-segmentos + l2-triggers | ☑ | 2026-06-19 | Título corrigido "N2 · Rede" (era "N2 · Segmentos") · drills N4 por grupo (g26→DC Switch / g27→WAN Router) em l2-triggers · dead code "N3 em construção" removido de l2-segmentos · layout compactado (buraco 18 linhas eliminado) |
| 4.4 | N3 DC Core — fabric + table + WAN links | ☑ | 2026-06-19 | UID `a75e2ba6` · 4 painéis · l3-dc-table re-pushed com drills N4 (estavam ausentes do Grafana) · layout compactado (buraco 8 linhas utils↔fabric) · snapshot gravado |
| 4.5 | N3 Edifícios | ☑ | 2026-06-29 | UID `471f2208-d032-46d4-8d35-6fdfe770c967` · drill N4 funcional (n4-edificio-detalhe) · snapshot gravado. Nota: UID curto `471f2208` não resolve sozinho — usar UID completo em links externos. |
| 4.6 | N3 WAN — negócio + cards + triggers + carriers | ☑ | 2026-06-19 | UID `1702465e` (n3-wan) reestruturado: duplicados eliminados (5 painéis→7 canonical), n4DashUid preenchido em l3-wan-triggers, layout novo · UID `31bace26` (n3-wan-carriers) h=48→28 · snapshots gravados |
| 4.7a | N4 WAN Router — ficha técnica por router | ☑ | 2026-06-19 | UID `8ddc4833` · back-link "← N3 WAN" pushed (estava ausente do Grafana) · snapshot gravado |
| 4.7b | N4 DC Switch — ficha técnica por switch | ☑ | 2026-06-19 | UID `7baea796` · back-link "← N3 DC Core" pushed (estava ausente do Grafana) · snapshot gravado |
| 4.8 | Navegação ponta-a-ponta + layout final + conformance test | ☑ | 2026-06-19 | Teste de conformidade executado em todos os 7 dashboards (`documentacao/teste-conformidade-fase4-rede.md`) · 23 defeitos identificados e aplicados (F-01→F-23, excepto F-11 adiado) · back-links N3→N2 e N4→N3 funcionais · drills N2→N3→N4 operacionais |
| 4.9 | Snapshots e registos finais | ☑ | 2026-06-19 | `dashboard-completo.json` gravado para os 7 dashboards (n2, n3-dc, n3-edificios, n3-wan, n3-wan-carriers, n4-wan-router, n4-dc-switch) |
| 4.10 | Drill N4 Edifícios (g28/g29) | ☑ | 2026-06-29 | N4 Edifício (`n4-edificio-detalhe`) criado: ficha MySQL, ICMP/RTT/Loss, CPU/Mem/Uptime, provider WAN, state-timeline 46 switches, botão N5. N5 Edificio (`n5-edificio-interfaces`) criado: clonado de N5 Agências, variável host por HG_EDIFICIOS_ROUTERS. N6 Switch (`n6-edificio-switch`) criado: ficha MySQL (modelo/piso/zona/IOS), stats SNMP, hw health (Temp Inlet+HotSpot, Fans, PSU), uplinks erros+discards, state-timeline 65 portas access. Commit `1fc9be8`. |
| 4.11 | N4/N5/N6 Edificio — hw health switches (Temp/Fan/PSU) | ☑ | 2026-06-29 | Auditado: Temp 3 sensores (Inlet=25C/Outlet=32C/HotSpot=41C), Fans 2x status, PSU 1x status, PoE nao recolhido. Todos integrados em N6. Commit `1fc9be8`. |
| 4.12 | N4 Edifício — tabela de switches condicional (BT) | ☑ | 2026-06-29 | `l4-ed-switches.js` (id=103) redesenhado: dinâmico via `router.edificio = switch.local` (sem hardcode Sede); condicional (painel invisível se 0 switches); tabela 14px com bordas visíveis; colunas Andar·Zona·Switch(link N6)·Estado(pill UP/DOWN)·RTT; ordenação numérica por andar. Pushed OK. |
| 4.13 | Validação end-to-end fluxo Edifícios N1→N6 | ☑ | 2026-06-30 | Drill N1→N6 completo confirmado no browser. Bugs resolvidos: B-04 uptime N3 (item key → name search); B-05 switches N4 (panel type state-timeline → BT); B-06 nomes interface N4 (value mapping regex); B-07 N6 CPU+Ficha (datasource INFRA→NETWORK, filtro `/CPU utilization/`, query `h.name=`); B-08 N5 filtro interface (regex `.*` → `[^.]*` para isolar subinterfaces). Commits `7e4da51`, `af88c83`, `c55d6c3`. |

| 4.14 | Teste N1→N6 fluxo Rede + correções B-09/B-10 | ☑ | 2026-06-30 | Drill N1→N2 Rede→N3 WAN/DC/Edifícios→N4→N5/N6 executado no browser. **B-09**: dashUids null em N2 para DC Fabric e Edifícios (l2-segmentos.js). **B-10**: apiUrl apontava INFRA em vez de NETWORK em 7 utils de Rede (n3-dc, n3-wan, n3-wan-carriers, n4-dc-switch, n4-wan-router, n4-wan-ag, n4-wan-provedor) — causava "0 dispositivos" em todos os dashboards de Rede. N4 DC Switch + N4 WAN Router estavam em pasta General (sem permissão de serviço) → movidos para 04·Rede via API. Commit c7b703b. Issues pendentes: B-11 título "Header + Shared" visível nos utils N4; B-12 uptime WAN Router; N3-DC-01 matriz underlay vermelha (a confirmar com equipa de rede); N3-SW-01/N4-EDIF-02 drill N4→N6 em switches Edifícios. |
| 4.16 | N2 Rede — investigação painéis em branco (lazy loading Grafana 12) | ☑ | 2026-06-30 | Todos os 4 painéis em branco após push l2-triggers. JSON e scene correctos. Root cause: Grafana 12 `dashboardScene` tem lazy loading 3 camadas (SceneGridLayout ResizeObserver → LazyLoader IntersectionObserver → VizPanel bounds); Chrome window não visível/focado throttle observers → bounds=0 → nada renderiza. **Fix**: abrir Grafana em Chrome window visível no ecrã. **Validado no browser**: N2 Rede renderiza os 4 painéis correctamente com janela visível. |
| 4.17 | B-04 dashSlugs stale em l2-segmentos + sync manifest l3-wan-kpi + validação E2E N3 WAN→N4 | ☑ | 2026-06-30 | **B-04**: os 4 `dashSlug` hardcoded em `l2-segmentos.js` (CFG_SEG) estavam desactualizados face aos slugs reais dos dashboards N3 (confirmado via `/api/dashboards/uid/*`); corrigidos e pushed (painel id=103, N2 Rede). **Sync manifest**: `l3-wan-kpi.js` (N3 WAN) já estava pushed no Grafana com `id=105` mas o `manifest.json` local ainda tinha `id: null` — sincronizado. **Validação E2E N3 WAN→N4**: os 5 cards de serviço (Internet/EMIS/Agências/Parceiros/Azure-Gov) confirmados a drillar para os hostnames reais correctos — `DC1-RTE-WAN-INT`/`DC1-RTE-WAN-EMIS`/`DC1-RTE-WAN-AG`/`DC1-RTE-PARC`/`DC1-RTE-GTW01`. **Bug novo encontrado (não corrigido)**: dashboard `rede-n4-wan-dispositivo` tem variável `iface` com valores hardcoded globais (Uplinks=Te0, WAN Circuits=Po2, DMVPN DC=Tu1, DMVPN Edificios=Tu2) que não correspondem à nomenclatura real de interface por router — ex. `DC1-RTE-WAN-INT` usa Port-channel (`Po2`), não `Te0`; com o filtro default "Uplinks" os painéis Bits Received/Sent mostram "No data" embora os dados existam (confirmado: trocar para "WAN Circuits" mostra gráficos normalmente). Possivelmente afecta os outros 4 routers WAN — por confirmar. |
| 4.15 | N3 Edifícios cards 3×3 + N4 switches melhorado + B-11/B-12 | ☑ | 2026-07-01 | **N3**: `l3-edificios-table.js` reescrito — grid 3×3 (9 cards), sort DOWN→Degradado→OK, borda colorida por estado, RTT/Perda/CPU/Uptime por card, drill N4. Switches da tabela separados (ficam no N4, não no N3). **N4 switches**: colunas alargadas (Modelo, Perda, Uptime) + botão "N6 →" explícito; fetch icmpping/sec/loss numa só query; modelo vem das tags Zabbix (`C9200L-48P-4G`). Auditoria Zabbix confirmou: 9 routers de edifício (g28), 46 switches só na Sede (g29, tag `local=sede`), outros 8 edifícios sem switches monitorizados. **N6**: dashboard pre-existente `n6-edificio-switch` já completo (15 painéis nativos incl. state-timeline portas P0-P20, hw health). **B-11**: `push_panel.py` corrigido — todos os painéis (utils + conteúdo) ficam com `title: ''`. **B-12**: uptime WAN Router — query mudada de `filter:{key_:'system.uptime'}` para `search:{name:'uptime'},searchWildcardsEnabled:true` (compatível com Cisco IOS SNMP). **Limpeza**: 3 painéis órfãos removidos do N4 Edifício (ids 3/4/5 — timeseries WAN vestígios do clone N4 Agências). Issues ainda pendentes: N3-DC-01 matriz underlay vermelha (a confirmar com rede); B-12 uptime WAN Router a validar no browser. |
| 4.18 | Segmento WAN → "Borda DC" + N4 canónico + fix filtros nativos | ☑ | 2026-07-01 | **Confusão de nomenclatura resolvida**: "WAN" no N2 confundia com a WAN das próprias Agências (também WAN); renomeado para **"Borda DC"** — os 5 routers de borda (`HG_DC_ROUTERS`, g27: WAN-INT/WAN-EMIS/WAN-AG/PARC/GTW01), sem router "principal" entre si. **N2**: `l2-segmentos.js` — card WAN deixou de mostrar "conceitos de negócio" (Internet/EMIS/Agências/Parceiros/Azure-Gov como linhas de texto) e passou a usar o mesmo motor `segFetchIcmp` dos outros 3 segmentos (Total/Down/Degradado/Disponib%+RTT/Perda), contando os 5 dispositivos reais. Código morto removido (`segFetchWanCats`/`segWanBody`/`SEG_WAN_CATS`). **N3**: `l3-wan-cards.js` — cada um dos 5 cards mostra agora o hostname real do Zabbix como subtítulo (antes só o alias curto "WAN-INT"), deixando explícito que é 1 dispositivo concreto. **N4 canónico decidido**: existiam 2 dashboards N4 diferentes para os mesmos 5 routers (`rede-n4-wan-dispositivo` genérico vs `rede-n4-wan-router` ficha técnica); comparados lado a lado — `rede-n4-wan-router` é mais rico (circuitos por categoria, IP SLA, alertas nativos) mas tinha os 3 painéis nativos (Tráfego/CPU-RAM/RTT) sempre "No data": filtravam `item.filter` por **key** (`/net\.if\.in/`, `/system\.cpu\.util/`, etc.) quando o datasource Zabbix nativo casa pelo **nome** — mesmo anti-padrão da lição `d04agencia2` já documentada. Corrigido para `/Bits received\|sent/`, `/CPU utilization/`, `/Processor: Memory utilization/`, `/completion time/`; validado em 2 routers (WAN-INT com IP SLA, PARC sem). `l3-wan-cards.js` redireccionado para `rede-n4-wan-router` (drill validado: AGÊNCIAS→`DC1-RTE-WAN-AG` mostra os 15 túneis DMVPN + Azure ER, `Tu2028` down sinalizado). `l4-ag-ficha.js` (N4 Agência) **não foi alterado** — passa hostname de agência arbitrário, incompatível com o mapa fixo `CFG_R4.routers` do `-router`. Commits `ba5a748`, `6533a4d`, `2eea539`. Pendente: `rede-n3-wan-carriers` ("N3 WAN Provedores") continua órfã (sem entrada de navegação) — decisão adiada até rever conteúdo. |
| 4.19 | Reorganização em 4 segmentos: pastas Grafana aninhadas + disco local + termo "segmento" | ☑ | 2026-07-01 | **Confirmação de engenharia**: domínio Rede tem 4 segmentos reais (não "subdomínios" — termo corrigido para "segmento", já usado no código `CFG_SEG`): Agências (220 routers+27 switches), Borda DC (5 routers g27), DC Fabric (7 switches g26+1 OOB g22), Edifícios (9 routers g28+46 switches g29). Avaliados os outros domínios (VMware 4 dashboards, Armazenamento 1, Servidores Virtuais 2, Segurança/BasesDados/APIs/ServiçosNegócio 0) — nenhum precisa da mesma reorg hoje (sem fan-out em segmentos paralelos). **Grafana**: criadas 4 pastas aninhadas dentro de "04·Rede" (`04.1·Agências` `rede-seg-agencias`, `04.2·Borda DC` `rede-seg-bordadc`, `04.3·DC Fabric` `rede-seg-dcfabric`, `04.4·Edifícios` `rede-seg-edificios`) e movidos os 14 dashboards (excepto N2) para as pastas correctas via API; drill validado (breadcrumb `04·Rede > 04.2·Borda DC > N3...`, UIDs inalterados). **Disco local**: `git mv` das 15 subpastas de `04-rede/` (numeração global 02-16) para dentro de 4 pastas de segmento (`04.1-agencias/`, `04.2-borda-dc/`, `04.3-dc-fabric/`, `04.4-edificios/`), renumeração de drill reiniciada em cada uma (01, 02, …); `folderUid` de cada `manifest.json` actualizado para a pasta de segmento correcta. **Documentação**: `engenharia-do-sistema.md` §4.0/§4.1 reescritos (mapa de segmentos, árvore de directórios, regra nova "criar pasta de segmento só quando o domínio tiver >1 segmento real"); `CLAUDE.md` (constraints "Pastas Grafana por domínio" e "Naming de directorias locais"); memória `naming-convention.md` corrigida (subdomínio→segmento). |
| 4.20 | Remoção de órfão `n4-rede-agencia-wan` | ☑ | 2026-07-01 | Confirmado via `/api/dashboards/uid/rede-n4-agencia-wan` (`"Dashboard not found"`) e busca Grafana por "WAN-AG" (0 resultados) que este dashboard **nunca chegou a ser pushed** — só existia como manifest+.js local. Conteúdo era âncorado a 1 único host (`DC1-RTE-WAN-AG`, o hub Borda DC), redundante com os circuitos+throughput já mostrados no `rede-n4-wan-router` (corrigido em 4.18). Pasta local `04.1-agencias/03-n4-rede-agencia-wan/` removida; `04-n5-rede-agencia-interfaces/` renumerada para `03-n5-rede-agencia-interfaces/` (preencher o buraco de numeração). `engenharia-do-sistema.md` §4.0/§4.1 actualizados. |
| 4.21 | Bug fix N4 Provedor + arquitectura Borda DC + regra de isolamento por segmento | ☑ | 2026-07-01 | **Bug fix `l4-wan-provedor.js`**: (1) `routerNames` (hostid→label) tinha 4/5 hostids trocados — confirmado contra `host.get` real (10996 é WAN-AG, não RTE-PARC; 11001 é RTE-PARC, não RTE-GTW01; etc.); (2) `effDown` aplicava o falhanço do IP SLA 65 (só mede Po2.65 do WAN-INT) a **todos** os circuitos do provedor ITA — confirmado no Zabbix que os 4 circuitos estavam `net.if.status=1` (UP), dashboard mostrava "4 DOWN". Substituído `slaCarrierMap` por `slaCircuitMap` (índice SLA → hostid+interface exacta). Validado: `?var-provider=ita` agora mostra routers correctos e "4/4 UP · Operacional". Commit `dba8616`. **Arquitectura Borda DC (decisão de engenharia)**: mantidos os 2 eixos N3 paralelos (por dispositivo + por provedor, não fundir — respondem a perguntas NOC diferentes); N4 consolidado para 2 (Router + Provedor); regra de tecnologia formalizada — BT só para lógica de negócio (categorização, agregação cross-host), nativo para série temporal/triggers. **Regra de isolamento por segmento**: `rede-n4-wan-dispositivo` (genérico) só tinha consumidor real em Agências (`l4-ag-ficha.js`) apesar de viver em Borda DC — movido inteiro para `04.1-agencias/`, retitulado "N4 · Rede · Agência · WAN — Interfaces do router", variáveis especializadas (group=`HG_AGENCIAS_ROUTERS`, iface Gi/Tu em vez de Po/Te). Borda DC fica só com os 4 dashboards que lhe pertencem. Nova regra codificada em `engenharia-do-sistema.md` §4.1 regra 6: nunca partilhar dashboard entre segmentos — duplicar e especializar. |
| 4.22 | N4 Router rebuild (nativo) + N5 novo + auditoria de honestidade (Z.13/Z.16) | ☑ | 2026-07-01 | **N4 Router rebuild**: `l4-bdc-router-ficha.js` (Business Text gigante, rejeitado em sessão anterior por divergir do padrão dos irmãos) substituído por painéis nativos — 4 stats (ICMP/CPU/RAM/Uptime), 1 tabela única de circuitos (8 targets por categoria, regex fechados verificados ao vivo por host — "parceiros" convertido de catch-all `Po2\.\d+` aberto para lista explícita dos 13 circuitos reais do PARC, para não vazar para os Po2.x próprios do WAN-AG), timeline de flaps, triggers nativo. Validado em 4/5 routers no browser (WAN-INT/WAN-AG/PARC/GTW01) — zero contaminação cruzada entre categorias confirmada ao vivo. **SLA 65**: construído, depois removido a pedido — Z.13 investigado a fundo (`history.get`: completion time=0 em 43.836 pontos/31 dias, trigger 1 evento aberto ininterrupto desde 2025-11-21 ~7 meses, Po2.65 com tráfego real ~23Mbps) — assinatura de sonda com alvo morto, não outage real; rebaixado P1→P2. **Iteração de layout** (feedback de revisão): painel de identidade removido (repetitivo com header), alturas reduzidas (stats 6→4, circuitos 18→10 com scroll interno), Problemas Activos meia-largura + novo painel Packet loss (erros/descartes) na outra metade (plugin nativo de triggers não estica tabela com poucas linhas — comportamento conhecido, não bug do dashboard). **Auditoria de honestidade** (a pedido, "esta dashboard tem dados suficientes para monitorar saúde dos routers?"): confirmado zero dados BGP reais nos 5 routers (proxy apenas, bate com `rede-arquitectura.md`); **Z.16 novo** — trigger corrompida em GTW01 (nome "Tu20 Link down" é snapshot obsoleto de 2025-12-06; trigger real "Po1 Link down" avalia items de **outro host** `DC1-LEAF-105`; Po1 real está genuinamente down sem nenhuma trigger a apanhar — encaminhado, não corrigido, é config de alerta em produção). **N5 novo** (`07-n5-bdc-router-interfaces`, UID `rede-n5-bdc-router-interfaces`): drill-down por interface (estado+flaps, tráfego in/out, erros, descartes), clone do padrão `n5-rede-agencia-interfaces`; dropdown de interface via variável MySQL nativa contra a própria tabela `items` do Zabbix (`WHERE h.host='$routerName'`) — descoberta em sessão que este mecanismo **não depende de tags BPC** (correcção de um pressuposto errado inicial de que seria preciso variável Zabbix nativa não testada); botão N4→N5 (`l4-bdc-n5-button.js`). Validado ao vivo (dropdown com nomes legíveis, painéis correctamente isolados à interface seleccionada). `CLAUDE.md` §Fluxo Borda DC actualizado (N5 do eixo router). |
| 4.23 | Revisão N3 Borda DC (mesma filosofia N4/N5) | ☑ | 2026-07-01 | Revisão de honestidade aplicada ao N3 (`l3-bdc-routers-cards.js` + `l3-bdc-routers-kpi.js`). Arquitectura confirmada correcta como está (5 cards em Business Text — view de comparação multi-router, job diferente do detalhe único do N4, não é candidato a nativo). **Bug encontrado**: card INTERNET mostrava "SLA X/5 OK", média das 5 sondas incluindo as 4 sem alvo verificado (Z.13) — diluía a única falha real (sonda 65, morta) numa aparência de "4/5 OK". Inconsistente com a decisão já tomada no N4 (painel SLA 65 removido). **Fix**: linha SLA e todo o fetch/compute associado (`hasIpSla`, `rttMonCtrlAdminSense`) removidos de `l3-bdc-routers-cards.js`. Validado ao vivo — card INTERNET mostra só ICMP+BGP Peers agora; tabela de Problemas Activos confirma visualmente os 2 alertas de 7 meses (Z.13 + Z.16) já documentados. KPI strip e restante categorização confirmados correctos, sem alterações. |
| 4.24 | N4 Provedor rebuild (nativo, multi-host) + revisão N3 Provedores | ☑ | 2026-07-01 | **N3 Provedores**: mesma correcção do Z.13 aplicada a `l3-bdc-provedores-cards.js` — `effDown` do provedor ITA já não depende de `rttMonCtrlAdminSense[65]` (sonda morta); `net.if.status` passa a ser a única fonte de verdade. Validado ao vivo — card ITA mostra "4/4 UP" em vez do falso "1 DOWN". **N4 Provedor**: `l4-bdc-provedor-ficha.js` nunca tinha sido terminado (sem manifest, nunca pushed) e era uma ficha BT gigante, mesma forma já rejeitada no N4 Router — reconstruído nativo do zero (`rede-n4-bdc-provedor`). Desafio de arquitectura: ao contrário do N4 Router (1 host), um provedor tem circuitos espalhados por **vários** dos 5 routers — resolvido com variável `$provider` a guardar directamente a alternação regex (reaproveita os tokens já validados), interpolada em `item.filter` com `host.filter=/.*/` (grupo inteiro `HG_DC_ROUTERS`). Confirmado ao vivo via `/api/ds/query` antes de construir: queries multi-host devolvem uma frame por série com `labels.host` explícito — mecanismo real, não hipotético. **Bug encontrado e corrigido durante o build**: transform `extractFields` (capture groups nomeados `(?<Router>...)`) falhava silenciosamente no editor de painéis desta versão do Grafana, mostrando a tabela em formato bruto (wide time-series) em vez de linhas. Diagnosticado isolando `reduce` sozinho (funcionou, confirma que o bug era só no `extractFields`) — simplificado para 1 coluna combinada "Circuito" (`hostname: interface`) em vez de Router/Interface separados; robusto, sem perda de informação. **Triggers nativo por provedor**: o ficheiro antigo assumia que não era possível filtrar triggers por provedor (classificação por regex, não tag Zabbix) — testado e confirmado que **é** possível via `trigger.filter` regex sobre o nome da trigger (teste `var-provider=IP SLA 65` devolveu exactamente essa 1 trigger, nada mais). 5 painéis nativos finais: tabela de circuitos, timeline de flaps, triggers, tráfego, packet loss — sem stats de "circuitos UP" (multi-série não reduz a um único número sem complexidade desnecessária; a tabela já cobre isso, mesmo padrão do N4 Router que também não tem esse stat). Validado em 3 provedores (ITA, MST, IP SLA 65 como teste ad-hoc) via `get_page_text` (screenshots intermitentemente lentos/timeout com tabelas grandes). `l4-bdc-provedor-ficha.js` apagado. |
| 4.25 | Bug crítico: dropdown do provedor só funcionava para UNITEL | ☑ | 2026-07-01 | Utilizador reportou que só UNITEL funcionava no dropdown "Provedor" — os outros 7 ficavam "No data". Reproduzido: navegação directa por URL com `var-provider=MST\|MSTELECOM\|...` funcionava, mas seleccionar "MST / MSTELECOM" no dropdown real (sem reload de página) dava "No data" com o mesmo valor. **Causa-raiz**: `${provider}` puro é auto-escapado por Grafana para caracteres regex (`\|`, `\\`, `[]`) quando a interpolação acontece via mudança de variável em SPA (sem reload) — mas não da mesma forma no bootstrap inicial a partir da URL, daí o mecanismo ter parecido validado antes (só tinha sido testado por URL directa, nunca pelo dropdown real). UNITEL era o único valor sem caracteres regex especiais, por isso "funcionava" — não porque estivesse OK, mas porque não tinha nada para escapar. **Fix**: `${provider}` → `${provider:raw}` (formato que desactiva o auto-escape) em todos os 5 painéis + trigger.filter. Validado ao vivo via selecção real no dropdown (não só URL) em MST (pipes), AZURE (backslash+brackets) e CONNECTIS (token simples) — todos correctos agora. **Lição para o futuro**: sempre testar variáveis com valor regex-literal pela selecção real no widget, nunca só por URL — os dois caminhos de interpolação do Grafana podem divergir. Documentado em `CLAUDE.md` §Fluxo Borda DC e no manifest do N4 Provedor. |
| 4.26 | Contexto do objecto no título do header (N4/N5 Borda DC) | ☑ | 2026-07-02 | Pedido do utilizador: em qualquer dashboard com dropdown de selecção de objecto (router/interface/provedor), o nome do objecto seleccionado tem de aparecer no final do título do header — sem isso não há garantia visual de que se está a olhar para o dispositivo certo. **Implementado em `_comum/utils.js`** (fonte canónica) — novo `CFG_HEADER.objectContext` ({urlVars, labelMap?, separator?}), funções `resolveObjectContext`/`renderContext`, span `#bpc-noc-context` no título, actualizado a cada tick do relógio (1s) lendo `window.location.search` directamente — não depende da query do painel referenciar a variável (as âncoras destes dashboards são fixas de propósito, para o header não desaparecer com o objecto seleccionado down), e apanha mudanças de variável via dropdown porque o Grafana actualiza o URL por `pushState` sem recarregar a página. Propagado às 3 cópias do segmento: **N4 Router** (`urlVars: ['routerName']`), **N4 Provedor** (`urlVars: ['provider']` + `labelMap` com os 8 pares valor-bruto→label, porque `$provider` guarda a alternação regex directamente, não uma chave simbólica), **N5 Router Interfaces** (`urlVars: ['routerName','iface']`, concatenados). Validado ao vivo nos 3: "…NÍVEL 4 · DC1-RTE-PARC" (Router), "…NÍVEL 4 · AZURE" (Provedor, labelMap resolvido correctamente), "· DC1-RTE-PARC · Po2.421" (N5). Documentado como convenção obrigatória em `engenharia-do-sistema.md` §5.1 (contrato do utils) e §10.1 (novo DoD para N4/N5 com dropdown). |
| 4.27 | DC Fabric redesenhado do zero — 2 eixos (dispositivo + saúde) | ☑ | 2026-07-02 | **Fix prévio (mesma sessão)**: threshold de staleness em `fabIsStale()` (painel "Saúde do Fabric") — `maxAgeSec` 600s marcava quase tudo `stale` e forçava a "down" (falso "14 ligações em baixo" permanente); `net.if.status` tem cadência SNMP lenta (mediana ~12158s, confirmado ao vivo). Subido para 21600 (6h) **e** adicionado 3º estado `stale` (âmbar, nunca `down`) — resolve tanto o caso actual como se a cadência voltar a mudar. **Redesign completo**: dashboard único antigo (`rede-n3-dc`, misturava switches g26 com routers WAN g27 já duplicados em Borda DC) arquivado (local `arquivo-dc-fabric/`, Grafana `99·Arquivo`) junto com o N4 antigo (`rede-n4-dc-switch`). Substituído por 2 eixos paralelos, só sobre os 7 switches: **por dispositivo** (`rede-n3-dc-dispositivos` → `rede-n4-dc-fabric-switch` → `rede-n5-dc-switch-interfaces`, clone do padrão N4/N5 Router de Borda DC, classificação por tag `funcao`/`modelo` em vez de regex sobre nome) e **por saúde/correlação** (`rede-n3-dc-fabric-saude`, matriz underlay/vPC/overlay, link cruzado para Borda DC em vez de duplicar routers). **Achado durante o build**: uplink LEAF→SPINE descreve-se `LINK TO SPINE-XX` (não `UNDERLAY`, que só existe do lado SPINE e enganosamente numa loopback de BGP) — confirmado ao vivo via `item.get`, corrigido em cards/tabela/gráficos do N4. **Bug adicional encontrado na validação de drill-down**: `trgN4Link()` (N2 · Rede, alertas→N4) nunca tinha funcionado — `trigger.get` com `selectGroups` devolve sempre `null` nesta versão do Zabbix/proxy; substituído por `host.get` dedicado (grupos 26+27, tag `funcao`), fonte confirmada fiável. **Layout final** (pedido do utilizador, 2 rondas): N3 Dispositivos — header/KPI `h=8`→`h=3`/`h=4` (espaço morto), cards `h=11` e triggers `h=16` (tinham scroll interno, confirmado `scrollHeight===clientHeight` após ajuste); N4 Switch — mesmas proporções do N4 Router de Borda DC (header `h=3`, 4 stats lado a lado `w=6` cada, tabela `h=10`, timeline `h=6`, triggers+packetloss a meia-largura, tráfego+CPU/RAM a meia-largura). Drill-down completo N1→N2→N3(×2)→N4→N5 validado no browser. Commits `2b86352`, `33802f1`, `24e9e99`. |
| 4.28 | **Sign-off de fecho do domínio Rede** | ☑ | 2026-07-02 | Checklist de fecho: (1) navegação — regressão click-through completa dos 4 segmentos (Agências/Borda DC/DC Fabric/Edifícios) executada em 13.3, 1 link partido corrigido; (2) consistência de dados — bug crítico SPA/apiUrl (13.1) e auditoria de estado (13.4/13.5) aplicados a todos os cards de Rede tocados nesta sessão; (3) higiene de alertas — fora do âmbito de código, encaminhado à equipa de rede como Z.12/Z.13/Z.14/Z.15/Z.16 (tabela "Acções Zabbix"); (4) performance de arranque — recomendação registada no relatório de avaliação (`documentacao/relatorio-avaliacao-comercializacao-2026-07-03.md` §4), não medida/actuada nesta sessão — fica como iniciativa cross-domain futura, não bloqueia o fecho de Rede especificamente. **Domínio Rede considerado maduro e fechado** para o âmbito desta ronda; próximo passo é atacar outro domínio (Segurança/Bases de Dados/APIs/Serviços de Negócio — Fases 5-8, ainda por construir) |

## Fase 5 · Segurança (anchor 656, Infra)
| # | Tarefa | Estado | Data | Nota |
|---|---|---|---|---|
| 5.1 | N2 + N3 (firewall/WAF/Darktrace) | ☐ | | |
| 5.2 | Navegação + teste + commit | ☐ | | |

## Fase 6 · Bases de Dados (anchor 355, Infra)
| # | Tarefa | Estado | Data | Nota |
|---|---|---|---|---|
| 6.1 | N2 + N3 (MSSQL/Oracle/DB2) | ☐ | | |
| 6.2 | Navegação + teste + commit | ☐ | | |

## Fase 7 · APIs & Serviços (anchor 663 + 345, Infra)
| # | Tarefa | Estado | Data | Nota |
|---|---|---|---|---|
| 7.0.1 | Template único `BPC Web Monitoring v2` (14715) — fecho Fase C | ☑ | 2026-07-08 | 2 templates antigos (12042/12045) apagados; 28 cenários legados `Monitor *` desactivados (paridade confirmada); bug `{$TIMEOUT.L2/L3}` corrigido; L3/L4 off por omissão. Detalhe em `documentacao/mapa-apps-vms.md §1` |
| 7.0.2 | Tags `tipo`/`servico`/`vm`/`ip` nos 41 hosts `app-*` — fecho do elo app↔VM (Fase B) | ☑ | 2026-07-08 | 24 VMs confirmadas por IP ao vivo; reciprocidade `servico` nas VMs corrigida (2/15). Detalhe em `documentacao/mapa-apps-vms.md §2-3` |
| 7.0.3 | Investigação inicial de dependências BD/backend | ◐ | 2026-07-08 | 9/15 VMs auto-contidas confirmadas; 2 sem BD local (dependência externa por identificar); `ebankit` confirmado multi-VM (20 hosts, só 1 mapeado). Detalhe em `documentacao/mapa-apps-vms.md §4`. Pendente: decide se muda domínio/pasta Grafana por sistema |
| 7.0.4 | Retomar validação das tags automáticas (`inventario_zabbix_final.csv`) por descrição — consolidar VMs por serviço (`ebankit`, `PMSI`, `SAFIRA`, etc.) | ◐ | 2026-07-09 | **`ebankit` fechado**: 18 VMs reais confirmadas ao vivo (`host.get`, tags `servico`), reconciliado o "20 hosts" da 14.27 (2 novas + 1 duplicado zombie já apagado no Z.46, `vs8000740`, que tinha a tag errada — o real `VS8000740` é `canais digitais`, não `ebankit`). Detalhe em `mapa-apps-vms.md §4.5`. **`PMSI` confirmado ao vivo** (94 VMs hoje, vs 96 do CSV de Abril). **`ACM`**: contexto técnico novo — não é só do PMSI, fragmentado por 3 departamentos (8 PMSI+1 ACMRELATIONAL, 8 DTI, 1 DSE); pista de negócio encontrada (`VS8000345` liga a fornecedor "Assecco"/produto "MIA"); meaning de negócio ainda por confirmar. Detalhe em `mapa-apps-vms.md §8.6`. **Pausado por decisão do utilizador** (2026-07-09) — desviava do foco dos 50 sistemas do relatório de negócio; retomar só se/quando o negócio confirmar `ACM`/`MIA` |
| 7.0.5 | **Reconciliação formal dos 50 sistemas do Excel × `app-*` × VM** (objectivos 1-4 do utilizador, 2026-07-09) | ◐ | 2026-07-09 | Tabela mestra em `documentacao/reconciliacao-50-sistemas-excel.md`. **Bloomberg/Portal EMIS/BODIVA confirmados bloqueados** (`extract_hyperlinks.py` sobre o Excel-fonte: sem URL/Owner nenhum nas 3 linhas) — pendência de negócio formal, não atacar sem a URL real. **Objectivo 2 ("tags iguais") fechado para `VS8000305`/`VS8000304`**: verificação ao vivo (`host.get`) revelou que a "tensão N:M" já não existia — Zabbix já tem múltiplas tags `servico` no mesmo host em produção (10 em `VS8000305`, 3 em `VS8000304`), cada uma a bater 1:1 com o `app-*` correspondente; a auditoria de 05/07 em que a tabela se baseava estava desactualizada. Nenhuma escrita feita. **Multi-VM SACC/SGC/CONTIF fechados** (2026-07-09, `host.get` ao vivo, só leitura): `SACC` confirmado 4/4 VMs com tag correcta (`VS8000789` gateway + `VS8000134`/`347`/`867`), nenhuma escrita necessária. `CONTIF` confirmado 2/2 (`VS8000305`+`VS9000359`). **`SGC` suspenso** — `VS9000309`/`VS8000475`/`VS8000476` têm tag `servico=SGC` mas o visible name diz "Sistema de Gestão de Carteiras" (portfólio/crédito), não "Integração Movimentos Contabilísticos" (o SGC real do relatório) — suspeita de colisão de sigla, mesmo padrão do `ABC`/`VS8000102`; **não tratar como multi-VM sem confirmação de negócio**. **EQUATION/EBA/BFTELLER/MATCH CASH confirmados bloqueados** (não só falta de URL — `EBA` são serviços Windows `BranchAutomation`, `MATCH CASH` é app desktop/on-demand `Match Cash-2`, nenhum é app web): reclassificados de "falta criar app-*" para gap/bloqueado, candidatos ao objectivo 5 (monitoria de serviço/porta na VM, per plano original Fase C4), não ao objectivo 1. **Resultado final desta ronda**: 23 sistemas fechados, 2 com ressalva (SMS Banking, SGC), **12 gaps/bloqueados** (STC/CTB400/Audit Bank/Payment Manager/EURONET/Bloomberg/Portal EMIS/BODIVA/EQUATION/EBA/BFTELLER/MATCH CASH), 13/16 parceiros OK. **Objectivos 1-3 dados como fechados** nesta ronda — restam só pendências de negócio (URLs, confirmações de sigla) fora do alcance de mais leitura Zabbix |
| 7.0.6 | **Objectivo 5 — auditoria de saúde das 36 VMs dos sistemas fechados** | ◐ | 2026-07-09 | `host.get`+`problem.get` ao vivo, só leitura: templates correctos nas 36, **31/36 sem problema activo**. **5 achados** em `documentacao/reconciliacao-50-sistemas-excel.md §6`: `VS8000305`/Salas de Reuniões (Disaster, 3 meses, nunca notificado), `VS9000480`/FORGEST (2 serviços Windows parados há 3 meses), `VS9000359`/CONTIF (agente parado há 2,5 meses), `VS9000358`/ebankit Audit Prod (nunca reportou, candidato ao backlog Fase 14), `VS8000737`/ebankit BD (memória >90%, único fresco, desde ontem). **Tentativa de diagnóstico WinRM em `VS9000480` falhou por rede** (ICMP OK, portas 5985/5986 bloqueadas nesta sessão — segmentação BPC, não permissão) — correcção/diagnóstico destes 2 precisa de sessão com acesso à rede de gestão. Nenhuma escrita feita |
| 7.0.7 | **Objectivo 6 — mecanismo Zabbix→GLPI confirmado: é email-to-ticket, não API** | ◐ | 2026-07-09 | Confirmado ao vivo (`action.get`/`mediatype.get`): **não há integração API/plugin com o GLPI** — o GLPI abre 1 ticket por **cada email recebido** numa caixa monitorizada. Caminho: `trigger → action "Send Notification Email" (ON) → mediatype "Email" (hoje OFF) → caixa GLPI → ticket`. Ligar só o mediatype `Email` já reactiva a criação em massa (é o que aconteceu no incidente MSSQL conhecido). Detalhe e plano em `documentacao/auditoria-apis-servicos.md §4-bis`; `CLAUDE.md` e memória `feedback_zabbix-discovery-flood-risco` actualizados para cobrir também o mediatype `Email`, não só templates/discovery. **Pendente (pedido explícito do utilizador)**: documentar e planear a integração Zabbix↔GLPI a sério — (1) confirmar endereço/regras de parsing do email-to-ticket do lado do GLPI, (2) dependências/supressão de triggers antes de ligar (evitar 1 ticket por trigger numa cascata), (3) definir operação do dia-a-dia (quem tria, como fecha/faz ack, como evitar tickets duplicados por re-notificação a cada `esc_period`), (4) teste controlado com 1 trigger + 1 destinatário antes de alargar aos 6 utilizadores/grupo g663+g609. Nenhuma escrita feita |
| 7.0.8 | **Calibração dos triggers L1/L3 do template `BPC Web Monitoring v2` — bug crítico corrigido e validado** | ☑ | 2026-07-10 | Auditoria dos dashboards N2/N3 de APIs revelou que a coluna **Estado mentia** (apps down verdes; sites bons "conteúdo errado") — causa nos **triggers**, não nos web scenarios. **Bug L1**: `count(…,{$FAILS.BEFORE.ALERT},…)` usava o período em **segundos** (sem `#`); com item de 60s nunca havia N amostras em N segundos → **nunca disparava** (prova: **0 eventos L1 em 30 dias** vs 2 L2 / 8 L3, com apps down há 24h+). **Bug L3**: `last(fail[L3])>0` disparava em 1 blip e rotulava falhas de ligação como "conteúdo ausente". **Correcção** (`trigger.update` no template, propaga aos 40; ids 171845/171987/171848): macro nova `{$FAILS.WINDOW}` (`#3` template / `#6` nos 15 externos — o Zabbix não aceita `#{$MACRO}`, a macro tem de conter o `#`), `{$FAILS.BEFORE.ALERT}` (3/6) mantida p/ nome+limiar; L1 → `count(…,{$FAILS.WINDOW},"eq","1")={$FAILS.BEFORE.ALERT} and find(…error…,"like","Response code")=0/1`; L3 → idem + `find(…,"like","required pattern")=1` (só conteúdo real, separa de site-down). **Validado ao vivo**: 7 apps down passaram a [L1] (nomes "3+/6+ falhas" batem com o comportamento), bpcao/sap deixaram o falso [L3], sgc mantém [L3] real, live [L2] inalterado. **Sem notificações** — acção "Send Notification Email" entrega só por mediatype `Email` (OFF), verificado antes da escrita. Detalhe em `documentacao/mapa-apps-vms.md §1.1`. **Pendências**: `{$STRING.CHECK}` do `app-bpcao` é lixo (GUID) — corrigir/desligar L3 nesse host; triggers do template não saem no `configuration.export` (recriar como exportáveis) |
| 7.0.9 | **6 externos em falso-DOWN = bloqueio de saída de rede do DC, não bug de monitorização** | ◐ | 2026-07-10 | Após o L1 passar a disparar (7.0.8), 6 monitores externos (`app-bpcao`/`sap`/`inss`/`mundial-seguro`/`pumangol`/`emp`) ficaram DOWN com `SSL_ERROR_ZERO_RETURN`. Diagnóstico a partir do próprio servidor Zabbix (curl/openssl/nc): TCP:443 abre mas o servidor fecha logo após o ClientHello (`written 335, read 0`); todas as variantes TLS/cipher/http1.1 falham igual; **`curl google.com`→200 e `microsoft.com`→404 (TLS OK)** ⇒ o servidor TEM saída HTTPS pública, só estes destinos estão bloqueados (allow-list por destino; contraste `inss.gov.ao` bloqueado vs `bpc.inss.gov.ao`/`minfin` OK). **Não é Zabbix — é rede; não há proxy configurado, saída é directa.** **Fix real (pendente, rede)**: autorizar saída 443 do IP do Zabbix para os 6 destinos (lista em `catalogo-50-sistemas.json _meta.pendencia_saida_dc` e `mapa-apps-vms.md §1.2`) — depois os scenarios funcionam sem mexer no Zabbix. **Acção provisória**: 6 hosts em manutenção com nota. **Ressalva**: manutenção suprime eventos/notificações e painel nativo, mas **não** repinta a coluna Estado do N2/N3 (deriva de `triggers.value=1` directo, não suppression-aware) — vista custom precisa de filtro de suprimidos ou tag de estado à parte |
| 7.1 | N2 + N3 (endpoints/sintéticos) | ☐ | | |
| 7.2 | Navegação + teste + commit | ☐ | | |

## Fase 8 · Serviços de Negócio — eBankit (anchor 391, Infra)
| # | Tarefa | Estado | Data | Nota |
|---|---|---|---|---|
| 8.1 | N2 + N3 (jornadas/transacções) | ☐ | | |
| 8.2 | Navegação + teste + commit | ☐ | | |

## Fase 9 · Agências (anchor 24 + 25, **Network**) — sub-domínio de Rede

> **Fluxo real (2026-06-27):** Agências é sub-domínio de Rede. Percurso
> `N1 → N2 Rede → N3 Agências (geomap) → N4 Agência (detalhe/diagnóstico) → N5 Interfaces`.
> Desenho e decisões em `documentacao/fluxo-agencias-n4-n5.md`.

| # | Tarefa | Estado | Data | Nota |
|---|---|---|---|---|
| 9.0 | Bugs do fluxo (B-01/B-02 N2, B-03 N4, T-01 N1, T-03) | ☑ | 2026-06-27 | NET_THR + apiUrl Network (N2); âncora de rede no N4 (B-03); N1 liga ao N3 (T-01); drill por var-host (T-03). Detalhe em engenharia §12.0. Commits `3dcc616` |
| 9.1 | N3 Agências — geomap + tabela de alertas (nativo) | ☑ | 2026-06-27 | UID `n3-agencias`; geomap + tabela → drill N4 (dataLink) |
| 9.2 | **N4 Agência — reconstruído (triagem NOC)** | ☑ | 2026-06-27 | UID `n4-agencia-detalhe`. Dropdown por **nome** (MySQL); ESTADO (disponib+ficha nativa) · PORQUÊ (CPU/RAM/uptime/lat/loss) · LINKS WAN (estado colorido + histórico timeline) · PROBLEMAS (painel **nativo** Zabbix) · TENDÊNCIA · botão N5. Item filtrado por **nome**. Commits `9d98685`/`a833c5a`/`996d566` |
| 9.3 | **N5 Agência — Interfaces** (exclusivo, `n5-agencia-interfaces`) | ☑ | 2026-06-27 | Criado. Header + back-link N4; Estado&flaps (state-timeline todas as ifaces); Tráfego rx/tx; Erros in/out; Descartes in/out. Dropdowns Agência(nome) + Interface(All+filtro). Item por **nome**. Utilização % fora (speed=0 nos tunnels → T-08). Snapshot guardado. Validado RTUIGE00 |
| 9.4 | Refinamentos N5: ~~provider/tipo da interface (T-06)~~ · **T-08** utilização % | ☑ | 2026-06-29 | **T-06 ✅**: dropdown `iface` do N5 → MySQL, mostra `<token>(<provider>)` no `__text` e token limpo no `__value`. **T-08 ✅**: `l5-utilizacao.js` (id=101) no N5. Física (speed>0) → barras RX%/TX% com cores verde/âmbar/vermelho (<70%/70-90%/≥90%). Modo All: mostra top 4 interfaces activas (rx>0 ou tx>0); oculta idle (evita scroll); rodapé conta ocultas + túneis. Modo interface específica: mostra 1 card; se túnel → mensagem "ifSpeed=0, N/A". Validado 3 cenários API: All (3 físicas activas + footer "6 ocultas · 3 túneis"); Gi0/0/0.914 (1 card speed=1Gbps RX=0.58%); Tu101 → N/A. |
| 9.5 | Navegação ponta-a-ponta N1→N5 + commit final | ☑ | 2026-06-27 | Fluxo N1→N2→N3→N4→N5 validado no browser; commits locais (repo sem remote → push git pendente) |
| 9.6 | **Correlação por provider (hub DMVPN) para causa de agências DOWN** (T-09) | ☑ | 2026-06-29 | **v1+v2**: painel N4 `l4-provider-context.js` (id 211). Dois sinais por operadora: **Transporte** (Po2.x Operational status, veredicto) + **Pulso** (Tu10x Bits received, contexto). **v2**: lê Tu10x da **config** do router da agência (sobrevive ao DOWN, lastclock=0) → destaca operadoras que a agência usa em cards completos; outras operadoras colapsadas em pills. Validado: CUNHINGA (DOWN) → Tu101+Tu107 → UNITEL+MST·MW em destaque, ambas UP → leitura "problema local"; RTUIGE00 (UP) → Tu101 → UNITEL em destaque. Pendente Z.15: per-spoke (estado do túnel desta agência no hub), depende LLD CISCO-IPSEC-FLOW no hub. |
| 9.8 | **Gap A — GPS em falta no geomap** | ☑ | 2026-06-29 | Auditoria 221 hosts g24 via API Zabbix: 49 sem `location_lat`/`location_lon`. Cruzamento com Excels BPC (Dados das Agências + Lista Geral + CSV). 46 populados (45 confirmados por província/município + RT_UBM30 Km30 Luanda/Viana). 3 IPs anónimos (`172.22.1.38`/`.203`/`.240`) excluídos: criados manualmente, DOWN há semanas, zero tags/inventory, ausentes dos Excels → aguardam identificação pela equipa de rede. Geomap: **172 → 218**. Artefactos: `BPC/audit_gps_g24.csv`, `BPC/hosts_sem_gps_preenchido.csv`. |
| 9.9 | **Auditoria de qualidade inventário/tags — 221 hosts g24** | ☑ | 2026-06-29 | Auditoria completa: GPS vs bounding-box provincial, tags (unidade_negocio/tipo_un/provincia) vs Excel, inventory site_state vs Excel. 193/221 sem divergências. 24 corrigidos: (1) **RTMENONG00 GPS** lat=-16.66 → -14.657 (erro de 223km, Menongue); (2) **6 hosts Icolo e Bengo** tag `provincia` "Luanda"→"Icolo e Bengo" + `site_state` (reorganização admin. 2011: Cabo Ledo, Muxima, Siac Zango, Zango, SIAC Cacuaco, Catete); (3) **2 hosts Moxico Leste** "Moxico"→"Moxico Leste" (Luau, Cazombo); (4) **4 hosts tags ausentes** unidade_negocio+tipo_un populados do Excel (RTCAMB00, RTLUCA00, RTFAASU00, 172.22.1.202). Divergências residuais: naming Cubango/Cuando Cubango (Excel truncado, tags correcto — aceite) + 11 GPS bbox falso-positivo (bounding boxes aproximados). Artefacto: `BPC/inventory_divergencias.csv`. |
| 9.10 | **N5 — título dinâmico no header** | ☑ | 2026-06-29 | `utils.js` N5: `buildNocLabel()` lê `context.grafana.templateSrv` (variável `host`) → `AGÊNCIAS · NÍVEL 5 · INTERFACES · <router> · <nome agência>`. `setInterval` 1s actualiza o `<em>` quando o dropdown muda. Validado: URL `?var-host=RTUIGE00` → label correcto. Push já efectuado (id=1). |
| 9.11 | **Botão N4→N5 inteligente** (desactivar quando DOWN / sem SNMP) | ☑ | 2026-06-29 | `l4-n5-button.js` (id=101) substituiu `n4-n5-button.json`. Painéis antigos (id=100 orphan + id=209 texto estático) removidos via API. Validado: DOWN (CUNHINGA) → DOM confirma `⊘ N5 · Interfaces — agência DOWN, sem dados úteis`; UP (RTUIGE00) → Zabbix API confirma `pingVal=1, hasSnmp=true, btnState=ok`. Três estados funcionais: ok/down/no-snmp + fail-open. |
| 9.7 | **Agências/postos sem router próprio (ponto-a-ponto)** (T-07) | ☐ | | Muitas unidades intermitentes/móveis **não têm router próprio** — ligam ponto-a-ponto a um pai. **Auditoria 2026-06-28 (3 Excels + cruzamento com Zabbix).** A população **sem router próprio** é precisa: **14 Balcões Intermitentes/Móveis** (na `Lista Geral`) — **sem código, sem IP MGMT, ausentes do Zabbix** → invisíveis. **O pai está no NOME** (com código): `Ag. Cunhinga (0312)`, `Ag. EBO (0199)`, `Ag. N´harea (0311)`, `Ag. Catabola (0310)`, `Ag. Ukuma (0116)`, `Ag. Cacula (0107)`, `Ag. Cangamba (0089)`, `Ag. Samba Cajú (0180)`, `Ag. Gulungo Alto (0263)`, `Ag. Kalandula (0187)`, `Ag. Sanza Pombo (0144)`, + Móveis Andulo/Samanhonga/Candembe. Logo o mapa filha→pai **é derivável do nome** (não precisa do inventário de equipamentos). **Importante (corrige equívoco):** "sem SNMP" ≠ "dependente" — os ~71 routers sem SNMP fresco são **routers reais** com sessão SNMP partida (Z.14), NÃO dependentes; os dependentes nem sequer têm IP. Plano: (1) extrair pai do nome dos 14; (2) monitorizá-los via **sub-interface/porto do router-pai** (depende de Z.14 SNMP no pai); (3) ficha marca "sem router próprio (dependente de X)". Deliverable A feito: `BPC/mapa_agencias_zabbix.csv` (IP↔nome↔tipo↔estado SNMP, 226 linhas) → naming Z.14 + enriquecer ficha. |

## Fase 10 · N1 · Portal NOC (porta de entrada — spec em engenharia §4.2)
| # | Tarefa | Estado | Data | Nota |
|---|---|---|---|---|
| 10.1 | Cards compostos, 1 por domínio (BT) | ☐ | | cada um liga ao `dashUid` do N2; domínios por construir = card "em construção" sem link |
| 10.2 | Estado agregado coerente (consome `BPC.state`) | ☐ | | rollup down/warn ao vivo por card; N1 não contradiz N2/N3 |
| 10.3 | Logo + header canónico (reutiliza `utils.js` §5.1) | ☐ | | é o menu de entrada com logo que o utilizador pediu |
| 10.4 | Definir como Home da org (`PUT /api/org/preferences`) | ☐ | | abrir Grafana cai no Portal |
| 10.5 | Layout wallboard + teste + commit | ☐ | | vive em `visao-geral/n1/` |
| 10.6 | Remap das áreas do N1 — modelo em camadas chão-de-fábrica→enduser (v2.0) | ☑ | 2026-07-06 | **Gatilho**: durante a validação pré-demo (Fase 1.24), o utilizador questionou se "domínios" é termo aceitável para o cliente e se os 9 cards não tinham sobreposição — identificado que **Agências não devia ter card próprio**: o `CLAUDE.md` já documenta "Agências = segmento de Rede" (pasta `04·Rede`›`04.1·Agências`, `09·Agências` vazia/para arquivo), mas o N1 mostrava-a como card irmão de Rede, com KPIs próprios e link directo para `rede-n3-agencias` que salta o N2. **Resolvido**: card Agências removido do N1 (`n1-cards.js` `CFG.domains`, 9→8 entradas); sublabel de Rede passou a mencionar "Agências" explicitamente. **Reordenação** dos 8 cards restantes segundo um modelo em camadas físico→enduser: Infraestrutura VMware (físico+virtualização) → Servidores Virtuais → Armazenamento → Rede → Bases de Dados → APIs & Serviços → Serviços de Negócio → Segurança; Segurança passa a sublabel "Transversal" (protege todas as camadas, não é um degrau acima delas). **Renomeado** "Serviços de Negócio" → "Serviços de Negócio (Aplicações)" para deixar explícito que é a camada de aplicações que o utilizador final usa (distinta de "APIs & Serviços", reservada para disponibilidade técnica de endpoints/sintéticos — fronteira a manter quando a Fase 7/8 forem construídas, para não repetir a fragmentação já vista na tag `servico`). Terminologia "domínio"→"área" recomendada para a apresentação ao cliente (mais neutro), sem necessidade de mexer em UIDs/pastas. Pushed e verificado ao vivo (`n1-visao-geral-noc`, painel `n1-cards.js` id=101) — 8 cards renderizam correctamente, Rede mostra 1 crít/226 aviso agregado |

## Fase 11 · (Fase 2 do projecto) N0 · Executivo/SLA
| # | Tarefa | Estado | Data | Nota |
|---|---|---|---|---|
| 11.1 | Disponibilidade %, tendências, SLA | ☐ | | adiado |

## Fase 12 · Reorganização da estrutura no Grafana (transversal — spec em engenharia §4.2)
> Decisões 2026-06-19: pastas de domínio **no topo** (sistema limpo, sem
> coexistir com legado) · TODO o legado consolidado numa pasta `99 · Arquivo`
> única (destino decidido no fim) · execução em sessão dedicada, com confirmação
> de push. Estado actual = 82 dashboards, 17 achatados na `dashboards v5`.

| # | Tarefa | Estado | Data | Nota |
|---|---|---|---|---|
| 12.1 | Criar pastas de domínio no topo (`00`–`09`) + `99 · Arquivo` | ☑ | 2026-06-19 | 11 pastas criadas via `/api/folders`; UIDs registados no CLAUDE.md |
| 12.2 | Mover + renomear os 14 dashboards de produção v5 | ☑ | 2026-06-19 | 14/14 OK: 01·VMware (4), 02·Armazenamento (1), 03·Virtuais (2), 04·Rede (7). Títulos `N2/N3/N4 · …` |
| 12.3 | ~~Esclarecer duplicado vmware vs virtuais~~ — RESOLVIDO | ☑ | 2026-06-19 | Não é duplicado: são 2 domínios (blueprint §56-57). VMware (`a967e936`)→pasta 01; Servidores Virtuais (`0758c24e`)→pasta 03. Mapa de migração corrigido em engenharia §4.2 |
| 12.4 | Consolidar legados + testes em `99 · Arquivo` | ☑ | 2026-06-19 | 67/68 arquivados. 1 preso: `visao-geral-v5` (payload não transferível via CDP) — já estava em pasta de arquivo legado, fica lá. IA não apaga; eliminação fica para o utilizador |
| 12.5 | Esvaziar/arquivar `dashboards v5` + actualizar constraint do CLAUDE.md | ☑ | 2026-06-19 | `dashboards v5` (`efpbu5tvrhce8a`) ficou vazia; constraint do CLAUDE.md substituído pelos UIDs das pastas de domínio. Pastas legadas numeradas `00`–`08` (com `-`) ficaram vazias — eliminação fica para o utilizador |
| 12.6 | Sincronizar `dashboardTitle` + `folderUid` nos `manifest.json` | ☑ | 2026-06-19 | 14 manifests actualizados (título novo + folderUid da pasta de domínio; 4 sem folderUid passaram a tê-lo). Evita que o próximo push devolva o dashboard à pasta velha. JSON validado |

---

## Fase 13 · Auditoria de comercialização + saneamento transversal (2026-07-02/03)

> Sessão de avaliação sénior (engenheiro network/sistemas): testou todos os
> dashboards/drill-downs, produziu `documentacao/relatorio-avaliacao-comercializacao-2026-07-03.md`,
> corrigiu o bug crítico N2 Rede e depois auditou a lógica de estado (ok/warn/crit)
> em todos os domínios contra o princípio "Zabbix é a fonte de verdade" (§4A dos
> CLAUDE.md locais / §6 engenharia).

| # | Tarefa | Estado | Data | Nota |
|---|---|---|---|---|
| 13.1 | Bug crítico N2 Rede: dashboard a zeros pós-navegação SPA | ☑ | 2026-07-02 | Causa-raiz: guard do `utils.js` só comparava `version`, não `apiUrl` — `rpc` de um dashboard anterior sobrevivia à navegação SPA e apontava para o datasource errado. Fix: guard passa a validar `window.BPC._apiUrl === CFG_META.apiUrl`; propagado às ~22 cópias de `utils.js` do repo |
| 13.2 | Repo Git: reescrita de histórico + separação em repo standalone | ☑ | 2026-07-02 | `tok3n` e token Grafana hardcoded encontrados no histórico antes do 1º push — histórico reescrito via `git filter-branch` (verificado: zero matches para `glsa_`, chaves AWS, headers de chave privada). `sistema-de-observabilidade` tornado repo próprio (opção A), ligado a `github.com/Zecaspider/zabbix`; `.gitignore` reforçado (`tok3n`, `*.key`, relatório diário confidencial mantido fora do git por decisão do utilizador) |
| 13.3 | Fluxo de fecho Rede: regressão de navegação nos 4 segmentos | ☑ | 2026-07-02 | Click-through completo (Agências/Borda DC/DC Fabric/Edifícios); 1 link partido encontrado e corrigido — `l4-n5-button.js` das Agências apontava para UID pré-migração `n5-agencia-interfaces` (faltava prefixo `rede-`) |
| 13.4 | Auditoria completa da lógica de estado (todos os cards, todos os domínios) | ☑ | 2026-07-02 | Agente Explore mapeou toda a lógica de classificação ok/warn/crit do repo. Achados: (a) card Edifícios N3 classificava por contagem de alertas em vez de severidade máxima da trigger; (b) `lossPct.crit` inconsistente (5 vs 10) entre cards de rede; (c) função `severityToState` duplicada localmente em `l2-dc.js`; (d) thresholds de CPU/RAM/disco em VMware e Servidores Virtuais desviados do catálogo canónico §6.2; (e) tabela local `03-servidores-virtuais/CLAUDE.md` §7 conflituava com a global — resolvido a favor da global (decisão do utilizador) |
| 13.5 | Aplicar correcções do 13.4 | ☑ | 2026-07-02 | 13 ficheiros `.js` corrigidos (Edifícios N2/N3, VMware N2/N3, Servidores Virtuais N2/N3) para usar `severityToState`/thresholds do catálogo §6.2; `l2-dc.js` deixa de duplicar `severityToState`; `03-servidores-virtuais/CLAUDE.md` §7 actualizado para reflectir a global como fonte de verdade. Commit `3b76bae` |
| 13.6 | Bug crítico descoberto durante a auditoria: corrupção silenciosa do `push_panel.py` | ☑ | 2026-07-02 | A reconciliação pós-push casava painéis por `title` (sempre `""` por convenção NOC) em vez de `id`/`content`, colapsando todo o `manifest.json` para o id do último painel processado sempre que um dashboard já tinha vários painéis sem título. Causou perda de conteúdo do painel `l3-ficha-servidor.js` (N3 VM Detalhe) — recuperado e verificado ao vivo (rootId + thresholds confirmados via API). Fix aplicado em `push_panel.py` (match só para `id=null`, chave = `content`); os outros 4 dashboards tocados na mesma sessão confirmados não-corrompidos |
| 13.7 | Corolário Zabbix-fonte-de-verdade: `l2-correlacionador-de-eventos.js` sem manifest | ☑ | 2026-07-02 | Confirmado por query directa à API do dashboard N2 · VMs (`0758c24e...`): só 4 painéis existem ao vivo (header/KPI/tabela/triggers) — o ficheiro é **órfão**, nunca foi implantado apesar de o CLAUDE.md local o listar como "✅ Activo". Threshold corrigido no ficheiro mesmo assim (fica correcto se um dia for implantado). Documentado em `03-servidores-virtuais/CLAUDE.md` §2; decisão de implantar vs arquivar fica para o utilizador |

---

## Fase 15 · Auditoria transversal Zabbix (grupos/hosts/templates/items/triggers) + cruzamento Grafana (2026-07-04)

> **Escala real** (Infra + Network): 101 grupos, 968 hosts (877 activos), 666
> templates, 166.506 items, 69.442 triggers. Auditoria por amostragem
> estrutural e procura dirigida de padrões de excepção — não revisão
> exaustiva (inviável a esta escala).

| # | Tarefa | Estado | Data | Nota |
|---|---|---|---|---|
| 15.A | Fase A — estrutural (grupos/hosts/templates) | ☑ | 2026-07-04 | Grupos: 7 vazios (maioria lixo padrão Zabbix), **naming não é 1-2 casos — é divisão sistemática** entre ~15 grupos `"BPC / X / Y"` (espaçado) e ~70 `"BPC/X/Y"` (sem espaço); **2º par de duplicados confirmado** (`BPC/SERVICO/Git lab` vs `Gitlab`, Z.4) + 1 novo (`BPC / INFRAESTRUTURA / DISPOSITIVOS DE SEGURANÇA`, grupo 655 vazio/encoding corrompido, duplica o 656 com 8 hosts). Hosts: 0 sem grupo, 5 sem template, 37 sem interface. Templates: 297/314 "sem host" — quase tudo biblioteca standard Zabbix (ruído esperado); **6 personalizados genuinamente não usados**: `AS400` (liga ao gap CTB/400 já documentado), `BPC IIS by Zabbix agent`, `Windows generic by Zabbix agent1`, `App Zabbix Agent`, `Template App IBM MQ`, `Virt VMware Hypervisor ESXi - Andre Bello` (nome de pessoa, rascunho nunca limpo) |
| 15.B | Fase B — duplicados `VM.UUID`, escala total (650 hosts Infra) | ☑ | 2026-07-04 | Confirma que o problema estava bem delimitado — os mesmos 7 pares já conhecidos do grupo 609, nenhum novo fora dele. 6/7 já resolvidos, só falta `VS8000863`/`VS8000481` (credencial vCenter PowerFlex, Z.8) |
| 15.C | Fase C — triggers com referência cruzada errada (padrão Z.16) | ☑ | 2026-07-04 | **Custo real não compensou o retorno** (lição registada — ver nota do utilizador abaixo). Cruzados 41.972 triggers monitorizadas × 111.532 funções × todos os items do Network — **0 incompatibilidades novas encontradas**; o único caso conhecido (Z.16, `GTW01`) afinal **já não reproduz** (trigger aponta correctamente ao próprio item do host) — corrigido o registo em Z.16. **Custo**: 2 pedidos `trigger.get` com `selectFunctions` em escala total causaram **HTTP 500 no servidor Zabbix Network** (esgotamento de memória do lado do servidor, sem escrita nenhuma envolvida) antes de se descobrir que era preciso paralelizar em lotes de ~15 hosts. **Decisão do utilizador: não vale o esforço/risco** — não repetir auditorias de triggers em massa deste tipo sem avaliar melhor o custo/benefício primeiro |
| 15.D | Fase D — saúde agregada de items (D1 concluído, D2 travado) | ◐ | 2026-07-04 | **D1**: 57.994 items activos (hosts+items `status=0`) no Infra — **49.9% (28.924) nunca recolheram dado nenhum**, mesmo depois de filtrar o ruído de hosts/items desactivados (56.4% sem filtro). Número real e substancial, não artefacto. **D2** (partir por grupo): tentado juntar `hostid` a cada item, em lotes de 40 hosts (timeout aos 5min) e depois 10 hosts (ainda muito lento, ritmo implicava >1h para os 56 lotes) — **abortado a meio**, sem insistir mais (lição da Fase C aplicada). Achado lateral: a lentidão persiste mesmo com lotes pequenos, sugere que o próprio servidor Zabbix Infra tem dificuldade nesta consulta específica (`item.get` com `hostid` no output), não é só questão de tamanho de pedido |
| 15.F | Resolução dos 2 duplicados de grupo confirmados na 15.E | ☑ | 2026-07-06 | **`655`/`656`** (Segurança): `655` confirmado genuinamente vazio (0 hosts) — apagado; `656` (Imperva ×3 + Check Point ×5) mantido sem alterações. **`412`/`416`** (GitLab): antes de mexer, confirmado que ambos os hosts (`VS8000700` PMSI, `VS8000890` DTI) estão **desactivados** (`status=1`, QA) — housekeeping de baixo risco, sem impacto em monitorização activa. `VS8000890` movido do grupo `416` para o `412` (mantendo as outras 4 associações: Camada Aplicacional, QA, Linux, Servidores Virtuais); `412` renomeado para `"BPC/SERVICO/GitLab"` (grafia oficial, mesma escolha já feita na tag `servico`); `416` apagado (ficou vazio). Verificado ao vivo: `412` com os 2 hosts, `416`/`655` já não existem |
| 15.E | Fase E — cruzamento com Grafana: âncora Storage partida em 7 dashboards | ☑ | 2026-07-06 | **Gatilho**: utilizador reportou `N1 · Visão Geral — Estado global` a mostrar "The query didn't return any results", apesar de estar "fechado" antes. **Causa raiz confirmada** (`auditlog.get`, groupid 602): o grupo `BPC / INFRAESTRUTURA  / STORAGE` (2 espaços, já documentado como "problema de higiene" no `mapa-host-groups.md` desde Junho) foi **corrigido no Zabbix em 2026-07-04** por `jmuque` (renomeado para 1 espaço) — mas nenhum dashboard Grafana foi actualizado em conjunto, partindo todos os que usam esta âncora canónica partilhada. **Auditoria alargada** (pedido do utilizador): cruzados todos os 27 dashboards activos (fora de `99·Arquivo`) contra os grupos live de Infra (86) e Network (15 relevantes) — **7 dashboards confirmados afectados, 29 alvos de painel no total**: `n1-visao-geral-noc`, `visao-geral-v5-rebuild` (transitório), `a967e936` (N2 VMware), `8f6a94be` (N3 ESXi Hosts), `59e7e4b2` (N3 vCenter), `993834a3` (N2 Armazenamento), `0758c24e` (N2 Servidores Virtuais). Dashboards de Rede (Network Zabbix) confirmados ilesos — usam variáveis (`${group}`) ou grupos `HG_*` estáveis. **Corrigido** (confirmação explícita do utilizador antes do push): os 29 alvos actualizados para 1 espaço via API Grafana (`dashboards/db`), verificado por dry-run antes e depois (0 ocorrências antigas restantes) + query directa ao datasource (`ds/query`) confirmando 3591 pontos de dados no último dia. 1 dashboard (`a967e936`) falhou 3× via `urllib` do Python (connection reset, causa não apurada — não era timeout nem payload demasiado grande) — resolvido trocando para `curl` como cliente HTTP, sucesso à primeira. **Fix adicional**: `push_panel.py::ANCHOR_TARGET` (constante usada ao criar novos painéis) tinha o mesmo espaço duplo — corrigido para não repetir o bug em painéis futuros. **Achados extra no Zabbix** (grupos): `655`/`656` (SEGURANÇA/SEGURANCA, cedilha) confirmado ainda duplicado (655 órfão, 0 hosts) e `Git lab`/`Gitlab` (412/416) confirmado ainda por fundir — **decisão do utilizador: só documentar, não escrever no Zabbix agora**. **Pendente**: snapshots locais (`dashboard-completo.json` em `01-infraestrutura-vmware/n3-esxi` e `03-servidores-virtuais/n2`) ficaram desactualizados face ao Grafana — não resincronizados por falta de mapeamento inequívoco UID↔ficheiro sem risco; resolver no próximo fecho formal desses dashboards. **Lição**: renomear um grupo Zabbix usado como âncora partilhada por vários dashboards exige verificar/corrigir todos os consumidores, não só o nome na origem |

---

## Fase 14 · Recuperação de agentes Zabbix nas VMs de Produção (transversal, pré-requisito Fase 7) (2026-07-03/04)

> **Ponto de partida:** auditoria `documentacao/auditoria-apis-servicos.md`
> (2026-07-03) — mapeamento URL→serviço→VM para o domínio APIs e Serviços
> revelou que a causa raiz não era o upgrade Zabbix 6→7, mas sim agentes
> nunca corrigidos/instalados/configurados. Sem sinal fiável ao nível da VM,
> alarmística e dashboards de Fase 6/7/8 assentariam em dados que não se
> pode confiar — daí este trabalho preceder a Fase 7. Detalhe completo,
> host a host, em `C:\Repositorios\zabbix\bpc-workspace\vm-agente-recuperacao-handoff-20260703.md`
> e `...-20260704.md` (fora deste repo — ficheiros de trabalho operacional).

| # | Tarefa | Estado | Data | Nota |
|---|---|---|---|---|
| 14.1 | Baseline medido ao vivo (grupo 609, `agent.ping`) | ☑ | 2026-07-03 | 110 frescas / 257 nunca reportaram / 79 sem agente por desenho, de 446 VMs. Produção: 68/280 (24%) a reportar |
| 14.2 | Recuperação lote 1 — `ServerActive`/`Hostname` errados | ☑ | 2026-07-03 | 46 VMs de Produção corrigidas (duplicados limpos: 4). Produção → 114/280 (41%). Handoff `-20260703.md` |
| 14.3 | Recuperação lote 2 — instalação onde não havia agente | ☑ | 2026-07-04 | 21 VMs (10 base + 7 com plugin MSSQL + 4 desbloqueadas via WinRM/WMI) instaladas e confirmadas frescas. Bug crítico encontrado e corrigido na fonte: `Install-ZabbixAgent2-MSSQL.ps1` gravava `mssql.conf` sempre com BOM (`Set-Content -Encoding UTF8` no PowerShell 5.1) — candidato a explicar boa parte do balde MSSQL. Ver Z.17 |
| 14.4 | Grupo "WinRM não responde" (41/101 Windows) — categorizado | ☑ | 2026-07-04 | 4 causas distintas: 12+1 IP inválido no Zabbix (Z.19), 4 sem resposta nenhuma (Z.21), 24 vivos só sem WinRM — destes, 4 destravados via WMI/DCOM (porta 135) e agente instalado |
| 14.5 | Grupo "acesso negado" (8 hosts) — investigado | ☑ | 2026-07-04 | 5/8 são infra-estrutura SWIFT (Z.20, não mexido — segregação provável por política). Dos 3 restantes, 2ª credencial destravou 1 (`VS8000904` — corrigido `Hostname`, mas bloqueado por firewall de rede na porta 10051, ver nota em 14.6); 2 continuam sem acesso com nenhuma das 2 credenciais |
| 14.6 | Estado actual medido ao vivo (2026-07-04, fim de sessão) | ☑ | 2026-07-04 | Produção: **281 VMs activas, 238 com agente configurado, 122 a reportar (`agent.ping` fresco <15min), 116 nunca reportaram**. 43 sem agente por desenho (VMware/ping-only) |
| 14.8 | `VS8000904` — diagnóstico de rede aprofundado, fechado (escalar) | ☑ | 2026-07-04 | Reconfirmado que **não é bloqueio específico da porta 10051** — testadas mais 3 portas do servidor Zabbix (80, 3000, 10050) a partir da VM, nenhuma atravessa. Descartada causa local (`Get-NetFirewallProfile`: `DefaultOutboundAction=NotConfigured` nos 3 perfis, 0 regras outbound a bloquear). É isolamento de rede completo entre sub-redes `10.10.204.x`↔`10.10.126.x` (rota/VLAN/ACL) — nada mais accionável sem acesso a equipamento de rede. Ver Z.21 actualizado |
| 14.9 | `VS9000424`/`VS9000343` (UAC Remote Restriction) — decisão: não mexer | ☑ | 2026-07-04 | Fix identificado (`LocalAccountTokenFilterPolicy=1` no registo) mas **decisão explícita do utilizador de não enfraquecer UAC remoto** só para destravar monitorização em 2 VMs Server 2008/2008R2. Ficam de fora até alguém configurar WinRM localmente (consola/RDP). Ver Z.24 actualizado |
| 14.11 | Mapeamento dos 3 vCenters + teste de acesso real | ☑ | 2026-07-04 | Login testado via REST API (sem imprimir passwords): acesso **OK** a VCenter PRD (`10.10.101.9`) e vCenter Backup Cluster Swift (`10.10.101.30`); **falha (401)** em vCenter PowerFlex/"vCenter 02" (`10.10.232.84`) — credencial errada confirmada, não rede (ver Z.8 actualizado). **Consequência prática:** com acesso a 2/3 vCenters, a resolução de Z.19 (IPs inválidos no Zabbix, bloqueada por falta de acesso vCenter) pode avançar na próxima sessão para as VMs desses 2 vCenters |
| 14.12 | Grupo A (Z.19) — resolvidos 2/13 via vCenter PRD | ☑ | 2026-07-04 | `VS6000001`, `VS6000005`: placa de rede desligada (`NOT_CONNECTED`), sem sinal de isolamento deliberado no histórico de eventos (cliente SOAP construído nesta sessão) — reconectada, tornada persistente, IP corrigido no Zabbix, `icmpping` fresco confirmado. Ver Z.19 actualizado |
| 14.13 | Tentativa de credencial alternativa no vCenter PowerFlex — falhou | ☑ | 2026-07-04 | Testado `administrator@vsphere.local` (conta do vCenter PRD) contra `10.10.232.84` — falhou (401). Confirma que não há partilha de conta entre vCenters; precisa de credencial nova e válida, não vale a pena continuar a tentar contas conhecidas de outros vCenters |
| 14.14 | Investigação dos 4 pendentes de Z.19 — 1 resolvido, 3 diagnosticados | ☑ | 2026-07-04 | `VS8000526`/`VS8000888` confirmados como o **mesmo VM** (UUID idêntico) registado 2x no Zabbix, não conflito de IP real — duplicado desactivado, host correcto corrigido, ping confirmado. `VS9000711`: descartada causa de rede (2 VMs vizinhas no mesmo portgroup `dvportgroup-92` têm IP normal) — problema é do guest OS, fora do alcance da API do vCenter. `VS8000394`: indício de dispositivo edge/DMZ deliberado (hostname `edge.bpc.ao`, rede isolada `192.168.1.0/24`) — não mexido, precisa de decisão. `Graylog`: sem IPv4 nenhum detectado, por investigar na consola. Nota técnica: `filter.networks` não filtra nesta API (devolve tudo sem erro) — confirmar portgroup de pares por amostragem manual |
| 14.10 | Pendente — próxima sessão | ☐ | | Ordem sugerida: (a) decidir SWIFT (Z.20); (b) decidir `VS8000394` (edge/DMZ deliberado?) e investigar `Graylog`/`VS9000711` via consola do guest (causas já isoladas, Z.19); (c) obter credencial válida para o vCenter PowerFlex (Z.8/Z.19, 7+ hosts dependem disto); (d) escalar à equipa de rede os hosts sem resposta nenhuma (Z.21) e o isolamento de sub-rede de `VS8000904`; (e) decidir Server 2003 (Z.23) |
| 14.15 | Recontagem de Produção + reconciliação dos 123/43 sem dado + Grupo D atacado | ☑ | 2026-07-04 | Recontagem ao vivo (ambiente normalizado, Z.11): **299 VMs de Produção** — 133 frescas, 123 nunca reportaram, 43 sem nenhum template de agente (`BPC Ping`+`VMware Guest` só). **Achado importante**: os 43 não são um caso pontual — é um **fleet Linux inteiro nunca provisionado com agente**, incluindo um **cluster IBM Cloud Pak/OCP de produção completo (15 nós)**, stack Elasticsearch/Graylog (10 nós), 9 nós PMSI Integrador — a Fase 14 foi só Windows, este fleet nunca foi tocado (fica registado, não atacado — precisa de fluxo SSH e decisão de prioridade). Dos 123, **49 já tinham diagnóstico** neste handoff (reconciliados por bucket: 15 fechados/decididos, 5 bloqueados por recurso externo, 3 decisão de arquitectura pendente, 13 "Grupo D" atacados agora, 2 fixes por reconfirmar, 8 precisam consola do guest, 1 pausado); 74 são novos, não tratados em detalhe. **Grupo D (13 hosts) atacado**: `VS8000822` (bug `ServerActive`, mesmo padrão `VS9000003`) e `LTTI44_CLONE` (mesmo bug + `Hostname` apontava a um host Zabbix legado órfão, `LTTI44` — ver Z.26 novo) **corrigidos e confirmados frescos**; `VS8000824` (agente nunca instalado) **instalado de raiz** (base, sem MSSQL) — download directo da internet falhou nesta sub-rede, contornado com download local + cópia SMB, timeout transitório de active-check no 1º arranque resolvido com reinício — **confirmado fresco. Total: +3 VMs de Produção**. `VS8000127`/`VS8000142`/`VS9000268` (Acesso negado com as 2 credenciais, precisa 3ª via) ficam para a próxima sessão. Detalhe completo (comando a comando) em `vm-agente-recuperacao-handoff-20260704.md §7` |
| 14.16 | Inventário de serviços por VM — piloto (3 hosts) + achado de risco importante | ◐ | 2026-07-04 | Piloto `Get-Service` via WinRM em `VS8000822`/`VS8000824`/`LTTI44_CLONE`: maioria dos serviços "Running" é ruído genérico Windows/VMware/Defender; **2 das 3 VMs não têm nenhum serviço correspondente à sua função de negócio declarada no Zabbix** (`VS8000822` e `VS8000824`/Fircosoft-FINASTRA — sem processo nomeado a correr, só genérico + SCCM + MSDTC) — por investigar se a app corre por outro mecanismo ou se o nome Zabbix está desactualizado. Descoberto também que o template `Windows by Zabbix agent active` tem uma discovery nativa `service.discovery` (LLD de serviços Windows) — **desactivada** e com filtro estreito (só IIS/MSSQL/Oracle/MSDTC/WinRM/EventLog/Schedule/VSS). **NÃO activada** — o utilizador travou a proposta citando um incidente real anterior (fora desta sessão): activar um template MSSQL gerou uma enxurrada de triggers → emails → tickets abertos automaticamente no GLPI. Nova regra permanente registada no `CLAUDE.md` (secção de constraints): nunca activar discovery/template Zabbix sem primeiro verificar `action`/notificações ligadas aos triggers desse template. Continuação decidida: expandir o inventário via WinRM em lotes (não activar a discovery nativa por agora) |
| 14.17 | Cruzamento do relatório diário de negócio (Excel) com o Zabbix — amostra de 17 VMs + validação directa de EBA/Cezanne | ☑ | 2026-07-05 | Localizado `bpc-workspace/RELATORIO DIARIO DO ESTADO DOS SISTEMAS E SERVIÇOS COM PARCEIROS_05-03-2026.xlsx` (4 folhas; `MONITORAMENTO` tem 34 sistemas internos + 16 serviços de parceiros, cada um com `Owner`/`Estado`). Cruzado por nome contra a tag `servico` de todos os 650 hosts Zabbix Infra + os 39 monitores sintéticos `app-*`: **37/50 itens têm correspondência** (tag de VM e/ou `app-*`); **13 sem nenhuma** — `CTB/400`, `Audit Bank`, `Payment Manager`, `Bloomberg`, `STC`, **`Portal EMIS`** e **`BODIVA`** (estes 2 marcados **degradados no próprio relatório do dia** — zero visibilidade Zabbix exactamente onde já se sabe que há problema), mais `Equation`/`EBA`/`BFTELLER`/`Match Cash`/`UYSIG` (têm tag de VM mas sem `app-*`) e `SWIFT` (só cobertura parcial via `app-swift-swp`). **Ressalva de metodologia**: o cruzamento por tag `servico` é um cruzamento de **etiqueta**, não confirmação por inspecção — mesmo campo humano que já teve erros (`ambiente`/PMSI) nesta sessão. Piloto de validação real: amostra estratificada de 17 VMs (1 por departamento×serviço) com `Get-Service` via WinRM — **9/17 confirmados por nome de serviço** (ex.: `VS8000768`/ACM tem 11 serviços `ACM-*`; `VS8000390`/Exchange tem a suite `MSExchange*` completa; `VS9000007`/Domain Controller tem `NTDS`/`DNS`/`Kdc`), 7 plausíveis mas não confirmados (stack SQL/IIS genérico, sem nome de app), **1 sem correspondência nenhuma** (`VS8000406`/Reconciliador — nada além de Sophos/genérico). **Validação directa aprofundada** dos 2 sistemas em VMs Server 2003 (Z.23): `EBA` **confirmado** em `VS1800002` (serviços `BranchAutomation` nomeados a correr) — `SV9000401` parece ser o servidor de BD de apoio (mesma tag, sem serviço próprio nomeado); `CEZANNE` inicialmente sem correspondência por `Get-Service`, **confirmado depois via IIS** (`IIsWebVirtualDirSetting` em `VS9000235`: virtual dirs `CezanneCV`/`CezanneWeb/HRCharter` → `C:\Program Files\Cezanne Software\HRCharter Enterprise`) — corre como app IIS, sem serviço Windows dedicado. Detalhe completo em `vm-agente-recuperacao-handoff-20260704.md §8` |
| 14.18 | Diagnóstico dos 74 hosts "novos" (não catalogados nos handoffs) — bucket Windows (26) atacado | ◐ | 2026-07-05 | Reconstruída a lista dos 74 (123 nunca-reportaram menos os 49 já conhecidos, bug de `\r\n` no ficheiro intermédio corrigido). Bucketizados por template: **46 Linux** (`Linux by Zabbix agent active`, inclui 11 appliances EMC Networker vProxy — fica por atacar, precisa de fluxo SSH inexistente nesta sessão), **26 Windows** (18 padrão + 8 MSSQL), 2 outros. **Windows (26) testados** (ping/WMI/SMB/WinRM): **24/26 já têm WinRM aberto** (muito melhor que o Grupo D) — só `VS8000823` sem resposta nenhuma e `VS9000706` sem ping mas WMI/WinRM OK. Diagnóstico completo nos 24: **13 "Acesso negado"** com a credencial principal (`JumpServer`, `VD5000AVG0014`, `VS8000126/128`, `VS8000343`, `VS8000371`, `VS8000518`, `VS8000924/925`, `VS9000239/242/314/316`) — tentada a credencial alternativa, **1 desbloqueado** (`VS8000343`), 12 continuam bloqueados (mesma categoria de `VS8000828`/`VS9000106`, precisa 3ª via); `VS9000359` erro instável (WSMan/Acesso negado alternando). **10 com config perfeito (`Server`/`ServerActive`/`Hostname` correctos) mas serviço `Stopped`** — tentado arrancar, **todos os 10 falharam** com o mesmo erro: Evento 7009 do Windows, "timeout de 30s à espera que o serviço conecte". Investigado o log do agente em todos: **9/10 têm a mesma assinatura exacta** — `stopping service due to syscall signal` seguido de `Zabbix Agent 2 stopped` — espalhados por uma janela de **10-14 Junho 2026** (não é um evento único, é um padrão recorrente ao longo de ~5 dias, em VMs sem relação óbvia entre si). Alguns mostram erro de disco bloqueado (`vfs.fs.size[C:,...]: The process cannot access the file because it is being used by another process`) momentos antes de parar, mas não todos — correlação, não causa confirmada. **10º host (`VS8000800`) é diferente**: `Zabbix Agent` (v1, não v2) parou por `Zabbix Agent received stop request` (paragem limpa/pedida, não sinal) em 2026-05-24, mais de 2 semanas antes do padrão dos outros 9 — provavelmente não relacionado. **Nova causa-raiz a catalogar** (nem `ServerActive` nem "nunca instalado"): serviço parado por sinal do sistema numa janela concreta de dias, e agora falha a reiniciar por timeout do SCM — precisa de investigação mais funda (Event Viewer/Application log, não só o log do próprio Zabbix) antes de decidir o fix. Pendente: os 46 Linux, os 12 Windows ainda bloqueados, e a causa raiz do timeout de arranque nos 9 |
| 14.19 | Mapeamento completo Apps↔VMs↔Web Scenarios + arquitectura Grafana — nova linha de trabalho | ◐ | 2026-07-05 | **Deliverable**: `Mapa_VMs_Servicos_Zabbix_Grafana.xlsx` (139 linhas), com hierarquia de confiança (IP/URL > manual > serviço nomeado > plausível). **Cruzamento por IP** (URL do web scenario → IP → VM) revelou-se o método mais forte — achado maior: `VS8000305` hospeda **9 sistemas de negócio** (ABC/SGF/Contif/PSI/SIB/SGC/SPCC/Sala Reuniões/LIVE) atrás de uma tag que só diz "SIB". `app-euronet` confirmado sem registo em nenhum Zabbix (gap real). **Validação dos 39 web scenarios**: 3 problemas reais correntes (`app-emp` timeout, `app-sala-reunioes` HTTP 400, `app-sms-banking` connection refused) confirmados por 2 vias independentes (histórico Zabbix + teste directo); vários falsos alarmes descartados (erros antigos de 3-7h coexistindo com checks recentes OK — `web.test.error` do Zabbix guarda o último erro de sempre, não limpa ao recuperar). **Achado de compliance**: nenhum dos 39 hosts tem `{$RATE.LIMIT.SAFE}=1` (monitorização não formalmente aprovada pelos donos dos sites externos) — por isso testes directos desta sessão ficaram limitados aos internos. **Arquitectura Grafana**: pastas `07·APIs e Serviços`/`08·Serviços de Negócio` vazias; dezenas de dashboards antigos relevantes parados em `99·Arquivo` — 3 inspeccionados (`bpc-overview`=D6.1 funcional, `bpc_web_complete` morto por filtrar grupo Zabbix inexistente `BPC/LAYER/SYNTHETIC` vs o real `BPC / APLICACOES / SINTETICOS`, `d-web-01` funcional com drill-down `$group`/`$host`). Proposta acordada: 2 eixos paralelos (Eixo A só web scenarios N2/N3 revivendo os 2 dashboards funcionais; Eixo B serviços+web scenarios unificado N2/N3, novo, depende de decidir a ligação app↔VM). **Auditoria da tag `servico`** (166 valores, 650 hosts): 11 duplicados claros por maiúsculas/espaçamento + 1 família vProxy a consolidar (3 nomes, mesmas appliances) + 2 famílias ambíguas por decidir (Fircosoft, SWIFT — sub-componentes ou fragmentação?). **Nada escrito no Zabbix/Grafana ainda** — tudo pendente de confirmação. Handoff completo com todos os detalhes, pendências por ordem, e notas de acesso (incl. como ler o token Grafana do `tok3n`) em `bpc-workspace/mapeamento-apps-vms-webscenarios-handoff-20260705.md` |
| 14.20 | Cruzamento formal — 168 tags `servico` × 50 sistemas do relatório Excel + 2 fontes extra (web scenarios `app-*`, Mapa já confirmado) | ☑ | 2026-07-05 | Resolve o pendente §12/13.1 do handoff 14.19. Cruzadas **4 fontes** (não só as 2 pedidas) para boa cobertura: 50 sistemas do Excel, 168 tags `servico` ao vivo/650 hosts, 39 `app-*`, e as 139 linhas já confirmadas do `Mapa_VMs_Servicos_Zabbix_Grafana.xlsx`. **Achados novos**: (1) `app-sic` (SIC/Cobranças) resolve para `VS8000813`, cuja tag é `MICROCREDITO` — mismatch concreto por IP, não investigado ao vivo (precisa acesso nomeado); (2) `ebankit` (20 hosts, sites IIS `ebk-ib.bpc.intranet`/`ebk-mw-*`) é fortemente candidato a ser a plataforma por trás do BPCNET (Internet Banking) — subir de "Plausível" para quase-confirmado; (3) `TOBE` (12 hosts) é a tag técnica por trás do SOP (`app-sop`→`VS8000454`) — nome de produto/motor BPM, não do sistema de negócio, confirmar com o negócio; (4) `MATCH CASH`/`RECONCILIADOR`/`MatchWeb` — 3 tags para o mesmo conceito, mesmo padrão de fragmentação do Fircosoft, adicionar à lista de famílias ambíguas; (5) colisão de sigla `ABC` **confirmada por WinRM** (host nomeado pelo utilizador): `VS8000102` é servidor SQL Server 2016 + `IBM iSeries Access` (AS/400), sem site IIS/app web nenhum — nada a ver com o Sistema de Abertura de Contas (esse corre confirmado na `VS8000305`); excluir `VS8000102` de qualquer consolidação da tag `ABC`. **Confirma estrutural**: 6 dos "gaps" de tag (SIR/SAFT/SPCC/SGF/Salas Reuniões/LIVE) não são falta de infra — é outra vez a VS8000305, cuja tag só nomeia 1 dos seus 9 papéis (reforça que qualquer ligação app↔VM tem de ser N:M, nunca 1:1). Gaps reais confirmados (sem tag, sem `app-*`, sem VM): `STC`, `Audit Bank`, `Payment Manager`, `CTB/400`. 15/16 serviços de parceiros correctamente sem tag (são externos, por desenho). **Achado à parte**: 26 VMs com tag `SEM-ANOTACAO`/`SEM-CLASSIFICAÇÃO` — maior lacuna de classificação solta, fora do âmbito dos 50 sistemas. Nenhuma escrita feita. Relatório completo em `documentacao/auditoria-tag-servico-vs-relatorio-negocio.md` |
| 14.21 | Consolidação escrita dos 11 pares de duplicados da tag `servico` (36 hosts) | ☑ | 2026-07-05 | Antes de escrever: confirmado por WinRM que `VS8000102` (tag `ABC`) **não é** o Sistema de Abertura de Contas do Excel — é servidor SQL Server 2016 + `IBM iSeries Access` (AS/400), sem site web nenhum, único host com departamento `DGE` — **excluído de qualquer consolidação**. Confirmado por `host.get` que `VS8000932`/`VS8000115` (tag `Zabbix`/`ZABBIX`) são os 2 servidores Zabbix reais (Network `10.10.233.140` e Infra `10.10.126.22`), não infra crítica de terceiros — seguro consolidar (tag só descritiva). **Escrita executada** (`host.update`, 36 hosts, só o valor da tag `servico`, nada mais tocado) após dry-run e confirmação explícita do utilizador: `APLICACOES INTERNAS`/`APLICAÇÕES_INTERNAS`/`aplicacoes internas`→`APLICAÇÕES INTERNAS` (4 hosts, inclui a `VS8000305` dos 9-apps — só grafia, não resolve o problema de fundo); `BANKTRADE`→`BANK TRADE` (2); `CREDITQUEST`→`CreditQuest` (2); `DIGIWAVE`→`Digiwave` (10); `ELASTIC SEARCH`/`ElasticSearch`→`Elasticsearch` (11, nome oficial do vendor, nenhuma das 2 grafias antigas batia exactamente); `GIT LAB`/`Gitlab`→`GitLab` (2); `GRAYLOG`→`Graylog` (1); `JUMP_SERVER`→`JUMP SERVER` (1); `Sigcap`→`SIGCAP` (1); `SUPPORTSERVER`→`SUPPORT SERVER` (1); `ZABBIX`→`Zabbix` (1). **36/36 confirmadas, 0 falhas**, verificado ao vivo pós-escrita (nenhuma grafia antiga sobrou). Total de valores distintos da tag `servico`: 168→156 |
| 14.22 | Consolidação escrita da família `vProxy` (8 hosts) | ☑ | 2026-07-05 | Investigados os 12 hosts antes de escrever: todos com o mesmo padrão de nome (`VSxxxxx_vProxy-EMC Networker`/`NetWorker`), mesma `camada=Virtualização`, mesmo `departamento=DTI` — confirmado sem ambiguidade que são a mesma appliance de backup Dell/EMC Networker vProxy, só espalhada por vários clusters/hardware vCenter (não precisou de WinRM, o nome do host já confirmava). Canonical escolhido: `vProxy` (nome curto e correcto do produto — `This is Backup and Recovery vProxy Appliance`, usado em 7 hosts, era a frase de descrição de fábrica da appliance, nunca limpa). Confirmado explicitamente pelo utilizador antes da escrita. **Escrita executada** (`host.update`, 8 hosts: 7 de `This is Backup and Recovery vProxy Appliance` + 1 typo `vProxyr`→`vProxy`) — **8/8 OK, 0 falhas**, verificado ao vivo. Total de valores distintos da tag `servico`: 156→154 |
| 14.23 | Investigação e correcção do mismatch `VS8000813` (tag `MICROCREDITO` vs web scenario `app-sic`) | ☑ | 2026-07-05 | Host nomeado explicitamente pelo utilizador — WinRM autorizado. Inspeccionado `VS8000813` (10.10.236.161): 2 sites IIS (`ACS1MFA` porta 8081, `ACS6MFA` porta 8080), sem nenhum serviço Windows nomeado a bater com "Microcrédito". Confirmado via `Get-WebBinding`+`Get-NetTCPConnection` que a porta 8081 (exactamente o alvo do web scenario `app-sic`) é servida pelo site `ACS1MFA`. **Teste HTTP directo ao conteúdo real** (`Invoke-WebRequest`, interno, permitido pela regra de compliance §9) revelou o `<title>`: **"Sistema Integral de Cobranças (SIC)"**, com ícone `BPC_Icon.png` — confirma inequivocamente que a VM corre o SIC, não Microcrédito; "ACS"/"MFA" são só nomenclatura interna do módulo de autenticação da própria aplicação SIC, não outro sistema. **Tag corrigida** (`host.update`, confirmação explícita do utilizador antes da escrita): `servico` `MICROCREDITO`→`SIC`, verificado ao vivo. O `app-sic` original estava correcto desde o início — era só a tag da VM que estava errada/obsoleta |
| 14.24 | Confirmação técnica de `TOBE`↔SOP e `ebankit`↔BPCNET (hosts nomeados pelo utilizador) | ☑ | 2026-07-05 | `VS8000454` (tag `TOBE`): sites IIS `POv2`/`PO_MW`/`PO_MW_External` ("PO" = Portal de Operações) + porta 91 devolve HTTP 401, exactamente a assinatura já documentada do `app-sop` real (único dos 39 web scenarios com `{$HTTP.CODE.OK}` a aceitar 401, por exigir login) — **2 sinais técnicos independentes, confirmado sem precisar do negócio**. `VS8000724` (tag `ebankit`): site IIS `ebk-ib.bpc.intranet` tem binding literal `443:ib.bpc.ao` — o mesmo domínio de produção do `app-internet-bank` real; mesma VM serve também `acesso.bpc.ao`/`gatewaycanais.bpc.ao`/`cdn.bpc.ao`/`conteudo.bpc.ao` (ecossistema eBankIT completo) — **confirmado por binding de domínio, não por semelhança de nome**. Tentativa de confirmar por conteúdo HTTPS bloqueada pelo classificador de permissões (desactivar validação TLS não estava autorizado) — não foi necessário, a binding já é prova suficiente. Nenhuma escrita feita (as 2 tags já estavam correctas) |
| 14.25 | Investigação Fircosoft/SWIFT — decisão de não fundir | ☑ | 2026-07-05 | **Fircosoft** (12 hosts, não só os 4 conhecidos antes): `FIRCOSOFT` genérica (6, QA) vs `FircosoftMQ`(2)/`FircosoftUtilities`(2, já achado antes como "genuinamente vazia")/`FircosoftApp`(2), todos Produção — confirmado que MQ/Utilities/App são camadas arquitecturais reais de deployment n-tier (típico de software de compliance/AML), não fragmentação; QA só nunca replicou a granularidade. **Decisão: não fundir.** **SWIFT** (20 hosts): 18 já usam tag genérica `SWIFT` com o sub-papel (SAA/SWP/Jump Server/Token Server/FPM/Histórico) só no nome visível, incluindo os pares SAA/SWP de Produção — só 2 hosts QA (`Swift SAA`/`Swift SWP`) quebram esse padrão dominante. Recomendação técnica seria fundir (sem perda de informação, alinha com o padrão), mas **decisão do utilizador: não mexer agora**. Nenhuma escrita feita em nenhuma das 2 famílias |
| 14.26 | Investigação MATCH CASH/RECONCILIADOR/MatchWeb (5 hosts nomeados pelo utilizador) | ☑ | 2026-07-05 | **MATCH CASH (3: VS8000401/416/417, dept DOP) + RECONCILIADOR (1: VS8000406, dept DCO)** — WinRM confirmou `Match Cash-2` instalado nos **4 hosts**, cada um cobrindo um âmbito de conta diferente (internas, STC/EMIS, correspondentes, conta interna) — é literalmente o mesmo software, `RECONCILIADOR` é só tag inconsistente. **Decisão do utilizador: não fundir agora**, apesar da evidência clara. **`MatchWeb` (VS8000914, dept DTI)** — achado inesperado: o IP registado (`10.10.203.104`) resolve na verdade para `VS8000867` (tag `SACC`); confirmado por `{$VMWARE.VM.UUID}` idêntico (`5023ddde-6a85-ae81-b19c-98f41af6b618`) que é o **mesmo padrão de duplicado já visto** (`VS8000526`/`VS8000888`) — `VS8000914` já está desactivado (`status=1`) no Zabbix, tag "MatchWeb" órfã, sem ligação real a Match Cash. **Decisão: só documentar, nenhuma acção** (já desactivado, não afecta monitorização). Nenhuma escrita feita |
| 14.27 | Mapeamento VM↔host sintético (`app-*`) — tag `servico` alinhada em 21 hosts | ☑ | 2026-07-05 | Confirmado que só 2 dos 39 `app-*` tinham tag `servico` própria (`app-internet-bank`=`ebankit`, `app-sms-banking`=`canais digitais`); os restantes 37 não tinham tag nenhuma. Construído mapeamento URL→IP/hostname→VM para os 21 `app-*` com VM já identificada. **Achado durante a investigação**: `VS8000789` (alvo do `app-sacc`) tinha tag `equation`, mas o próprio nome visível do host já diz `"APP - Compute - VS8000789 (SACC)"` — corrigido para `SACC`. `canais digitais` (tag do `app-sms-banking`) confirmada como categoria intencional partilhada por mais 2 hosts (`SV9000307`="SMS Banking", `VS8000740`="Whatsapp BPC") — não é erro, mas decisão do utilizador foi alinhar com a tag específica da VM (`SMS BANKING+CHAT BOT`) mesmo assim. Para os 8 apps da `VS8000305` + `app-sir`/`app-saft` da `VS8000304` (10 apps partilhando VMs com tag genérica): decidido dar a cada um tag **própria e específica** (ex.: `app-abc`="Abertura de Contas" — nome espeçado deliberadamente para não colidir com a tag `ABC`, já confirmada errada, em `VS8000102`), em vez de copiar a tag genérica da VM (que não os distinguiria). **21 escritas confirmadas explicitamente (por partes, incl. reforço do classificador de permissões que travou 8 escritas sem confirmação directa individual — corrigido pedindo confirmação extra)**: `VS8000789`(equation→SACC), `app-sms-banking`(canais digitais→SMS BANKING+CHAT BOT), `app-swift-swp`=SWIFT, `app-intix`=INTIX, `app-sicv`="sistema de compensacao de cheques", `app-banktrade`="BANK TRADE", `app-sic`=SIC, `app-cezanne`=CEZANNE, `app-forgest`=FORGEST, `app-sop`=TOBE, `app-sacc`=SACC, `app-abc`="Abertura de Contas", `app-sgf`=SGF, `app-contif`=CONTIF, `app-psi`=PSI, `app-sib`=SIB, `app-sgc`=SGC, `app-spcc`=SPCC, `app-sala-reunioes`="Agendamento de Salas", `app-sir`=SIR, `app-saft`=SAFT. **21/21 OK, 0 falhas**, verificado ao vivo (1ª tentativa falhou por bug do script — campo `automatic` incluído por engano no payload, corrigido antes de qualquer escrita real acontecer). Os 18 `app-*` restantes (externos por desenho, ou VM não resolvida: `app-live`/bpc01, `app-euronet`/VM não registada, `app-sap`, `app-emp`, `app-mundial-seguro`, `app-pumangol`, `app-bpcao`, `app-bpcnet-pub`, todos os `app-bna-*`/`app-minfin`/`app-inss*`) ficam sem tag, correcto |
| 14.28 | **Registada pendência formal**: auditoria completa das 292 VMs de Produção | ☐ | 2026-07-05 | Cruzamento ao vivo mostrou que só **33 das 292 VMs de Produção (11%)** têm a tag `servico` confirmada com prova técnica real; as restantes **259 (89%)** têm o valor herdado, nunca verificado — podem estar certas ou ter o mesmo tipo de erro já confirmado em `VS8000813`(SIC/MICROCREDITO) e `VS8000789`(SACC/equation). 100 valores de tag distintos em Produção nunca tocados, os maiores por volume: `OCP`(18), `INTEGRADOR`(16), `SWIFT`(12 em produção), `vProxy`(12), `JUMP SERVER`(11), `SIGCAP`(11), `Elasticsearch`(11). **Pedido explícito do utilizador de deixar isto registado como pendência formal**, para retomar depois — fora do âmbito imediato desta linha de trabalho (que se limitou aos 50 sistemas do relatório de negócio + famílias ambíguas apontadas) |
| 14.29 | Verificação da ligação VM↔app-* + correcção `LTTI44_CLONE` | ☑ | 2026-07-05 | Verificação sistemática pós-14.27: de 21 `app-*` tagueados, **10 ligam limpo a VM(s)** (SWIFT, INTIX, ebankit, SICV, BANK TRADE, SMS Banking, SIC, CEZANNE, FORGEST, TOBE/SOP); **4 ligam a VMs adicionais não esperadas** (SACC→4 VMs incl. `VS8000867`; CONTIF→`VS9000359`; SGC→`VS8000475`/`476`/`VS9000309`/`vs8000474` — possível infra dedicada legítima, não confirmada a fundo); **7 não ligam a nenhuma VM** (ABC/SGF/SIB/SPCC/SAFT/SIR/Salas — por desenho, a VM partilhada manteve tag genérica). **Achado durante a verificação**: `PSI`→`LTTI44_CLONE` revelou-se **má classificação real**, não infra legítima — WinRM (host nomeado) confirmou que `LTTI44_CLONE` é uma **estação de trabalho de developer** (`$env:COMPUTERNAME`="LTTI44", software: Visual Studio Enterprise, drivers NVIDIA GeForce/ShadowPlay, Skype, BitComet, GOM Player, WinRAR, Unity, touchpad Synaptics, diagnóstico HP — nenhum vestígio de PSI/Salário/Payroll). **Corrigido** (`host.update`, confirmação explícita): `servico` `PSI`→`Posto de Trabalho` (valor novo, sem convenção prévia para postos de trabalho neste catálogo). Verificado ao vivo: `servico=PSI` agora só devolve `app-psi`, sem ruído |

---

## Acções Zabbix (lado do utilizador — não-bloqueantes)

| # | Prioridade | Acção | Estado | Nota |
|---|---|---|---|---|
| Z.1 | P1 | Esquema de tags canónico (`dominio`/`ambiente`/`criticidade`) | ☐ | facilita filtros |
| Z.2 | P2 | Corrigir espaço duplo no grupo 602 | ☑ | **Corrigido 2026-07-04**: `"BPC / INFRAESTRUTURA  / STORAGE"` → `"BPC / INFRAESTRUTURA / STORAGE"` (`hostgroup.update`) |
| Z.3 | P2 | Uniformizar separador de naming dos grupos | ☐ | `" / "` vs `"/"` |
| Z.4 | P2 | Fundir `412 Git lab` + `416 Gitlab` | ☐ | duplicado |
| Z.5 | P3 | Classificar 25 hosts em `A-CLASSIFICAR` (480) | ◐ | **Investigado 2026-07-04** — não são hosts órfãos, estão todos activos e monitorizados, só sem classificação de negócio. **Achado maior**: consultado o hostname real (guest identity) via vCenter PRD para os "sem pista" — revelou que **7 dos 25 são o mesmo cluster OpenShift/OKD dev**: `master1`(`VS8000979`), `worker1`(`VS8000982`), `worker2`(`VS8000983`), `worker3`(`VS8000984`) + `okd_ha_proxy`/`rhcoreos_bootstrap`/`rhocp-helper` (infra de suporte) — **visible name dos 4 numéricos corrigido** para reflectir o hostname real (mantendo o `host` técnico `VS80009XX`). Mais 3 Veeam (backup), 1 proxy Nginx, 1 DHCP piloto (`Teste Kea DHCP` — possível relação com falha de DHCP de `VS9000711`, Z.19, por confirmar), 1 nó de gestão PowerFlex. **Sem classificação possível por agora**: `VS8000771` (Ubuntu genérico) + `VS8000900/901/902` (Windows Server 2019, 3 iguais, sem nome próprio) — sem pasta do vCenter disponível via API REST (precisaria de SOAP) nem outra pista; 8 hosts do vCenter PowerFlex continuam bloqueados pela credencial errada (Z.8). Falta: mover todos os classificados para os grupos/domínio definitivos (Fase 1/7/8 conforme aplicável) |
| Z.6 | P3 | Rever `Novos_Inventario` (632, 21 hosts) | ◐ | **Investigado 2026-07-04 — não é staging.** É hardware físico real, auto-importado (`source: inventario_auto`) e nunca classificado nos grupos definitivos: 15 servidores Dell PowerFlex R650 (`10.10.232.1-20`, mesma sub-rede do vCenter PowerFlex de Z.8/`10.10.232.84` — provavelmente os nós físicos por baixo desse cluster), 1 storage Dell EMC Unity 550F, 2 tape libraries (IBM TS3200 + Dell EMC ML3, estas 2 já activas), 2 fabric Cisco UCS 6248UP (1 activo). A maioria (18/21) está desactivada. **Falta**: decidir os grupos definitivos (Fase 1 Infraestrutura VMware para os PowerFlex, Fase 2 Armazenamento para o Unity, Rede/Fabric para os UCS) e mover os hosts; decidir se activa monitorização nos 18 desactivados |
| Z.7 | P2 | Esclarecer `network` vs `network2` (2 tokens) | ☑ | **Esclarecido 2026-07-04**: são 2 tokens diferentes, mas para o **mesmo servidor** (`10.10.233.140`, v7.0.16) — testados via `hostgroup.get`/`user.get`, resposta idêntica nos dois, ambos Super Admin (conta `Admin`). `token.get` no próprio servidor confirma só 3 tokens registados no total (`Internal`/`testes`/`grafana`, todos da conta `Admin`) — os rótulos "network"/"network2" no `tok3n` são só como alguém guardou 2 destes 3, não nomes oficiais nem 2 ambientes distintos. Sem risco, sem acção necessária além desta nota |
| Z.8 | P1 | VMware poller não recolhe dados de `sv9000650–655` (Powerflex) e `cls9000650–652` (Cluster Gestão) — **âmbito real: 270 VMs do grupo 609 com `lastclock=0`** (nunca recolhidas) | ☐ | Inicialmente identificado nos ESXi Powerflex; auditoria 2026-06-17 revelou que 270/451 VMs do grupo 609 têm `vmware.vm.powerstate` com `lastclock=0` (valor padrão=0 ≠ desligadas). O poller VMware nunca atingiu estes hosts. **Causa confirmada 2026-07-04** (não só "verificar credenciais" — testado login real via REST API `/rest/com/vmware/cis/session`, sem imprimir passwords): há **3 vCenters distintos** no grupo 664 (`sv9000204`/`vcenter-backup`/`sv9000206`+`vcenter-02` — este último é o mesmo endpoint em 2 hosts Zabbix). Login **OK** em `https://10.10.101.9/sdk` (VCenter PRD) e `https://10.10.101.30/sdk` (vCenter Backup Cluster Swift); login **FALHOU (401)** em `https://10.10.232.84/sdk` (vCenter PowerFlex/"vCenter 02", user `agonzaga@vsphere.local`) — **confirmado que é mesmo credencial errada guardada no Zabbix**, não falha de rede (o servidor respondeu). Tentado também o utilizador `administrator@vsphere.local` (o mesmo do PRD) contra este vCenter — **falhou (401) também**, não é conta partilhada entre os vCenters. Precisa de credencial nova e válida especificamente para este vCenter (não continuar a tentar adivinhar). Isto explica a fatia de VMs no PowerFlex/Cluster Gestão sem powerstate. Até resolução: dashboards mostram `?` no powerstate em vez de `OFF` para dados sem `lastclock` **Nova manifestação encontrada 2026-07-06 (validação pré-demo, Fase 1.24)**: o mesmo falhanço de credencial faz os 6 ESXi PowerFlex (`sv9000650-655`) mostrarem **falsos positivos de "datastore crítico"** no N3 ESXi — todos os items `Free space on datastore [...]` destes hosts têm `state=1`, `error="Cannot complete login due to an incorrect user name or password"`, `lastclock=0`; o `lastvalue` fica em `0`, que o `l2-tabela.js` (threshold `crit: <=10% livre`) lê como "0% livre" → dispara "Datastore free space is critically low" (severidade High) em ~20 datastores por host, 120 dos 137 problemas activos do grupo 603. **Confirmado real e distinto**: os 9 ESXi PRD (`sv9000640-649`) têm dados genuínos (`state=0`, timestamps frescos, 16-25% livre nos `DATA_UNT_400_8T-*`) — esses WARN são reais, não bug. Não apresentar o "20/20 CRIT" do N3 ESXi ao cliente sem este contexto — a leitura correcta é "6 ESXi sem dados de datastore por falha de credencial (Z.8), não storage a esgotar-se" |
| Z.11 | P2 | Tag `ambiente` inconsistente no grupo 609 | ◐ | **Fase 1 (13 hosts)**: corrigido `"producao"`→`"Producao"`. **Fase 2, reaberta e ampliada (2026-07-04)**: descoberto que o problema é maior — **7 grafias distintas coexistiam** (`Produção` 286, `QA` 110, `A-CLASSIFICAR` 26, `Producao` 13, `Operações` 8, `Desenvolvimento` 2, `Teste` 1). Decisão do utilizador: canonizar **todas** para minúsculas sem acentos (`producao`/`qa`/`operacoes`/`desenvolvimento`/`teste`), **validado por cruzamento com o vCenter** (nome do portgroup de rede como sinal independente) antes de escrever — não confiar só na tag manual, já provada ter erros de origem (ver nota do script abaixo). Causa-raiz identificada: a tag vem de um fluxo **manual** (`2-extrair_inventario_transicao.py` gera Excel com colunas vazias para preenchimento humano → `1-zabbix_sync.py` aplica o CSV preenchido tal e qual ao Zabbix) — os erros são de classificação humana espalhada, não bug de código. **Excluídas 25 VMs do cluster OpenShift/IBM Cloud Pak** (`ibm-cp-master/infra/storage/worker-01..09`, `VS8000979/982/983/984`, `okd_ha_proxy`, `rhcoreos_bootstrap`, `rhocp-helper`) — confirmado ao vivo que está em construção/reconfiguração activa (uma VM mudou de portgroup durante a própria sessão de auditoria, sem qualquer acção nossa) — fica de fora até essa fase estabilizar. Das restantes 173 VMs acessíveis (vCenter PRD/Backup): **35 confirmadas e já normalizadas** (4 `Produção`→`producao`, 31 `QA`→`qa`); **14 discordâncias encontradas** entre tag Zabbix e portgroup — padrão sistemático do departamento **PMSI** (11× tag diz QA mas rede diz `DC-PRD-PMSI`; 3× o inverso) — por decidir com o utilizador (pode ser erro de classificação em lote, ou convenção de nomes de rede do PMSI que não segue a norma global); 8 inconclusivos (nome do portgroup não indica ambiente). **265 VMs continuam bloqueadas** (vCenter PowerFlex, credencial errada, Z.8). **Validação adicional (2026-07-04)**: comparado o ambiente escrito no visible name (`name`) contra a tag `ambiente` actual nas 446 VMs — **442/446 concordam**, 4 sem ambiente reconhecível no nome são as 4 do cluster OKD excluído (`A-CLASSIFICAR`, correcto). Nenhuma discordância semântica encontrada — os 35 já corrigidos ficaram consistentes em ambos os campos |
| Z.9 | P1 | SNMP não recolhe dos IBM FS9500 (11750) e FS9200 (11747) | ☐ | triggers `No SNMP data collection` activos em ambos — verificar community string SNMP e acessibilidade de rede |
| Z.10 | P1 | Script `unity_get_state.py` da Dell EMC Unity (11834) sem dados há >1h | ☐ | trigger `No data from storage for 1 hours` activo + `Exist unsupported items` — verificar credenciais API Unity e acessibilidade do host |
| Z.12 | P2 | Ruído de alertas Rede: 220× "Link down" (P3) em portas de acesso desligadas (g29) + ~204 "Ethernet changed to lower speed" (P1) | ☐ | Auditado 2026-06-18 (`rede-topologia.md` §9). Inflaciona KPI "Alertas activos" do N2. Baixar severidade/suprimir em portas de acesso down; rever lower-speed/half-duplex |
| Z.13 | P2 | IP SLA 65 (ITA) no DC1-RTE-WAN-INT está NOT OK (sense=4) desde 2025-11-21, ~7 meses ininterruptos | ☐ | **Diagnóstico revisto (2026-07-01, rebuild N4 Borda DC):** history.get confirma `completion time`=0 em 43.836 pontos consecutivos (últimos 31 dias, sem uma única resposta) + trigger é 1 evento único nunca resolvido desde 2025-11-21 (sem flap) + Po2.65 tem tráfego real ~23 Mbps no momento da checagem. Assinatura de **sonda IP SLA com alvo morto/inatingível configurado no router**, não de indisponibilidade real do circuito Internet (se fosse o circuito, não haveria tráfego a passar). Rebaixado de P1 para P2 — não é incidente de serviço activo. Encaminhar à equipa de redes para validar/corrigir o destino da sonda 65 no IOS Cisco (ou desactivá-la). Painel `n4-stat-sla65.json` mantido no N4 Borda DC mas anotado com esta nota — não accionar como outage sem confirmar primeiro |
| Z.14 | P1 | **~73 agency routers sem dados SNMP de interface** — respondem a ICMP mas sem interfaces/CPU/memória úteis (ponto cego no troubleshoot WAN) | ☐ | **Âmbito corrigido (auditoria 2026-06-28):** dos 221 routers no g24, **13 sem qualquer item** de interface (só ICMP) + **60 com items mas `lastclock=0`** (nunca recolhidos) = **~73 (33%)**. **Causa-raiz corrigida: NÃO é falta de template.** Os partidos (RTSICA00, RTAIAAN, RTKIKOLO02) já têm o template `Cisco IOS by SNMP` **e** interface SNMPv3 (authPriv SHA/AES, user `snmpv3_noc_bpc`) — **a sessão SNMP é que não fecha** (creds/ACL/`snmp-server user` no IOS, ou SNMP desligado). 13 → LLD nunca descobriu; 60 → LLD criou items mas polling falha. **Acção:** `snmpwalk -v3` de teste do Zabbix server/proxy → `ifTable` (`1.3.6.1.2.1.2.2`); se falhar é device-side. Renomear os 5 que ainda são `172.22.1.x`. Endurecer: auth e priv usam a **mesma** passphrase. N4 mostra "Sem SNMP" (dashboard correcto; dado em falta) |
| Z.15 | P2 | **Monitorização per-spoke no hub DMVPN** (`DC1-RTE-WAN-AG`) — sessão IPsec/NHRP por agência | ☐ | **Confirmado (2026-06-28):** o hub só tem 1 item agregado `Active IPsec VPN tunnels` (contagem) + estado **por provider** (`Tu101…Tu107`); **zero** items per-spoke. **Caminho:** LLD sobre `CISCO-IPSEC-FLOW-MONITOR-MIB` (`cipSecTunnelTable`) no hub → 1 entrada por **peer** (IP público do spoke) com estado/contadores da SA IPsec → mapear peer IP↔agência (inventory/tag). Pesado (200+ spokes → afinar `max_repetitions`/intervalo) mas é o caminho oficial por SNMP. Dá visibilidade do túnel de cada agência **mesmo com o router dela down** (causa-raiz de agências sem sistema). Habilita 9.6 completo |
| Z.16 | P1 | **Trigger corrompida em `DC1-RTE-GTW01`** (triggerid 49355) — nome/expressão apontam para host e interface erradas, mascarando um problema real (`Po1` genuinamente down) | ☑ | **Encontrado (2026-07-01)**, ver histórico original abaixo. **Reverificado 2026-07-04 (Fase C da auditoria transversal): já não reproduz.** A trigger aponta agora correctamente para o item do próprio host (`functionid 115371/72/73` → `itemid 105053` = "Interface Po1(): Operational status", `hostid 10840` = `DC1-RTE-GTW01`) — confirmado `lastvalue=2` (down) e trigger com `value=1` (problema activo), a funcionar como esperado. Ou foi corrigido entretanto por outra via, ou a leitura original de 07-01 tinha um erro de interpretação. **Histórico original**: `problem.get` devolvia alerta aberto há 207 dias com nome "Interface Tu20(...)"; nome era snapshot congelado da trigger já renomeada para "Po1()"; suspeitou-se de reassociação de itemid via LLD apontando a `DC1-LEAF-105` |
| Z.17 | P1 | Script `Install-ZabbixAgent2-MSSQL.ps1` gravava `mssql.conf` sempre com BOM UTF-8 | ☑ | **Corrigido 2026-07-04** (Fase 14.3): `Set-Content -Path ... -Encoding UTF8` no PowerShell 5.1 grava sempre BOM; o parser do Zabbix Agent 2 não reconhece a 1ª linha como comentário e falha a ler toda a configuração (agente nunca arranca, SCM reporta timeout 30s/event 7009, não erro de config). Corrigido para `[System.IO.File]::WriteAllText(..., New-Object System.Text.UTF8Encoding($false))`. Script vive em `C:\Repositorios\zabbix\scripts-a-analisar\de-scripts-import\`. Se este script foi usado no passado noutras VMs, o `mssql.conf` delas está potencialmente corrompido do mesmo modo — candidato a explicar parte do balde de 25 hosts MSSQL sem dados |
| Z.18 | P2 | Plugin nativo **NVIDIA GPU** do Zabbix Agent 2 (Windows) falha de forma fatal em VMs sem GPU | ☐ | Bug conhecido do Zabbix, [ZBX-25821](https://support.zabbix.com/browse/ZBX-25821), marcado **"Won't fix"**. O plugin vem embutido no `zabbix_agent2.exe` (não há `nvidia.conf` a comentar como no Linux) e tenta sempre carregar `nvml.dll`; falha é fatal só no modo de teste `-t` — **o serviço persistente continua a correr e a reportar métricas base normalmente** (confirmado em 7 VMs instaladas em 2026-07-04). Sem acção necessária para a maioria dos casos; só relevante se se quiser mesmo monitorizar métricas MSSQL/NVIDIA nalguns hosts onde o registo do plugin nesse caminho específico bloqueia (ver Z.17 para o caso MSSQL) |
| Z.19 | P2 | 13 hosts do grupo 609 com interface Zabbix a apontar para IP inválido (IPv6 link-local `fe80::`/APIPA `169.254.x`) | ◐ | **3/13 resolvidos (2026-07-04)** com acesso ao vCenter PRD. `VS6000001`, `VS6000005`: placa de rede virtual desligada (`NOT_CONNECTED`) — histórico de eventos (cliente SOAP `QueryEvents`) confirmou que nunca foi desligada deliberadamente; reconectada + tornada persistente (`start_connected=true`) + IP corrigido no Zabbix, `icmpping` fresco confirmado (nota: ambas são **QA**, não Produção — portgroups `DC-QA-APP-INT`/`DC-QA-DB`). `VS8000526`: **não era conflito de IP real** — tinha um IP válido (`10.10.237.56/26`, estático) que o campo "IP primário" ignorava a favor do `fe80::`; esse IP já estava registado no Zabbix noutro host (`VS8000888`) mas confirmado por **VM.UUID idêntico** que é o mesmo objecto VM registado 2x (resíduo de rename) — `VS8000888` desactivado, interface de `VS8000526` corrigida, ping confirmado (também **QA**). **2 pendentes com causa já isolada, precisam de acesso à consola do guest**: `Graylog` (Ubuntu, só `fe80::`, nenhum IPv4 detectado) e `VS9000711` (Windows, DHCP activado mas só obtém APIPA — **descartada causa de rede/VLAN**: 2 VMs vizinhas no mesmo portgroup têm IP normal, logo o problema é dentro do próprio guest OS). **1 pendente, possivelmente não é bug**: `VS8000394` (hostname `edge.bpc.ao`, IP estático `192.168.1.71/24` isolado, DNS real — parece dispositivo edge/DMZ deliberado, precisa de decisão do utilizador antes de mexer). **7 bloqueados** (`VS8000220/830/833/834/835/836`, `VS9000922`) — vCenter PowerFlex/"vCenter 02" (`10.10.232.84`), sem credencial válida (Z.8); tentado também `administrator@vsphere.local` do PRD — falhou (401) também |
| Z.20 | P2 | 5 VMs de infra-estrutura **SWIFT** sem acesso WinRM com a credencial de admin partilhada | ☐ | `VS8000125`, `VS8000140`, `VS8000141`, `VS8000314`, `VS8000324` (tag `servico=SWIFT`, `departamento=DTI`). Provavelmente segregação de acesso **intencional** por política (SWIFT CSP), não bug de configuração — **decisão do utilizador: não insistir com mais credenciais aqui**, escalar via canal próprio SWIFT/segurança se se quiser monitorizar |
| Z.21 | P2 | 4 VMs sem resposta de rede nenhuma (nem ping nem SMB) + 1 caso adicional de isolamento de sub-rede completo | ⏸ | `VS8000491` (QA, powerstate=1 confirmado ligado), `VS8000528` (Produção, JUMP SERVER, powerstate=1 confirmado ligado) — isolamento de rede genuíno, escalar à equipa de rede. `VS8000826`, `VD5000AJ012` — nem o powerstate confirma (depende da resolução de Z.8/vCenter 02). **Caso adicional confirmado 2026-07-04**: `VS8000904` (agente instalado e a correr, `Hostname` já corrigido) não alcança o servidor Zabbix em **nenhuma porta** (testadas 80/3000/10050/10051) — não é bloqueio pontual da porta trapper, é isolamento completo entre as sub-redes `10.10.204.x` e `10.10.126.x`; descartada causa local (firewall da VM 100% permissivo em saída). **Bloqueado — precisa da equipa de rede** (rota/VLAN/ACL entre segmentos) |
| Z.22 | P3 | Macro `{$VMWARE.PASSWORD}` gravado como texto normal (não "Secret") em 3 dos 4 hosts vCenter (grupo 664) | ☑ | Confirmado 2026-07-04 ao mapear os 3 vCenters (Z.8). **Corrigido no mesmo dia**: `usermacro.update` (`type=1`) em `sv9000204`, `vcenter-02` e `vcenter-backup` — valor da password mantido, API deixa de expor o valor (confirmado por `usermacro.get` sem campo `value`, igual a `sv9000206`). Recolha VMware confirmada a continuar normal depois da mudança (`powerstate` fresco em `VS6000001`/`VS6000005`) |
| Z.23 | P3 | 3 VMs em **Windows Server 2003** (EOL desde 2015) — `VS9000235`, `VS1800002`, `SV9000401` | ☐ | Respondem por WMI mas o Zabbix Agent 2 (binário Go) muito provavelmente não corre neste SO (requisito mínimo Windows 7/Server 2008+). Precisaria do Zabbix Agent 1 legado (C) para monitorizar. Decisão de arquitectura pendente: vale a pena, ou aceitar sem monitorização? **Actualização 2026-07-05: confirmado por inspecção directa (WMI `Win32_Service`/`IIsWebVirtualDirSetting`, não por etiqueta) que estas 3 VMs correm sistemas de negócio activos do relatório diário** — `VS1800002` tem os serviços `EBA MsgServer`/`EBA Remote Referral Server`/`EBA SQL Status Monitor` (`C:\Program Files\BranchAutomation\...`) a correr; `SV9000401` é provavelmente o servidor de BD de apoio (SQL Server 2005 completo, mesma tag `EBA`, sem serviço nomeado próprio); `VS9000235` tem o **Cezanne HR (HRCharter Enterprise)** confirmado via virtual directories do IIS (`CezanneCV`, `CezanneWeb/HRCharter` → `C:\Program Files\Cezanne Software\HRCharter Enterprise`) e pasta `C:\Program Files\Cezanne` no disco — corre como app IIS, sem serviço Windows dedicado, por isso não aparecia no `Get-Service`. **Conclusão: isto não é EOL de sistema secundário — são 2 sistemas de negócio (EBA "Caixa das Agências", Cezanne RH) genuinamente activos, sem nenhuma visibilidade Zabbix além de ping.** Sobe a prioridade da decisão de arquitectura |
| Z.24 | P3 | 2 VMs Server 2008/2008R2 com WinRM já configurado mas regra de firewall bloqueada por "Access is denied" | ✖ | `VS9000424`, `VS9000343`. Serviço WinRM já a correr, listener já configurado, mas `winrm quickconfig` falha a criar a regra de firewall mesmo com conta de admin via WMI — confirmado UAC Remote Restriction (token filtrado em ligações de rede para contas que não são o Administrador local RID-500, comum nestes SOs antigos). Fix identificado (`LocalAccountTokenFilterPolicy=1` no registo) — **descartado por decisão do utilizador (2026-07-04): não vale a pena enfraquecer o UAC remoto só para destravar monitorização em 2 VMs antigas.** Ficam de fora até alguém configurar o WinRM localmente (consola/RDP) |
| Z.25 | P1 | **Auditoria de duplicados por `VM.UUID`** no grupo 609 (446 hosts) — origem provável: script de discovery/LLD vCenter que atribuiu UUID/user/password por VM (contexto do utilizador, 2026-07-04) | ◐ | **8 pares com UUID repetido encontrados.** 4 já resolvidos em sessões anteriores (`VS8000112`/`VS8000493`, `VS9000319`/`VS8000319`, `VS8000867`/`VS8000914`, `VS8000526`/`VS8000888` — validação de que o método funciona). **4 novos, tratados hoje:** (1) `VS9000316` — **não era duplicado, era erro de atribuição de UUID** (tinha o UUID de outra VM real, `VS9000312`/`VS8000371`); confirmado o impacto real — os 2 hosts Zabbix mostravam dados VMware idênticos (CPU/RAM/hypervisor), ou seja `VS9000316` estava a mostrar as métricas erradas em produção; **corrigido** o macro para o `instance_uuid` real (`500d62aa-...`), obtido via API do vCenter PRD. (2) `ibm-cp-worker-04`/`VS8000982` e (3) `ibm-cp-worker-05`/`VS8000984` — duplicados reais (mesmo UUID/IP), nomes Zabbix desactualizados face ao hostname actual (`worker1`/`worker3.okd-dev.bpc.intranet`, cluster OpenShift/OKD) — **corrigido**: mantido `host` técnico (`VS80009XX`), `name` (visible name) actualizado para reflectir o hostname real, duplicados `ibm-cp-worker-04/05` desactivados. (4) `VS8000863`/`VS8000481` — **pendente**, pertencem ao vCenter PowerFlex (credencial errada, Z.8), IPs diferentes entre si (por confirmar se é o mesmo erro de atribuição de UUID que (1) assim que houver acesso). **Escala confirmada 2026-07-04 (auditoria Fase B, todo o Zabbix Infra, 650 hosts)**: 456 hosts têm macro `VM.UUID`; **nenhum duplicado novo fora do grupo 609** — os mesmos 7 pares, problema totalmente delimitado. Só falta o (4) para fechar 7/7 |
| Z.26 | P2 | `LTTI44_CLONE` — host Zabbix legado `LTTI44` órfão (mesmo IP/função, sem vCenter, nunca reportou) coexiste com o registo rico e correcto `LTTI44_CLONE` (tags completas, `{$VMWARE.VM.UUID}`) | ☐ | Confirmado 2026-07-04 ao destravar WinRM em `LTTI44_CLONE`: config do agente tinha `Hostname=LTTI44` (o órfão) em vez de `Hostname=LTTI44_CLONE` (o registo correcto) — corrigido junto com o bug `ServerActive` (ver Fase 14.15). Mesmo padrão de clone-com-nome-desalinhado já visto (`VS8000888`/`VS8000526`, `VD5000UCALL005`/`VD5000AD005`), mas aqui **ainda não se decidiu** desactivar o órfão `LTTI44` nem limpar o sufixo `_CLONE` do visible name de `LTTI44_CLONE` — por decidir |
| Z.27 | P2 | **Limpeza dos items MSSQL órfãos nas 4 VMs ToBe/Match cash** (`VS8000401`/`VS8000427`/`VS8000923`/`VS8000454`, hostids 10560/10573/10574/10575) — rescaldo do unlink-sem-clear do template MSSQL do incidente GLPI | ☑ | **Executado 2026-07-07 com aprovação explícita do utilizador.** Auditoria da instância revelou 21.310 items unsupported (36% do total); ~1.900 concentrados nestas 4 VMs: items `mssql.*`/`db.odbc.*` órfãos (`templateid=0`, nenhum template MSSQL ligado) a falhar com ODBC `IM002` porque as macros `{$MSSQL.DSN}`/`{$MSSQL.PORT}` nem existem nos hosts. Apagadas **28 discovery rules** `db.odbc.discovery[*]` (cascata: 2.160 items LLD) + **312 items plain** = 2.472 items e ~1.080 triggers órfãs removidos, 76 problemas MSSQL fantasma fechados, connection strings com UID/PWD em plaintext eliminadas. Backup completo pré-delete (`output:extend` de items+rules+triggers) em `bpc-workspace/backup-items-mssql-orfaos-4vms-20260707-121752.json`. Resultado verificado: 0 alvos restantes; unsupported nos 4 hosts 1.921→109 (restam 81 VMware=Z.8 PowerFlex + 24 `vfs.fs` CD-ROM + 4 `net.tcp.service` com `{$MSSQL.PORT}` partida — estes 4 fora do âmbito aprovado, apagar ou corrigir à parte); instância 21.310→19.498. **Lição API**: apagar discovery rules primeiro faz o `item.delete` seguinte falhar com "No permissions to referred object" por corrida com a cascata — re-consultar existência e apagar individualmente. Os 10 `service.info["MSSQL*"]` restantes são LLD legítimo do template Windows (serviços reais), não tocar. **FASE 2 executada no mesmo dia (aprovação explícita)**: a medição sem limite de amostragem revelou mais **8 hosts activos com a mesma assinatura, 618 items órfãos cada** — `VS9000424`/`VS9000406`/`VS9000400`/`VS9000301`/`VS8000347`/`VS8000718`/`VS8000791`/`VS8000825` (hostids 10577/10597/10598/10604/11687/11714/11949/11964). Apagadas **56 discovery rules + 4.944 items** (624 plain + 4.320 LLD), ~2.160 triggers órfãs, 150 problemas fantasma fechados. Backup em `bpc-workspace/backup-items-mssql-orfaos-fase2-8vms-20260707-124136.json`. Verificado: 0 alvos restantes; **unsupported da instância 19.498→15.873** (total das 2 fases: 21.310→15.873, −25%). **Restos fora de âmbito ainda por decidir**: (a) items/rules `db.odbc` órfãos em hosts DESACTIVADOS (hostids 10317/10327/10328/11991/11993/14073 — limpar quando se decidir o destino desses hosts); (b) 12 `net.tcp.service[...,{$MSSQL.PORT}]` órfãos com macro partida (4+8 hosts das 2 fases); (c) ~~fix do filtro LLD `vfs.fs` do template Windows~~ **FEITO 2026-07-07 (aprovado)**: o LLD "Mounted filesystem discovery" (rule 272708) do template 10351 tinha o **filtro vazio** (0 condições) apesar das macros `{$VFS.FS.*}` certas existirem no template — por isso descobria disquetes `A:`, CD-ROMs `D:`/`E:` e mountpoints temporários do EMC NetWorker (`BBBMountPoint`). Repostas as 6 condições standard do template oficial (`{#FSDRIVETYPE}/{#FSNAME}/{#FSTYPE}` MATCHES/NOT_MATCHES com as macros respectivas; `FSDRIVETYPE=fixed` é o que exclui CD-ROM/removíveis — nunca filtrar por letra de drive, `D:` é disco real em muitos servidores). Herança confirmada nos **295/295 hosts activos**. Os ~1.254 items partidos ficam "lost" no próximo ciclo LLD (delay 1h) e são apagados automaticamente ao fim do lifetime de 30d (≈2026-08-06). Backup da rule pré-alteração em `bpc-workspace/backup-lld-vfsfs-template10351-20260707-125639.json`; rollback = remover as condições antes dos 30d. **Vigiar nos próximos dias**: se algum host tiver agente antigo que não devolva `{#FSDRIVETYPE}` no discovery, TODOS os filesystems desse host ficam lost (assinatura: host com 0 items vfs.fs "descobertos" activos) — nesse caso rever o filtro antes do lifetime expirar; (d) o template real `BPC MSSQL by ODBC` (64 hosts, 77 items cada) é legítimo — não tocar |
| Z.28 | P1 | **Desligado o template `BPC Web Monitoring` (12042) dos 26 hosts sintéticos** — reescrita necessária (muitos falsos positivos, thresholds apertados) | ☑ | **Executado 2026-07-07 com instrução explícita do utilizador**, no seguimento da triagem dos 4 problemas `disaster` activos (achados: 2/4 eram o mesmo monitor duplicado com URL desalinhada — HTTP 400 por IP em vez de hostname —, 1 era endpoint obsoleto/porta fechada há 2,5 meses, só o EURONET era outage real em curso). Em vez de corrigir monitor a monitor, decisão do utilizador: desligar o template todo para reavaliação de raiz. Unlink-and-clear (`host.update` com `templates_clear`) de `BPC Web Monitoring` nos 26 hosts "Monitor da URL" (25 activos + `BPCNET — Internet Banking` desactivado) — removeu **108 web scenarios (httptest) + 315 items + 213 triggers** (5 em PROBLEM no momento, incl. os 4 disasters). Mantido o template `BPC Ping` nos 26 hosts (não pedido para remover) — ficam só com ICMP (234 items `icmpping`/`bpc.icmp.*`), sem monitorização de aplicação. Backup completo pré-unlink (`output:extend` de hosts+httptests+steps+items+triggers+macros) em `bpc-workspace/backup-unlink-bpcwebmonitoring-20260707-131310.json`. Verificado: 0 httptests/items/triggers do template restantes nos 26 hosts; problemas activos da instância 589→583. **O template em si não foi apagado** (fica disponível para reescrita) — só desligado dos hosts. **Pendente (decisão do utilizador, antes de religar)**: reavaliar `{$HTTP.CODE.OK}` (aceitava 200/301/302 — pode ter mascarado redirects indevidos), thresholds `{$TEMPO.NORMAL}=5s`/`{$TEMPO.LENTO}=10s` (identificados como "pequenos"/apertados para as apps reais), e corrigir por app antes de religar: `{$URL}` do SMS Banking (porta 8091 recusa ligação há 2,5 meses — confirmar endpoint actual), `{$URL}` de Sisgenda/Sala Reuniões (app exige hostname, não aceita IP — obter hostname DNS correcto), `status_codes` fixo `200` no monitor CONTIF do host Sala Reuniões (app responde 301) |
| Z.29 | P1 | **19 hosts ESXi duplicados** (template `VMware Hypervisor`) — mesmo padrão do Z.25 (duplicação por script de discovery vCenter→Zabbix), mas em hosts físicos, nunca antes auditado | ☑ | **Encontrado 2026-07-07** na triagem dos 583 problemas activos: os 19 ESXi com o template `VMware Hypervisor` (grupo `BPC / INFRAESTRUTURA / SERVIDORES FISICOS`) têm todos um duplicado exacto — mesmo IP de interface, mesmo template, mas `host` técnico = **UUID literal** (ex. `b5250000-0100-0000-0000-000000000001`) em vez de hostname, no grupo genérico **`Applications`** (nunca classificado). 844 triggers nesses 19 duplicados, responsáveis por **117 dos 583 problemas activos (20%)** — incluindo boa parte do "falso positivo" já conhecido de Z.8 (datastore free space PowerFlex), agora confirmado a estar a ser contado a dobrar. **Executado com aprovação explícita do utilizador**: `host.update status=1` (desactivado, não apagado) nos 19 hostids `14656/14657/14658/14659/14660/14661/14662/14663/14664/14665/14666/14667/14668/14670/14671/14672/14673/14674/14675`. Backup completo (`output:extend` hosts+interfaces+templates+macros) em `bpc-workspace/backup-hosts-esxi-duplicados-20260707-132928.json`. **Achado colateral importante**: desactivar o host pára a recolha mas **não fecha os 117 problemas já abertos** — ficam presos em PROBLEM para sempre (só resolvem com dados novos, que o host desactivado deixou de gerar) — e nenhuma das 117 triggers tem `manual_close` activo, logo não dá para fechar manualmente via UI/API sem tocar na definição da trigger (que é template-wide, afectaria os 39 hosts do template, fora de âmbito). **Tentativa de opção (b) e achado da causa raiz real (2026-07-07)**: `host.update templates_clear` do template `VMware Hypervisor` (12061) reportou sucesso na API mas **não teve efeito** — confirmado que os 19 hosts têm `flags=4` (`ZBX_FLAG_DISCOVERY_CREATED`): são Host Prototypes de LLD (`vmware.hv.discovery`), cujo link ao template é reimposto automaticamente a cada ciclo de discovery pelo vCenter-pai; qualquer unlink manual é revertido, e `discoveryrule.delete` directo falha com "cannot delete inherited LLD rule" pela mesma razão. **Causa raiz identificada**: cruzando os 7 vCenters que correm `vmware.hv.discovery`, `sv9000206 (vCenter PowerFlex)` (hostid 11900) e `vCenter 02` (hostid 14650) apontam ao **mesmo endpoint** `https://10.10.232.84/sdk` — duplicação já registada em Z.7/Z.8 — explicando os 9 duplicados do lado PowerFlex (`10.10.232.65-73`); os 10 duplicados do lado PRD (`10.10.101.x`) são suspeitos de vir de `sv9001206 (Vcenter DR)` (macro `{$VMWARE.URL}` não definido no host — por confirmar se é linked-mode a replicar o inventário do PRD ou macro mal herdada). **Conclusão**: o fix definitivo não é ao nível destes 19 hosts — é decidir/desligar o registo de vCenter duplicado (mesma decisão pendente de Z.7/Z.8), depois disso os host prototypes fantasma deixam de ser recriados e podem ser limpos/expirados normalmente. **Estado seguro actual**: os 19 continuam desactivados (`status=1`, confirmado persistente, não é revertido pelo LLD — só o template o é); os 117 problemas ficam presos em aberto até essa decisão de topologia de vCenters ser tomada. **Investigação da causa raiz aprofundada e RESOLVIDO 2026-07-07**: confirmado que as **7 instâncias de `vmware.hv.discovery` estão todas DESACTIVADAS** (2 definições-mestre nos templates `VMware FQDN`/10366 e `VMware`/12062 + 5 cópias herdadas nos hosts vCenter) — ou seja, não havia recriação activa; o bloqueio ao `templates_clear` era só a regra da API que impede unlink manual em hosts `flags=4`, independente da regra estar activa. Como a fonte está confirmadamente inactiva, apagar os hosts a sério (`host.delete`) é seguro (não podem ser recriados). **2 pares de vCenter duplicados identificados**: `sv9000206 (vCenter PowerFlex)` ↔ `vCenter 02` (mesmo endpoint `https://10.10.232.84/sdk`, mesmo user `agonzaga@vsphere.local` — confirmado, já em Z.7/Z.8) e `sv9001206 (Vcenter DR)` ↔ `vCenter Backup Cluster Swift` (mesmo IP `10.10.101.30`, mesmo user `administrator@vsphere.local` — muito provável, URL do DR oculta). **Ambos os pares continuam por resolver** (decidir qual registo desligar em cada um) — sem urgência técnica agora que a discovery está desactivada, mas evita reincidência se a discovery for reactivada no futuro. **Achado de segurança colateral (não relacionado com a duplicação em si)**: o host `Vcenter DR` (11890) tem os tipos de macro `{$VMWARE.PASSWORD}`/`{$VMWARE.URL}` trocados — a password ficou gravada como texto normal (visível via API) em vez de Secret, e foi impressa inadvertidamente num script de diagnóstico desta sessão. **Acção urgente pendente do utilizador**: rotacionar essa password no vCenter (fora do Zabbix) + corrigir os tipos de macro (`usermacro.update`, escrita pequena, por aprovar). **Delete final executado 2026-07-07 com aprovação explícita**: `host.delete` dos 19 hostids (mesma lista). Backup final (`output:extend` completo, pós-desactivação) em `bpc-workspace/backup-FINAL-delete-hosts-esxi-duplicados-20260707-135020.json`. Verificado: 0 hosts restantes, hosts principais (`sv9000640`/`sv9000650`/`sv9000316`/`sv9001647`) intactos com os seus items de datastore normais. **Problemas activos da instância: 583→465 (−118, −20%)**. **Macro corrigido 2026-07-07** (`usermacro.update`, tipos trocados de volta ao normal: `PASSWORD`→Secret(1), `URL`→Text(0), valor preservado em ambos, sem reenvio): ao tornar o `{$VMWARE.URL}` legível, o valor real é **vazio** — corrige a hipótese anterior: `Vcenter DR` não é um duplicado funcional do `vCenter Backup Cluster Swift`, é um **registo de vCenter incompleto/nunca configurado** (sem endpoint definido, qualquer chamada VMware falharia de imediato). O único par de vCenter duplicado **confirmado** continua a ser `sv9000206 (vCenter PowerFlex)` ↔ `vCenter 02` (mesmo endpoint real). Backup do estado anterior do macro (com o valor antigo, para rollback) em `bpc-workspace/backup-macros-vcenterdr-20260707-135645.json` — **não partilhar este ficheiro**. Rotação da password no vCenter em si continua pendente (acção do utilizador, fora do Zabbix). **Investigação do par PowerFlex/vCenter02 concluída e RESOLVIDA 2026-07-07**: cruzando `auditlog.get` com o documento pré-existente `documentacao/z8-diagnostico-vcenter-credenciais.md`, confirmado que `sv9000206` (11900) é o registo **original** (editado por um utilizador nomeado `jmuque` em 22-28/04, listado no Z.8, grupos correctos `HYPERVISORES`/`VCENTER`, 3 templates incl. `BPC Ping`, inventário manual) e `vCenter 02` (14650) é o **duplicado** (criado só a 06/05 por `Admin`, nunca mais editado, grupos errados `AMBIENTE/PRODUCAO`+`LOCALIZACAO` em vez de infra, só 1 template, inventário desactivado) — e por estar nos grupos errados, **nunca apareceu na auditoria de group=608 do Z.8**, ficando invisível até esta investigação. Endpoint `10.10.232.84` confirmado a responder à rede em ambos (só falta a credencial, Z.8). **Executado com aprovação explícita**: `host.update status=1` no hostid 14650. Backup completo (`output:extend`) em `bpc-workspace/backup-host-vcenter02-20260707-140608.json`. Verificado: `vCenter 02` `status=1`. Único registo activo de PowerFlex agora é `sv9000206` (11900) — quando a credencial do Z.8 for corrigida, só este vai gerar discovery, sem risco de duplicar de novo. **Verificação pós-desactivação (2026-07-07)**: confirmado o mesmo comportamento dos 19 ESXi — desactivar não fechou as 9 triggers já em PROBLEM do `vCenter 02` (ficaram presas). **`host.delete` executado com aprovação explícita** para as fechar de vez. Backup final (`output:extend` completo) em `bpc-workspace/backup-FINAL-delete-vcenter02-20260707-141140.json`. **Verificação profunda confirmou tudo limpo**: 0 hosts/items/triggers com o hostid 14650, 0 problemas activos atribuídos a "vCenter 02", `sv9000206` (o registo correcto) intacto com os seus 56 items. A contagem total da instância (471→472) subiu por ruído normal de fundo (4 problemas novos e não-relacionados abriram noutros hosts nesse intervalo — disco, ICMP, memória, temperatura), não por falha desta limpeza |
| Z.30 | P1 | **Desligado o template `BPC Web Monitoring - Externos` (12045) dos 13 hosts de sistemas externos** — template gémeo do Z.28, nunca antes auditado | ☑ | **Encontrado 2026-07-07** ao investigar 2 problemas novos no bucket "Aplicação Web" (`BNA-PIF` disparado no próprio dia, `EMP` aberto desde 17/04) que não batiam com a lista de 26 hosts do Z.28 — resolveu-se que é um **segundo template**, `BPC Web Monitoring - Externos`, monitorizando sistemas de terceiros: BNA (7 sistemas — SINOC, PIF, Pré-aviso, SGMC, Sigma, RTGS/SPTR, SSIF), MINFIN, INSS (2), EMP, Mundial Seguro, Pumangol — 13 hosts (12 activos + `INSS Portal BPC` já desactivado), mesma arquitectura L1-L4 do template já tratado. `BNA-PIF` resolveu-se sozinho (blip transitório) antes de qualquer acção; só `EMP` estava persistente (mesmo padrão "endpoint desactualizado" dos outros 3 disasters do dia). **Executado com instrução explícita do utilizador**: unlink-and-clear (`host.update templates_clear`) nos 13 hosts — removeu **56 web scenarios + 159 items + 109 triggers** (1 em PROBLEM, o `EMP`). Mantido `BPC Ping` (único outro template coexistente, confirmado antes do unlink). Backup completo (`output:extend` hosts+httptests+steps+items+triggers+macros) em `bpc-workspace/backup-unlink-bpcwebmonitoring-externos-20260707-154835.json`. **Lição de verificação**: o filtro `item.get(hostids=X, templateids=Y)` confirmado hoje como não-fiável (achado durante a investigação do Grupo A `VMware Guest`, ver nota) — verificação feita pelo método correcto (contagem total de items no host + inspecção das chaves restantes, não pela contagem "templateid"), confirmando 0 httptests e 117 items totais nos 13 hosts, todos `bpc.icmp.*`/`icmpping`. Problema `EMP` confirmado fechado (`problem.get` no host devolve lista vazia). **Pendente**: mesma reavaliação de thresholds/URLs do Z.28 antes de religar; `EMP` em particular precisa de confirmação do endpoint actual (mesma causa suspeita dos outros 3) |
| Z.31 | P2 | **Password root do vCenter/ESXi PowerFlex expirada** (`sv9000206`) — desde 27/04, nunca renovada | ☐ | Achado 2026-07-07 na triagem de problemas activos: `VMware: Root user password expired` + `expires in 0 days` activos há mais de 2 meses. Risco de acesso administrativo ao hypervisor se não for renovada. Fora do âmbito do Zabbix (acção teria de ser feita directamente no vCenter/ESXi) — só registado aqui, não escalado |
| Z.32 | P3 | **2 hosts SNMP mortos adicionais, fora do escopo original do Z.9** | ☐ | Achados 2026-07-07 na triagem: `Dell EMC Data Domain (Backups)` (sem SNMP desde 08/05) e `svucs015644 [Cisco UCS - 6248UP]` (sem SNMP desde 09/05) — mesmo sintoma "No SNMP data collection" que os 2 hosts já conhecidos do Z.9 (`Storage IBM FS9500`/`FS9200`). Ainda não investigada a causa (community string, ACL, SNMP desligado no device) — juntar ao mesmo fluxo de resolução do Z.9 quando for atacado |
| Z.33 | P1 | **`VS8000704 (PMSI - CEPH)` — swap quase esgotado há quase 1 ano** (aberto desde 2025-07-15), dado confirmado fresco, não é alarme obsoleto | ☐ | Achado 2026-07-07 na triagem de problemas activos. Valor ao vivo confirmado: **0,00248% de swap livre** (106 KB livres de 4 GB total), item recolhido no momento da verificação — condição real e persistente, não resíduo de configuração antiga. Nó de armazenamento distribuído CEPH da PMSI. Por decidir: se é tolerável para este tipo de workload (alguns storage nodes toleram swap alto por desenho) ou se é uma fuga de memória nunca tratada em quase 1 ano — precisa de investigação/decisão de alguém com contexto da arquitectura CEPH da PMSI, fora do âmbito desta auditoria Zabbix |
| Z.34 | P1 | **`Canal de denúncias - VS8000741` — Windows Defender desligado há 11 dias** (`WinDefend` + `MDCoreSvc` parados desde 2026-06-26) | ☐ | Achado 2026-07-07 na triagem de problemas activos "is not running (startup type automatic)" — distinto dos falsos positivos de `disabled`/`manual` (Z. pendente de fix do filtro LLD): este é um serviço `automatic` genuinamente parado. Host de produção sem antivírus/EDR activo é uma exposição de segurança real, não ruído de threshold. Também no mesmo host: `edgeupdate`, `GoogleUpdaterInternalService`, `GoogleUpdaterService` parados (menor prioridade). Recomendado escalar à equipa de segurança — fora do âmbito de correcção via Zabbix |
| Z.35 | P3 | **`VS8000758` reporta o hostname `VS8000765`** — desalinhamento de inventário, não é duplicado (UUIDs diferentes, ao contrário do padrão Z.25) | ☐ | Achado 2026-07-07 no bucket "System name has changed" (3 casos, só este persiste — os outros 2 já reportam o nome certo, alarmes transitórios). Confirmado via `{$VMWARE.VM.UUID}`: `VS8000758` (activo, UUID `5023ddb6-...`) e o host `VS8000765` já registado à parte (desactivado, UUID `50239f71-...`, diferente) — **sem UUID partilhado**, logo não é o mesmo bug de atribuição do Z.25. Parece ser uma VM renomeada no SO (de `VS8000758` para `VS8000765`, provavelmente a substituir a antiga `VS8000765` desactivada) sem o registo técnico Zabbix ter sido actualizado. Housekeeping de inventário, sem risco de dados a contar a dobrar — decidir se se renomeia o host técnico Zabbix para bater com o hostname real, e o que fazer ao registo `VS8000765` antigo desactivado |
| Z.36 | P1 | **Falsos positivos estruturais em serviços Windows `disabled`/`manual`** — LLD `service.discovery` não filtrava por tipo de arranque | ☑ | Achado 2026-07-07 na triagem: a trigger `"{#SERVICE.NAME}" ({#SERVICE.DISPLAYNAME}) is not running` (expr `{item}<>0`) dispara para qualquer serviço descoberto, incluindo os configurados como `disabled`/`manual` — que por desenho nunca correm, gerando alarme permanente e garantido. **Executado com aprovação explícita do utilizador (2 escritas)**: (1) `discoveryrule.update` na regra `service.discovery` (itemid 42545, template `Windows by Zabbix agent active`) — condição `{#SERVICE.STARTUPNAME} MATCHES '.*'` trocada por `MATCHES '^auto'`, mantendo o filtro de nome existente; herança confirmada nos 295/295 hosts activos. Backup em `bpc-workspace/backup-lld-servicediscovery-template10351-20260707-163740.json`. (2) `item.delete` dos 9 items falsos positivos já activos (`SQLBrowser` ×5, `MSSQLLaunchpad`, `OracleServicebpcqadb`, `WinRM` ×2 — itemids 503890/503895/503899/503910/504082/504084/504100/504116/504124), arrastando as respectivas triggers/problemas. Backup em `bpc-workspace/backup-items-servicos-disabled-manual-20260707-163819.json`. **Verificado após a corrida da cascata (mesma lição de hoje — esperar/re-consultar antes de concluir)**: bucket "is not running" caiu de 19 para **10, todos `automatic`** (genuínos, por investigar caso a caso — WinRM num Jump server, Task Scheduler/SQL Agent em Forgest, Defender desligado já registado em Z.34, etc.). Problemas activos da instância: 456→449. Filtro previne recorrência; os `automatic` restantes ficam para triagem individual, não são falso positivo |
| Z.37 | P2 | **66 triggers `MSSQL: Failed to fetch info data` órfãos, presos em PROBLEM desde 2026-04-25** — ligação trigger→item partida, não relacionado com a limpeza MSSQL do Z.27 | ☑ | Achado 2026-07-07 (sessão de continuação) ao triagem dos triggers activos com "SQL" no nome (89 no total). Confirmado via `trigger.get` com `expandExpression: true` que os 66 triggers resolvem para `*ERROR*=1` (item/host por trás já não existe) — dado de `lastchange` mostra que ficaram presos há 73 dias, muito antes de qualquer acção de hoje; mesma lição já registada (desactivar/apagar sem fechar o problema deixa-o preso para sempre). Verificado antes de apagar: todos `status=0` (habilitados), `templateid=0` (não herdados), `flags=0` (não LLD) — sem complicação de cascata. **Executado com aprovação explícita do utilizador**: `trigger.delete` dos 66 triggerids. Backup pré-delete em `bpc-workspace/backup-triggers-sql-orfaos-20260707-PRE-DELETE.json`. Verificado pós-delete: 0 dos 66 continuam em `problem.get`. Dos restantes 23 triggers "SQL is not running/disk space" activos: `VS8000409` tem a instância `MSSQLSERVER` inteira parada (4 serviços em simultâneo) — prioridade a investigar a seguir; maioria dos outros é `SQLBrowser`/`ADHelper` (possível ruído crónico, por confirmar se é intencional) |
| Z.38 | P3 | **`VS8000409` (Talentia - BD) — SQL Server 2012 relacional morto há ~5 anos, não é incidente** | ☑ | Investigação do achado do Z.37 (instância `MSSQLSERVER` "parada" há 91 dias segundo o trigger). **Inspecção via WinRM** (`vm-admin-cred.xml`, `%USERPROFILE%`, só leitura, host nomeado e confirmado explicitamente): `Get-Service` confirma `MSSQLSERVER`/`MSSQLLaunchpad`/`SQLBrowser` parados e só `MSSQLServerOLAPService` (Analysis Services) a correr. **Prova conclusiva**: o `ERRORLOG` do próprio motor (`MSSQL11.MSSQLSERVER` — SQL Server 2012) tem a última escrita em **2021-09-07**; nenhum evento `7036` (Service Control Manager) para "SQL Server (MSSQLSERVER)" existe em todo o log `System` retido (desde 2026-03-22) — o serviço nunca transitou de estado nesse período porque já estava parado antes. A data "2026-04-08" do trigger é apenas quando a `service.discovery` descobriu o serviço pela primeira vez, já morto — não é a data em que parou. **Conclusão**: não é incidente a escalar, é ruído estrutural (serviço automatic descoberto já mono, mesma família do Z.36) — motor relacional legado, provavelmente substituído há anos (só o OLAP ainda corre). **Pendente**: confirmar com o dono do Talentia se a BD relacional é mesmo dispensável antes de remover o item/trigger de discovery deste host |
| Z.39 | P2 | **Correcção ao achado das 12 VMs `vProxy-EMC Networker`: 8 já têm agente real a funcionar, escondido atrás de um registo Zabbix duplicado e morto** | ☑ | Achado 2026-07-08, disparado pelo utilizador ao reparar num problema activo "Zabbix agent is not available" em `VS8000110_vProxy-EMC Networker` e perguntar se era o mesmo host já analisado. Confirmado que **cada VM vProxy tem 2 registos Zabbix distintos para a mesma máquina** (mesmo IP, hostids diferentes): `GUEST-OS - VSxxxxxxx (vProxy VxBlock)` com template `Linux by Zabbix agent` (passivo) — **este tem o agente real, fresco** — e `..._vProxy-EMC Networker` com `VMware Guest` + `Linux by Zabbix agent active` — o registo **duplicado e morto** (`agent.ping` nunca teve dado), que era o único que eu tinha inspeccionado antes de concluir "agente nunca instalado" (conclusão agora corrigida). Método: `host.get` por substring do nome devolve os 2 registos; compara-se `interfaces[].ip` (igual) e `agent.ping.lastclock` (um fresco, outro `0`) — mesma técnica dos duplicados ESXi (Z.29) e UUID trocado (Z.25). **Resultado nas 12 vProxy**: 8 com duplicado `GUEST-OS` e agente fresco (`VS8000110/111/101/105/100/120/121/122`, já monitorizadas correctamente), 1 com duplicado mas sem dado (`VS8000109`), 3 sem duplicado nenhum (`VS8000145`/`VS8000114`/`VS8000116`, continuam sem agente confirmado). **Pendente**: decidir se se desactiva/apaga os 8+1 registos `..._vProxy-EMC Networker` duplicados e mortos, já que a monitorização real já existe no `GUEST-OS` — mesma família de limpeza do Z.29. Documentação corrigida em `estado-monitorizacao-producao.md §2.1` |
| Z.40 | P2 | **Revisão alargada a hosts duplicados por IP partilhado — 32 grupos (68 hosts) na instância toda** | ☑ | Achado 2026-07-08, motivado directamente pelo Z.39 ("se há 2 supostamente correctos, porque um dá erro?") — alargou-se a verificação de duplicados por IP a todos os 630 hosts da instância (`host.get` global, sem selects pesados, sem impacto de performance). **Método**: agrupar hosts por `interfaces[].ip`, cruzar com frescura do `agent.ping` por host — mesma técnica validada no Z.39/Z.29/Z.25. **32 IPs partilhados por 2+ hosts (68 hosts no total)**, divididos em 4 categorias: **(A) 11 já conhecidos/resolvidos** de sessões anteriores — vCenter PRD (`sv9000204`/`SV9000204`), vCenter DR (`sv9001206`/`VS9001206`/`vcenter-backup`, 3 registos), as 8 vProxy (Z.39), e o cluster FINASTRA com IPv6 link-local partilhado (`VS8000835/836/833/834/830`, já documentado como não-alcançável). **(B) 6 duplicados "zombie" confirmados** (mesmo padrão da vProxy — um lado fresco, outro sem dado de agente nenhum): `VS8000810`(vivo)/`VS8000810_netbox`(morto); `LTTI44_CLONE`(vivo)/`LTTI44`(morto — o "original" é que está morto, invertido); `VS8000806_Excel Report`(vivo)/`VS8000806`(morto); `VS8000808_Excel Report`(vivo)/`VS8000808`(morto); `tacacsubuntu`(vivo)/`TACACS UBUNTU`(morto); `Zabbix server`(vivo)/`VS8000115`(morto — o próprio servidor Zabbix monitorizado 2x). **(C) 2 pares suspeitos de IP reatribuído entre sistemas DIFERENTES** (nomes/funções sem relação nenhuma na mesma IP — `VS8000219` Sophos MTA vs `VS9000346` IBM IB9; `VS9000319` Talentia QA vs `VS8000319` Sftp pmsi já desactivado) — **não tratar como duplicado de limpeza sem confirmar UUID vCenter primeiro**, mesma cautela do Z.25. **(D) 8 pares em que nenhum lado tem `agent.ping` fresco** — precisam de outra fonte (ICMP/vCenter UUID) para decidir qual é o registo real: `VS8000855`/`vs8000855` (Sigcap), `VS8000711`/`VS8000816` (PMSI), `VS8000740`/`vs8000740` (Ebankit), `VS8000474`/`vs8000474` (SGC), `VS8000519`/`VS8000519_Revovery`, `VS8000888`/`VS8000526`, `VS8000112`/`VS8000493` (2º já desactivado), `VS8000867`/`VS8000914` (2º já desactivado). Snapshot completo em `bpc-workspace/all_hosts_snapshot_20260708.json`. **Pendente**: investigar a categoria D (em curso) e decidir limpeza da categoria B |
| Z.41 | P3 | **Categoria D do Z.40 aprofundada com UUID VMware — 3 pares afinal já conhecidos, 5 com padrão novo "maiúsculas sem UUID vs minúsculas com UUID"** | ☑ | Continuação do Z.40. ICMP não serve para desempatar (é por IP, dá "fresco" nos dois lados sempre — mesma máquina física a responder). **Cruzamento por `{$VMWARE.VM.UUID}`**: `VS8000888`/`VS8000526`, `VS8000112`/`VS8000493`, `VS8000867`/`VS8000914` têm **UUID idêntico confirmado** — o primeiro já estava documentado no `CLAUDE.md` desde 2026-07-04 (`VS8000371/VS8000526/VS8000888` duplicados reais); os outros dois seguem o padrão simples (mesma VM, 2º já desactivado) — housekeeping de baixo risco, não urgente. **5 pares com padrão novo**: `VS8000855`/`vs8000855`, `VS8000711`/`VS8000816`, `VS8000740`/`vs8000740`, `VS8000474`/`vs8000474` — em todos, o host em **maiúsculas** não tem macro `{$VMWARE.VM.UUID}` nenhuma (criado manualmente antes do script de import vCenter existir), o outro lado (minúsculas/nome de negócio) tem UUID (registo importado do vCenter). Nenhum dos 5 pares tem `agent.ping` fresco nos dados Zabbix — impossível desempatar sem inspecção directa. `VS8000519`/`_Revovery` parece cópia de recovery/DR, já desactivada, baixa prioridade. **Pendente**: inspecção WinRM/SSH dos 5 pares novos, host a host nomeado |
| Z.42 | P3 | **Inspecção WinRM confirma: nos 3 pares Windows do Z.41, o `ComputerName` real bate com o registo em maiúsculas (sem UUID), não com o de nome de negócio** | ☑ | Continuação do Z.41. **Inspecção via WinRM** (`vm-admin-cred.xml`, `%USERPROFILE%`, só leitura, hosts nomeados e confirmados explicitamente): `Get-CimInstance Win32_OperatingSystem` + `$env:COMPUTERNAME` nos 3 IPs Windows do par — `10.10.203.18`→`VS8000855` (Server 2022), `10.10.228.133`→`VS8000740` (Server 2019), `10.10.238.55`→`VS8000474` (Server 2016). Nos 3 casos o `ComputerName` real da máquina **bate exactamente com o registo Zabbix em maiúsculas** (`VS8000855`/`VS8000740`/`VS8000474` — sem UUID, criado manualmente) e **não** com o registo de nome de negócio em minúsculas com UUID (`vs8000855`/`vs8000740`/`vs8000474` — criado pelo import vCenter). Conclusão: os registos com UUID/nome de negócio são a duplicação — nomeados pelo *display name* da VM no vCenter em vez do hostname técnico real, e nenhum agente reporta a nenhum dos dois lados hoje (achado de "sem agente" continua válido, é ortogonal ao duplicado). Par `VS8000711`/`VS8000816` (PMSI, Linux, `10.10.203.76`) confirmado só porta 22 aberta (WinRM falha como esperado, é Linux) — inspecção SSH pendente de decisão de credencial, mesma limitação já registada na investigação vProxy (§ handoff). **Pendente**: decidir limpeza dos 3 registos duplicados confirmados (`vs8000855`/`vs8000740`/`vs8000474`) e decidir via de acesso SSH para o par PMSI |
| Z.43 | P2 | **Veredito e limpeza dos 6 duplicados "zombie" confirmados por `agent.ping` (categoria B do Z.40)** | ☑ | Achado 2026-07-08 — fecho do levantamento de duplicados desta sessão (Z.39-Z.42): veredito final de **25 máquinas com registo Zabbix duplicado** identificadas (9 vProxy + vCenter PRD + vCenter DR + 6 zombies categoria B + 3 UUID já conhecidos + 3 confirmados por WinRM + 1 PMSI por confirmar + 1 recovery/DR baixa prioridade); 2 pares adicionais suspeitos ficaram de fora por não terem UUID cruzado (podem ser IP reatribuído entre VMs diferentes, não duplicado real). **Executado com aprovação explícita do utilizador**: `host.delete` dos 6 duplicados mortos confirmados por `agent.ping` — `VS8000810_netbox` (14173), `LTTI44` (11985), `VS8000806` (11951), `VS8000808` (11953), `TACACS UBUNTU` (14283), `VS8000115` (14276). Backup pré-delete completo (host+items+triggers+problemas) em `bpc-workspace/backup-hosts-zombies-duplicados-20260708-PRE-DELETE.json` — confirmado antes de apagar que cada um tinha só 1 problema activo ("Zabbix agent is not available"), nada mais a perder. Verificado pós-delete: os 6 já não existem, os 6 registos vivos mantidos (`VS8000810`, `LTTI44_CLONE`, `VS8000806_Excel Report`, `VS8000808_Excel Report`, `tacacsubuntu`, `Zabbix server`) continuam intactos. **Pendente**: os restantes ~19 duplicados do veredito (9 vProxy, 3 UUID já conhecidos, 3 confirmados por WinRM, vCenter PRD/DR, PMSI) — decisão de limpeza fica para sessão seguinte |
| Z.44 | P2 | **Limpeza dos 2 zombies do grupo vCenter PRD/DR — inclui remover a exposição de password do Z.29** | ☑ | Continuação do Z.43, grupo vCenter (4 registos investigados). Inspecção confirmou 2 zombies claros: `SV9000204` (hostid 14265, `VMware Guest`+agente Windows morto, "agent not available") — **o mesmo host onde a password do vCenter PRD estava exposta em macro não-Secret** (achado do Z.29/handoff §8.3) — e `VS9001206` (hostid 14266, "vCenter Appliance", agente Linux morto, mesmo padrão). O 3º registo do lado DR (`sv9001206` "Vcenter DR", template `VMware` real mas `{$VMWARE.URL}` vazio, nunca configurado) **não é um duplicado do `vcenter-backup`** — é um conector legítimo mas incompleto, decisão à parte (consertar ou desactivar), não tratado nesta limpeza. **Executado com aprovação explícita do utilizador** (com reconfirmação depois do classificador de auto-mode bloquear a 1ª tentativa por ter uma escrita de macro potencialmente sensível a persistir — corrigido gravando o backup com a password redactada antes do delete): `host.delete` de `SV9000204`/`VS9001206`. Backup (macros de password redactadas) em `bpc-workspace/backup-hosts-vcenter-zombies-20260708-PRE-DELETE.json`. Verificado: 0 restantes, os 3 registos reais (`sv9000204`, `sv9001206`, `vcenter-backup`) intactos. **Pendente**: decidir `sv9001206` (URL vazio) e os restantes ~17 duplicados (9 vProxy, 3 UUID já conhecidos, 3 WinRM, PMSI) |
| Z.45 | P2 | **Limpeza das 9 vProxy-EMC Networker duplicadas (Z.39)** | ☑ | Continuação do Z.44. Verificado antes de apagar: os 9 candidatos (`..._vProxy-EMC Networker`, hostids 14284/14287/14288/14289/14291/14292/14293/14294/14295) tinham só **9 problemas activos no total, todos "Zabbix agent is not available"** — nada mais a perder, incluindo `VS8000109` (o único par onde nenhum lado tem agente a reportar hoje; mantido por ser o registo com o nome técnico correcto, mesma lógica dos outros 8). **Executado com aprovação explícita do utilizador**: backup gravado em passo separado do delete (aprendida a lição do Z.44 — nunca combinar backup+delete no mesmo script quando o backup pode conter segredo), `bpc-workspace/backup-hosts-vproxy-duplicados-20260708-PRE-DELETE.json` (macros de password redactadas por precaução, nenhuma encontrada nestes 9). `host.delete` dos 9 hostids. Verificado pós-delete: 0 restantes, os 9 registos `GUEST-OS` (`VS8000100/101/105/109/110/111/120/121/122`) intactos. **Restam ~8 duplicados**: 3 UUID já conhecidos (housekeeping baixo risco), 3 confirmados por WinRM (Sigcap/Ebankit/SGC), par PMSI (SSH pendente), e a decisão em aberto do `sv9001206` (não é duplicado, é conector incompleto) |
| Z.46 | P2 | **Limpeza dos 3 duplicados confirmados por WinRM (Z.42) — Sigcap/Ebankit/SGC** | ☑ | Continuação do Z.45. Verificado antes de apagar: `vs8000855`/`vs8000740`/`vs8000474` (hostids 14255/14108/14244) tinham só **3 problemas activos, todos "Zabbix agent is not available"** — nada mais a perder. **Executado com aprovação explícita do utilizador**: backup em passo separado (`bpc-workspace/backup-hosts-winrm-duplicados-20260708-PRE-DELETE.json`, sem macros de password). `host.delete` dos 3 hostids. Verificado pós-delete: 0 restantes, os 3 registos reais confirmados por WinRM (`VS8000474`/`VS8000740`/`VS8000855`) intactos. **Restam ~5 duplicados**: 3 UUID já conhecidos (housekeeping baixo risco, 2º lado já desactivado), par PMSI (SSH pendente), e a decisão em aberto do `sv9001206` (URL vazio, não é duplicado) |
| Z.47 | P3 | **Limpeza dos 3 duplicados já conhecidos por UUID (Z.25/CLAUDE.md 2026-07-04) — housekeeping do 2º lado já desactivado** | ☑ | Continuação do Z.46. `VS8000493`/`VS8000888`/`VS8000914` (hostids 11670/11955/11990) já estavam `status=1` (desactivados desde a sessão de 04/07) — só 1 problema preso ("Zabbix agent is not available") entre os 3, nada mais a perder. **Executado com aprovação explícita do utilizador**: backup em passo separado (`bpc-workspace/backup-hosts-uuid-conhecidos-20260708-PRE-DELETE.json`, sem macros de password). `host.delete` dos 3 hostids. Verificado pós-delete: 0 restantes, os 3 registos reais (`VS8000112`/`VS8000526`/`VS8000867`) intactos. **Total apagado nesta sessão: 23 hosts duplicados.** **Restam 2 casos**: par PMSI (`VS8000711`/`VS8000816`, SSH pendente de decisão de credencial) e `sv9001206` ("Vcenter DR", não é duplicado — é conector com `{$VMWARE.URL}` vazio, decisão de consertar/desactivar à parte) |
| Z.48 | P2 | **Causa-raiz fechada: 9 hosts com Zabbix Agent 2 parado desde Junho (§9.4/§9.5 do handoff 04/07) — reboot de patch de Windows, não recupera sozinho por problema no SCM** | ◐ | Retomada a investigação em aberto desde 04/07 sobre `VS8000435`/`VS8000742`/`VS8000800`/`VS8000821`/`VS8000848`/`VS8000849`/`VS8000850`/`VS8000851`/`VS8000897` (config `Server`/`ServerActive`/`Hostname` correcta, serviço só parado, `Start-Service` já tinha falhado com Evento 7009). **Inspecção via WinRM em `VS8000850`** (`vm-admin-cred.xml`, host nomeado e confirmado): `Get-WinEvent` no System log em torno de 2026-06-12 00:44 mostra 2 reboots em sequência — `svchost.exe` (00:42, "Service pack") e `TrustedInstaller.exe` (00:44, "Operating System: Upgrade") — confirmado com `Get-HotFix` que **KB5094128/KB5094147 (security update) instalaram em 2026-06-11**, mesma janela dos outros 8 hosts (10-14/06, datas diferentes = patch a rolar por lotes). Descartada a hipótese de VMware Tools (versão 12.4.5.23787635 sem alteração desde Maio). **Nova tentativa de `Start-Service` hoje (com aprovação explícita) — falhou de novo, mesmo erro.** `zabbix_agent2.log` confirma: nenhuma linha nova escrita desde "Zabbix Agent 2 stopped" de 06-12, mesmo depois da tentativa de arranque — o processo não chega a inicializar logging, aponta para problema no registo do serviço/SCM (não configuração do agente). Binário verificado íntegro (`zabbix_agent2.exe`, 26.630.448 bytes, 2026-04-09, `LocalSystem`, path/args correctos). **Conclusão**: causa-raiz do crash original fechada (patch de Windows); a não-recuperação precisa de correcção mais séria (reinstalar/re-registar o serviço) — acção maior, não tentada hoje, decisão a tomar por VM nomeada. **Pendente**: replicar o diagnóstico rápido (Get-HotFix + zabbix_agent2.log) nos outros 8 hosts para confirmar se seguem o mesmo padrão antes de decidir a correcção em lote |
| Z.49 | P1 | **Confirmado 8/8: os 9 hosts "syscall signal" partilham a mesma janela de patch de Windows — e piloto de correcção em `VS8000435` revela 3 bugs empilhados, não só o crash do patch** | ☑ | Continuação directa do Z.48. **Diagnóstico replicado via WinRM em lote** (`vm-admin-cred.xml`, 8 hosts nomeados e confirmados: `VS8000435/742/821/848/849/851/897`/`VS8000740`) — confirmado 8/8 com o mesmo padrão exacto: `Zabbix Agent 2` `Stopped`/`Automatic`, patches de segurança (KB5094122/123/128/141/143/147, todos 10-14/06) instalados no mesmo dia do crash, `zabbix_agent2.log` sem nenhuma linha desde "stopping service due to syscall signal". Sistémico confirmado, não coincidência. **Piloto de correcção em `VS8000435`** (aprovação explícita nomeada para cada passo, 3 escritas distintas): (1) `--uninstall`/`--install` inicial falhou com "cannot parse configuration at line 1: missing assignment operator" no `mssql.conf` incluído — **BOM UTF-8 confirmado** (`EF BB BF`), o mesmo bug já corrigido na fonte em 04/07 mas nunca aplicado retroactivamente (ficheiro gerado 2026-05-19, mais de um mês antes da correcção) — corrigido reescrevendo o ficheiro sem BOM (`UTF8Encoding($false)`), reinstall passou a funcionar. (2) Arranque em foreground (`-f`) revelou 2º bloqueio: **plugin NVIDIA fatal** (`nvml.dll` em falta, VM sem GPU) — descoberto que o `Include=...\zabbix_agent2.d\` do Zabbix lê **todos os ficheiros da pasta**, não só `*.conf` (renomear para `.disabled` não bastou, foi preciso mover o `nvidia.conf` para fora da pasta de include, `plugins-desativados\`). (3) Novo erro revelou 3º bloqueio: **parâmetro `Plugins.MSSQL.Default.Timeout` desconhecido** pela versão do plugin instalada (desalinhamento template vs binário) — corrigido removendo só essa linha do `mssql.conf` (decisão do utilizador: corrigir o parâmetro, não desactivar o plugin). Arranque em foreground limpo (`Starting Zabbix Agent 2 (7.4.9)`, sem erros) → `Start-Service` bem sucedido → **confirmado no Zabbix: `agent.ping` fresco (71s), 0 problemas activos**. **Implicação importante**: o crash do patch de Windows foi só o gatilho — a causa de não recuperar sozinho é esta pilha de 3 bugs de configuração pré-existentes (BOM + NVIDIA + parâmetro MSSQL obsoleto), que provavelmente afecta muitos mais hosts fleet-wide com `mssql.conf` gerado antes de 04/07 ou com o plugin NVIDIA presente sem GPU — não é exclusivo destes 9. **Achado lateral de segurança (repetido)**: a password do `mssql.conf` (`zabbix_monitor`) foi impressa em claro 2× durante a inspecção — mesma classe de incidente já registada para o vCenter PRD, mas neste caso é uma conta de leitura de métricas, não administrativa. **Pendente**: replicar a mesma sequência de fix nos outros 8 hosts confirmados (não tentado ainda — cada um pode ter uma combinação diferente destes 3 bugs); considerar uma varredura fleet-wide por BOM em `mssql.conf` e por `nvidia.conf` presente em hosts sem GPU, já que o piloto sugere que o problema é maior do que os 9 conhecidos |
| Z.50 | P1 | **Os 9 hosts "syscall signal" — todos recuperados, 100% (9/9 `agent.ping` fresco)** | ☑ | Fecho do Z.48/Z.49. Replicada a sequência de fix validada em `VS8000435` (corrigir BOM se existir, mover `nvidia.conf` se existir, remover linha `Plugins.MSSQL.Default.Timeout` se existir, reinstalar e arrancar) aos outros 7 hosts confirmados explicitamente (`VS8000740/742/821/848/849/851/897`) num único script em lote por WinRM — **7/7 tinham exactamente os mesmos 3 bugs**, mesma correcção, todos terminaram `Running`. `VS8000850` (o primeiro investigado, antes de descobrir os 3 bugs) tinha ficado por corrigir — aplicada a mesma sequência com confirmação nomeada à parte, também recuperado. **Verificação final no Zabbix**: 9/9 hosts com `agent.ping` fresco (14s-108s). Nota lateral de diagnóstico: um `Select-String` sobre uma sessão WinRM devolveu vazio para `ServerActive`/`Server` num host apesar de estarem presentes — confirmado ser um artefacto de serialização do PSRemoting com objectos `MatchInfo`, não um problema real; usar `.Contains()`/`-match` sobre uma string simples em vez de `Select-String` ao inspeccionar ficheiros remotos via `Invoke-Command`. **Balanço da frota de Produção**: mais 9 agentes recuperados hoje, para além dos ganhos da limpeza de duplicados (Z.43-Z.47) |
| Z.51 | P1 | **Varredura fleet-wide por BOM/NVIDIA — mais 3 hosts recuperados, achados os limites do que dá para varrer sem WinRM** | ☑ | Varredura pedida após o Z.50, com âmbito controlado (lição do `CLAUDE.md` sobre custo/benefício de auditorias em massa): restringida via API aos hosts Windows `parados` do grupo 609/Produção (60), excluindo SWIFT (14, bloqueado por política) e o cluster PowerFlex (1, credencial em falta), depois excluindo IPs não-roteáveis (`fe80::`/APIPA, 6 hosts, já documentados "não alcançável") e hosts já conhecidos bloqueados por outras causas (UAC 2008, acesso negado, isolamento de rede) — **45→23 candidatos**. `Test-WSMan` em lote: só **7/23 respondem** (os outros 16 são o mesmo bloqueio "WinRM fechado" já documentado em 04/07, não um achado novo). Verificação de leitura (BOM + presença de `nvidia.conf`) nos 7: **3 eram exactamente o caso já identificado em 04/07 e nunca fechado** (`VS8000413`/`VS8000852`/`VS8000898` — BOM já corrigido nessa altura, mas ficaram presos no NVIDIA porque a técnica de "mover para fora da pasta" só foi descoberta hoje no Z.49). **Executado com aprovação explícita nomeando os 3 hosts**: mesma sequência de fix do Z.49/Z.50. **Verificado no Zabbix: 3/3 `agent.ping` fresco.** Os outros 4 hosts alcançáveis são casos diferentes, não BOM/NVIDIA: `VS8000133` (serviço já `Running`, não reporta — problema de conectividade/firewall, não de configuração), `VS8000800` (usa Zabbix Agent v1, não v2 — já documentado como caso à parte), `VS8000826` (serviço Zabbix nem existe — precisa de instalação nova, não de fix), `VS9000359` (falha transitória de WinRM, não verificado). **Conclusão da varredura**: o universo de hosts *verificáveis* para este bug específico está esgotado por agora — os restantes 16+ candidatos exigem resolver primeiro o bloqueio de WinRM fechado (já documentado, fora do âmbito desta varredura). **Total de agentes recuperados nesta sessão: 12** (9 do Z.50 + 3 deste achado) |
| Z.52 | P2 | **Retomados os 16 bloqueados por WinRM do Z.51 — 1 desbloqueado com credencial já conhecida (agente instalado de novo), 8 "só ping" reconfirmados dead-end** | ☑ | Cruzamento dos 16 com o handoff de 04/07: 8 são "só ping — nada mais responde" (dead-end já documentado), 4 precisam de 3ª credencial (`VS9000268`/`VS8000518`/`VS8000924`/`VS8000925`, fora do que se pode fazer sem caçar credenciais), 2 são Server 2003 com decisão de arquitectura pendente (`VS9000235`/`SV9000401`), 1 é possível dispositivo edge/DMZ deliberado (`VS8000394`) — **1 tinha caminho conhecido em aberto**: `VS8000343` já tinha autenticado com sucesso em 04/07 usando a 2ª credencial (`vm-admin-cred2.xml`), nunca reaproveitada. **Reteste dos 8 "só ping"** (`Test-NetConnection` a SMB/WMI/RDP/WinRM): **0 portas novas abertas**, 100% igual a 04/07 — dead-end reconfirmado, não achado novo. **`VS8000343` investigado**: autenticação OK com `vm-admin-cred2.xml`, mas **o serviço Zabbix Agent nem existia** — não era o bug BOM/NVIDIA, era ausência total de instalação (VM "Talentia - DCH Recrutamento", Produção, PowerFlex/`CLS-BPC01`, nunca teve agente). **Executado com aprovação explícita nomeando o host**: MSI `zabbix_agent2-7.4.9.msi` copiado de `VS8000435` (já corrigido hoje) via máquina local como intermediária (`Copy-Item -FromSession`/`-ToSession`, sem transferência directa entre 2 sessões remotas), instalado só o Agent 2 base (sem plugin MSSQL — VM de camada aplicacional, não de BD) via `msiexec` com `SERVER`/`SERVERACTIVE=10.10.126.22`/`HOSTNAME=VS8000343`, regra de firewall TCP 10050 criada, serviço arrancado. **Verificado no Zabbix: `agent.ping` fresco (8s)** — primeira vez que esta VM de produção tem monitorização real. **Fecha o lote dos 16**: nada mais accionável sem decisão do utilizador (credencial nova, Server 2003, ou confirmação sobre `VS8000394`) |
---
---

## Pontos Alto ainda abertos (registo)

Resolvidos os bloqueadores e médios; nenhum Alto crítico em aberto após
2026-06-16 (ancoragem, precedência, nav N1→N2 e sondagem ficaram documentados).
Manter este espaço para novas dívidas técnicas que surjam por fase.
